node_modules/
dist/
*.tsbuildinfo
.next/
coverage/
.playwright-cli/
*.log
.DS_Store
.env
.env.*
!.env.example
test-results/
playwright-report/
# Runtime data working dir (e2e harness mirrors the canonical api/assets/demos spine here;
# server featured-builds/sandboxes also land here). Not source - never committed.
ekoa-data/

# run-ekoa-code driver screenshots (.claude/skills/run-ekoa-code). Local proof only.
.ekoa-run/

# >>> autothing >>>
# evidence binaries + liveness state (resume-critical small files stay versioned)
**/*.cast
docs/autothing/runs/**/*.webm
docs/autothing/runs/**/*.mp4
docs/autothing/runs/**/*.png
docs/autothing/runs/**/status.json
docs/autothing/runs/**/owner.lock.json
# <<< autothing <<<
RUN_LOG.knowledge-import.txt
api/tests/evidence/
.walkthrough/auth.json

# Compiled operator assistant panel runtime (operator-run G2): built at api build time
# from api/assets/panel-runtime/src, served by serving.ts. Build artifact - never committed.
api/assets/panel-runtime.js

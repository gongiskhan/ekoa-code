{
  "$comment": "Suite ledger (chapter 14 \u00a714.2.5). Maps every ported test artifact from spec/reference/test-audit.md to the gate at which it must first be green. Skeleton written at G-P; per-file rows and their exact new paths are finalized when Phase 1 (G1) ports the estate. Before its gate a red/non-runnable artifact reports 'skipped (awaiting G<N>)'. At/after its gate, red = gate failure. Ratchet: once green, never skipped/red later. The schema-coverage allowlist (shared/ domains not yet landed) shrinks at every domain gate, empty at G9.",
  "version": 1,
  "runId": "20260706-182515-9080f2cb",
  "currentGate": "G12",
  "playwright": {
    "band1_zero_change": {
      "targetGate": "G9",
      "source": "ekoa-dev/ekoa/e2e/",
      "specs": [
        "ui-foundation",
        "shell-nav",
        "coherence-locale",
        "pages-core",
        "pages-flagship",
        "pages-manage",
        "integrations-sections",
        "integrations-pipedream",
        "integration-session-automations",
        "legal-knowledge",
        "demos",
        "legal-shared-drift"
      ]
    },
    "band2_fixture_swap": {
      "targetGate": "G9",
      "note": "assertions unchanged; seeding-helper bodies rewritten to ch03 REST; onboarding also swaps one POST /api/v1/chat/runs stub",
      "specs": [
        "onboarding",
        "vertical-profile",
        "artifacts-apps-section",
        "update-from-bundle",
        "artifact-backend-panel"
      ]
    },
    "band3_served_app": {
      "targetGate": "G6",
      "note": "37 specs run UNMODIFIED against api/ alone; a failure is a byte-compat defect in the served-app plane, never a reason to edit the spec. legal-dossie also stubs /api/app-sso/me + /api/app-sso/m365/**",
      "specs": [
        "legal-nucleo",
        "legal-dossie",
        "legal-prazos",
        "legal-citius",
        "legal-agenda",
        "legal-kanban",
        "legal-tempos",
        "legal-recursos",
        "legal-pecas",
        "legal-pesquisa",
        "legal-apoio",
        "legal-honorarios",
        "legal-financas",
        "legal-cobrancas",
        "legal-contratos",
        "legal-contratos-gerar",
        "legal-modelos",
        "legal-forms",
        "legal-correio",
        "legal-portal",
        "legal-conflitos",
        "legal-kyc",
        "legal-calculos",
        "legal-assinatura",
        "legal-injuncoes",
        "legal-transcricao",
        "legal-rcbe",
        "legal-insolvencias",
        "legal-jurimetria",
        "legal-ret-cons",
        "legal-dropdown-persistence",
        "legal-launcher",
        "legal-cadeia-audiencia",
        "legal-cadeia-credito",
        "legal-suite",
        "legal-suite-journey",
        "demo-spine"
      ]
    },
    "retired": {
      "note": "behaviors re-covered by store-level unit halves + new schema-validated-stub specs at G9. 2026-07-31: RETIRED simuladores-trabalho. It drove a UI app at ekoa-data/apps/simuladores-trabalho that was never ported into this repo and is a featured artifact in NEITHER repo - so it built from a path resolving OUTSIDE the checkout and had failed with MODULE_NOT_FOUND on every run the estate has ever done. Nothing is lost: every figure it asserted (compensacao art. 366, faltas art. 251, ferias art. 238-239, aviso previo) is covered deterministically by api/tests/legal/simuladores.test.ts - 18 tests over the ported pure engine api/src/legal/simuladores.ts. What is NOT covered is the UI wiring of an app that does not exist here; if that app is ever ported, this spec comes back with it.",
      "specs": [
        "chat-fixes",
        "chat-preview-resolution",
        "simuladores-trabalho"
      ]
    },
    "band5_live_proof": {
      "targetGate": "OPERATOR-RUN",
      "note": "LIVE-MODEL proof specs: no run stubs - real agent turns against the credentialed boot-b stack (api/tests/journeys/boot-b.mjs; claudeAuth preflight fails loudly on an uncredentialed stack). Burn real model tokens (~8 turns, 8-15 min): run deliberately (npx playwright test web/e2e/part-b-proof.spec.ts), never in the per-PR lane - same posture as the operator-run drivers. part-b-proof (mega-run 20260717-190134 slice B7): the BRIEF \u00a74 Part B proof session - short/long Q&A scale tiers, live reply_summary card upgrade, 3-edit drafting flow with revisions 1->4 on ONE sheet (live sheets read) + auto-follow flash, revision card focuses the same sheet, chip dismiss -> new sheet, reload restore INCLUDING the persisted summary title (B7 finding 1: reply_summary is stamped onto the turn metadata at emit time and rehydrated by the transcript read), provenance best-effort branch (memoriesUsed / traceId), notifications-blocked summary degradation (B.E). Build-mode coexistence + mobile FAB are proven by the band4 stub specs (chat-layout-unified, mobile-sheet-feed), deliberately not redone live. Live-verified green at landing (slice B7); ratchet applies at its gate. 2026-07-18 (C7): registered voice-proof - the full manual + talking voice turn journeys the C4 band4_gap_plan note deferred here. ONLY the voice vendors are stubbed (Deepgram/Google keys absent, C6 blocked) - the agent is the real credentialed boot-b model, so every reply is a genuine live turn; a marker-frame WS override + a deterministic VAD test factory (both declared in use-voice-session.ts, gated behind window globals a production load never sets) make MANUAL send-now, TALKING auto-send-on-silence-endpointing + barge-in (clears playback, captures), and STANDBY + pending-note-QUEUED-as-next-turn (deviation memo c-voice-deviations.md \u00a7v) provable without a real mic or a real multi-minute wait. A fourth test (the 10-minute inactivity timeout, VOICE_INACTIVITY_TIMEOUT_MS shortened) is additionally env-gated on EKOA_VOICE_SHORT_INACTIVITY_MS and skips cleanly against a normally-configured boot-b. Reducer/driver correctness is NOT re-tested here (already zero-mock-covered in voice-machine.test.ts/session-driver.test.ts, C3/C4) - this spec proves the real-browser integration only. Live-verified at landing (slice C7); ratchet applies at its gate.",
      "specs": [
        "part-b-proof",
        "voice-proof"
      ]
    },
    "band4_gap_plan": {
      "targetGate": "G9",
      "note": "New specs from the ch13 \u00a713.6 coverage-gap plan; committed at the gate that lands the backend surface, due green when the UI exists. 2026-07-18 (C4): registered voice-modality - mega-run 20260717-190134 slice C4, the voice UI wiring floor vs the boot-b stack (mocked getUserMedia handing back a real silent MediaStream; everything downstream live: worklet asset, authenticated WS upgrade to the stub-provider relay, capturing state + PT-PT status, the insecure-context message path, and the ouvir footer action round-tripping the tts path). The full manual/talking turn journeys land at C7. 2026-07-18 (OS-mode run 1): registered os-mode - the OS-mode shell exit-gate spec (skips cleanly when NEXT_PUBLIC_OS_MODE is off). 2026-07-25 (2C-S6): registered document-redline - the document base v2 source-linked REVIEW surface (accept a tracked change / comment on a selection / reply in a thread / resolve it) driven through the REAL served app on the api-only plane, like the band3 byte-compat specs (no dashboard, no login). LLM-FREE AND HERMETIC: no agent is driven and no model is called - the app is the SHIPPED base scaffold (api/assets/bases/document/scaffold/frontend/src copied VERBATIM into a temp project; only documentData.js is written, which is the one file a build agent edits) handed to POST /api/dev/register so the api builds and serves it through the same pipeline + injected context as any generated app - so drift between the shipped base and this spec is a real defect, not a stale fixture; and the linked document is seeded DIRECTLY into <EKOA_DATA_DIR>/app-data/{appId}/docx (the two well-known blobs + meta sidecar apps/document-source.ts owns) from the committed web/e2e/fixtures/contrato-redline.docx, which already carries native tracked changes + a comment thread (regenerate: scripts/make-redline-fixture.mjs, which drives the product's OWN engine so the binary stays auditable). The proof is the FILE, not the DOM: the working .docx is downloaded from GET /api/app-docx/current, unzipped (fflate, already a web dep) and the OOXML asserted - surviving w:ins/w:del on the change left pending, the accepted change reduced to plain text, word/comments.xml carrying the original comment + the reply + the selection comment, and commentsExtended.xml w15:done=\"1\" for the resolved thread - plus a reload proving durability. Zero console errors (4xx/5xx pinned by URL from response events, regressions-dashboard's pattern). A SECOND test in the same spec is the additive half of the graft: the SAME shell with no sourceDocument (i.e. every document app built before this feature) must behave exactly as before - authored blocks incl. ekoa-code's own pagebreak/signatures types, the plain 'Descarregar Word' toolbar, the notes tab, no redline markup on the sheet, and NOT one request to /api/app-docx. 2026-07-25 (2C-S6 ux-qa loop-back): the spec grew a THIRD test and the first one grew the recourse leg. uxqa-1 (accept/reject rewrote the real .docx in ~163ms with no confirm and no undo) is closed by a NEW POST /api/app-docx/restore route - shared AppDocxRestoreResponse + contract test + schema-coverage entry in the same change - re-deriving the working copy from the pristine document-source.docx, surfaced as a confirmed 'Repor original' toolbar action; test 1 now drives that round trip and asserts the FILE goes back (the accepted change is a tracked change again, the reply and added comment are gone). uxqa-2 (raw English engine text '- Action 1 Failed: Target ID 9999 not found.' in a PT-PT app) is closed by a failure-family mapper in the scaffold, pinned by the new test 2, which provokes a REAL 422 with no stub (commenting on a phrase that occurs several times makes the target ambiguous) and asserts the toast is PT-PT with a remedy and carries no 'Action'/'Failed'/'Target ID', no leading dash, no em dash and no internal numeric id. Registered in the same change that lands the file; ratchet applies from landing. 2026-07-31: registered settings-redirects - the users/orgs/pedidos pages moved into the settings tab group and the three old routes became redirects, which NOTHING asserted; a later tidy-up deleting three four-line redirect files would pass every gate in this repo and break every external link silently. Registered in the same change that lands the file; ratchet applies from landing. 2026-08-03 (C2): registered integration-write-gate - the `mutates: true` EXECUTION GATE's human half. The gate itself lives in the api executor (pinned by tests/integrations/action-consent.test.ts + tests/security/integration-write-gate.test.ts); this spec is the place a human actually answers it, driven on the SHIPPED Slack package because it carries exactly the two shapes the gate distinguishes (send_message mutates, list_channels does not). Asserts the full round trip against the real api with no stubs: needs-approval -> the dialog NAMES the three facts a human needs (integration, action, and the real destination POST https://slack.com/api/chat.postMessage, not a paraphrase) -> cancel records nothing -> approve-always flips the chip and offers a revoke -> a RELOAD reads the state back off the api (a dialog with no persisted effect would pass a click-only test and fail this one) -> revoke returns the action to the gate. Also pins the Rule-7 half: the mutates:false action gains NO chip, no control and no prompt. Re-runnable by construction (it clears any standing approval first and revokes what it granted); nothing is executed against Slack - approving records an answer, it does not call the integration. Zero console errors + no non-asset 404s. Registered in the same change that lands the file; ratchet applies from landing. 2026-08-03 (CS7): registered citius-sync-outcome - the Citius sync OUTCOME panel on /integrations, i.e. the one surface where a lawyer learns whether their inbox is provably complete. The three states (Completa / INCOMPLETA / Falhou) are asserted DISTINGUISHABLE the way a person perceives them: the spec reads COMPUTED styles off the live page (accent-bar width, border and surface colour, chip fill, headline size/weight) and requires INCOMPLETA to carry weight Completa does not and a colour family Falhou does not - a class-name rename cannot satisfy it. Plus the copy each state must carry (INCOMPLETA says notifications may be missing AND that the read pointer did not advance and the next run re-sweeps from the same point; Falhou says the sync never ran and never prints a per-run '0 notifica\u00e7\u00f5es', which would read as 'there were none'), and a fourth test pinning that a 404 (CS6's flag defaulting OFF) renders NO panel at all. The api rail is flag-gated off and drives a real court portal, so the three outcomes come from stubs of the ONE state endpoint, each validated in-spec against the shared SyncStateView schema; login, the integrations page and every other call are live. Zero console errors + no non-asset 404s (the flag-off test scopes a single expected 404 to that one URL). Registered in the same change that lands the file; ratchet applies from landing. 2026-08-03 (D3): registered integration-achieve - `achieve` (EXECUTE-OR-AUTHOR) end to end. The browser is the HUMAN (real UI login, a real gateway key minted on the real /settings/api-keys page, the real write-gate dialog on the integrations page) and an ordinary HTTP client carrying that key is the AGENT, so the property that matters is asserted across a real socket: the agent's goal is refused at C2's write gate with the typed consentRequest, the SAME key is refused 401 at BOTH self-approval doors (POST .../approval and D3's POST .../trust), the human approves on the real page, and only then does the identical call get past the gate - answering not_connected, i.e. Slack is never contacted. HERMETIC AND LLM-FREE by construction rather than by stubbing: every assertion sits on a path that refuses BEFORE any model call or outbound request - the gate answers before a credential is loaded, authoring on a SHIPPED package answers baseline_package before drafting, and the promotion route is refused at admission. The author arm's happy path is deterministic in api/tests/contract/integrations-achieve.test.ts, where only the model turn is faked at the seam. Re-runnable: it revokes the approval it grants and the key it mints. 2026-08-08 (WS8a): registered knowledge-shared-scope - the Biblioteca's `_shared` legal-corpus visibility fix (the operator's \"the knowledge base is empty\" report: the 262k+-document reserved corpus was always SEARCHABLE, never BROWSABLE - a fresh org's own vault is genuinely empty, which is what made it look empty). Two tests: (1) the scope toggle switches Fornecido between the org's own vault (live, real requests throughout - including the empty-looking default and the return trip) and `_shared`, asserts the three collections render their PT-PT names (Jurisprud\u00eancia/Legisla\u00e7\u00e3o/Legisla\u00e7\u00e3o laboral) and the collection filter narrows within that scope, and that a shared document offers NO delete affordance (read-only); (2) the search box surfaces a shared hit BADGED (\"Base p\u00fablica\") beside an org hit that carries no badge, and clearing the box returns to the tabbed browse view. STUBBED: there is no online write path to `_shared` (ch04 \u00a74.4.1) and, unlike the ported legal-* specs, no committed fixture backs it either - the 262k-document corpus is a one-off local import, so a CI runner's `_shared` starts genuinely empty and a real scope=shared request there would legitimately return zero rows; planting real files into a live `_shared` from a spec was also explicitly out of bounds (risk of contaminating a real corpus on any machine that already has one). So the three requests addressing `_shared` (collections?scope=shared, documents?scope=shared, search) are intercepted with fixtures validated in-spec against the shared CollectionsResponse/DocumentsResponse/KnowledgeSearchResponse schemas; every scope=org request, the login and the rest of the dashboard are live and unstubbed - a route handler branches on the `scope` query param and calls `route.continue()` for anything not addressed to `_shared`, so a regression in the real org-vault path fails this spec exactly as a stub-only spec could not catch. Registered in the same change that lands the file; UNVERIFIED against a live boot at registration time (task constraints excluded running the e2e suite) - ratchet applies once CI/a live run first proves it green, not before. 2026-08-08 (WS4b): registered composer-drop-attach - drives the composer drag-drop and paste-to-attachment feature (chat-panel.tsx / page.tsx: onDrop/onDragOver/onPaste handlers) against the real dev servers with a real UI login, so a dropped or pasted file actually reaches POST /api/v1/uploads and renders as a chip - no route stubbing. It belongs here rather than in band3_served_app because it needs the dashboard shell and a logged-in session, not just the served-app plane. Three tests: a dropped file stages as a visible chip; a long paste (>800 chars) stages as a text attachment and never fills the textarea; a short paste is left untouched (no preventDefault on the composer's paste handler), proving the hook does not interfere with normal typing/pasting. The in-session composer (chat-panel.tsx) shares the identical drop/paste handler logic and is covered at the unit level instead (components/chat-panel-composer) since reaching it here needs a session with content first. Registered in the same change that lands the file; ratchet applies from landing. 2026-08-15: registered app-sso-frame - the framed sign-in regression: the injected __ekoa.signIn() navigated the preview IFRAME into /api/app-sso/microsoft/start, a surface served with X-Frame-Options: DENY + frame-ancestors 'none' (and the provider's page refuses framing too), so every preview panel that reached Microsoft sign-in rendered 'refused to connect'. The runtime is now frame-aware and this spec pins the handshake, LLM-free and stub-free: (1) a page on the dashboard origin frames the featured Nucleo, a REAL click calls signIn() (production fidelity: activation gates popups there; the harness itself runs Chromium with --disable-popup-blocking, so the popup-BLOCKED branch - frame left alone + console warning - is pinned by the served-app contract strings, not here), and the assertions are that a NEW top-level window opens on the SSO leg (unconfigured e2e answers the start URL 503 - same URL, still top-level; an SSO-configured stack 302s straight to the provider, so every navigation assertion accepts start-URL-or-provider-authority) while the frame provably never leaves the app document; (2) the same click on the TOP-LEVEL app still navigates in place (the byte-compat path, unchanged). The injected-runtime strings are separately pinned in the served-app contract suite; the popup half (named '__ekoa_sso_' window reloads its framed opener and closes itself after the callback lands back on /apps/<id>/) was probe-verified at registration but is deliberately NOT a committed assertion (window.close timing is browser-dependent; the frame-side session poll (the quiet /api/app-sso/session probe against a popup-open baseline, reload only on session CHANGE; definition AND invocation string-pinned in the served-app contract suite) is the fallback that makes the committed half sufficient). Verified green at registration against a scratch --built stack, and verified to FAIL against the pre-change runtime (the framed test times out waiting for a popup that never opens) - the spec discriminates. Registered in the same change that lands the file; ratchet applies from landing. 2026-08-15 (same-day second pass): app-sso-frame gained a THIRD test - cancelling the popup must SETTLE the flow (the frame reloads), pinning a regression the first version of the fix introduced and that was measured on the real customer ERP: with the frame no longer navigating away, the app's own sign-in button stayed disabled with its spinner running forever because nothing told it the flow had ended. Verified green against the fixed runtime and RED against the old one (that run fails both framed tests), so the new test is not vacuous. 2026-08-17: registered schedules - the timer rail's UI driven end to end against the REAL api (no stubs): sidebar nav -> /schedules; create a MANUAL-task schedule through the real form (target card, recurrence builder, the SERVER-computed preview via POST /api/v1/schedules/preview); the row lists under the when-grouping with its humanised cadence; run-now (202) surfaces the pending run in the TASK INBOX; complete removes it; delete leaves the estate as found (re-runnable). LLM-FREE AND HERMETIC by construction: a manual target executes nothing - the pending run IS the behaviour - so no automation engine, no integration egress, no model. Zero console errors asserted. Registered in the same change that lands the file; UNVERIFIED against a live boot at registration time - ratchet applies once a live run first proves it green, not before.",
      "specs": [
        "automation-deterministic",
        "chat-thinking",
        "regressions-dashboard",
        "integration-builder",
        "bridge-presence",
        "device-approval",
        "gateway-keys",
        "privacy-grants-ledger",
        "trust-chip",
        "reference-attach",
        "live-bridge",
        "branding-colors",
        "chat-layout-unified",
        "sheet-feed",
        "summary-cards-chip",
        "mobile-sheet-feed",
        "voice-modality",
        "os-mode",
        "document-redline",
        "settings-redirects",
        "integration-write-gate",
        "citius-sync-outcome",
        "integration-achieve",
        "knowledge-shared-scope",
        "composer-drop-attach",
        "app-sso-frame",
        "schedules"
      ]
    }
  },
  "frontend_unit": {
    "targetGate": "G9",
    "source": "ekoa-dev/ekoa/__tests__/ (+ ekoa/lib/)",
    "surviving": [
      "execute-skip-user-message",
      "attachment-upload-refs",
      "navigation",
      "orchestration-hydrate-reconcile",
      "orchestration-persist-optout",
      "orchestration-queue-stop",
      "orchestration-thinking",
      "sanitize-error",
      "verticals",
      "components/data-backups-panel",
      "components/memory-page",
      "components/no-mode-picker",
      "components/thinking-block",
      "components/side-panel-preview-recovery",
      "components/usage-page",
      "components/users-page",
      "ui/badge",
      "ui/button",
      "ui/confirm-dialog",
      "ui/dialog",
      "ui/input",
      "ui/tabs",
      "ui/textarea",
      "ui/toast",
      "lib/artifact-bundle",
      "lib/stream-error-collision",
      "lib/format-tokens",
      "lib/preview-probe",
      "use-bridge-presence",
      "device-code",
      "bridge-local",
      "refusal-file-request",
      "lib/edit-intent",
      "orchestration-reply-summary",
      "voice/voice-machine",
      "voice/grace-window",
      "voice/wav",
      "voice/tts-playback",
      "voice/pcm-worklet",
      "voice/capture-support",
      "voice/voice-wire",
      "voice/session-driver",
      "voice/use-voice-session-unmount",
      "voice/voice-socket-lifecycle",
      "voice/latency-record",
      "os-tiling",
      "lib/cofre-state",
      "components/settings-layout",
      "components/sync-outcome-panel",
      "components/integration-lessons-panel",
      "integration-user-scoped-skill",
      "components/chat-stripes-featured",
      "lib/file-picker",
      "components/chat-panel-composer",
      "components/chat-runtime",
      "components/chat-error-retry",
      "lib/artifact-app-url",
      "schedules-recurrence-text",
      "schedules-store",
      "automations-needs-credentials",
      "components/schedule-detail-page",
      "components/schedule-form-error",
      "components/schedules-page",
      "lib/schedules-authority",
      "components/run-status-badge"
    ],
    "census_note": "2026-07-10: registered 5 files that had accrued on disk unledgered (orchestration-persist-optout, sanitize-error, components/memory-page, components/usage-page, components/users-page \u2014 added by post-rc fix batches) plus the thinking-channel pair (orchestration-thinking, components/thinking-block); the count census was failing before this reconciliation. 2026-07-14: registered refusal-file-request (landed by the BRIEF-9a commit 8996048, whose message said 'ledgered' but never touched this list \u2014 the census was red again). 2026-07-18: registered the voice/ trio (voice-machine, grace-window, speakable) - mega-run 20260717-190134 slice C3, the NEW pure voice reducer estate (Node, zero mocks per BRIEF \u00a75); registered in the same change that lands the files, ratchet applies from landing. 2026-07-18 (C5): voice/speakable RELOCATED, not retired - the module moved to api/src/voice/text/speakable.ts (the TTS pipeline is api-side; FIXED-1 forbids either side importing the other and shared/ is contract-only - see docs/decisions.md) and its test moved VERBATIM to api/tests/voice/speakable.test.ts (ledgered under the C5 module group; the ratchet follows the assertions, which never went red). Registered the C5 playback pair voice/wav (WAV header parse + RIFF-length stream segmentation, iOS-strict) and voice/tts-playback (SentenceQueue ordering, sync-in-tap unlock machine, defensive resume, barge-in flush - hand-rolled fake AudioContext, zero mock libs). 2026-07-18 (C4): registered the capture-chain quartet - voice/pcm-worklet (drives THE ACTUAL public/voice/pcm-downsample.worklet.js asset with stubbed worklet globals: native-rate->16k resample, fractional carry across blocks, clamping, packing, RMS level, flush), voice/capture-support (secure-context gate + the mobile/desktop AGC constraints selector), voice/voice-wire (WS url/token framing, zod-validated parse/serialize both directions, locale->lang map, SentenceAssembler boundaries, SpeechChannel one-say-at-a-time serialization + barge-in flush), and voice/session-driver (the host driver end-to-end with injected fakes: full manual + talking turns, barge-in gated + short-burst immunity, standby + pending note, inactivity shutdown, disconnect honesty, the latency-mark trail). Registered in the same change that lands the files; ratchet applies from landing. 2026-07-18 (C4 review fixes): registered voice/use-voice-session-unmount (unmount closes the TTS socket + playback AudioContext, jsdom) and voice/voice-socket-lifecycle (stt drops late frames after a deliberate close; tts clear() during socket-opening cancels a stale say) - the codex-found lifecycle regressions, pinned in the same change. 2026-07-18 (C7): registered voice/latency-record - the client-side per-turn latency collector (lib/voice/latency-record.ts) that folds session-driver.ts's per-mark onLatencyMark stream into ONE record per turn (mirroring api/src/voice/session.ts's server-side per-turn stage clocks); full-turn close on tts_first_audio, next-audio_in close for manual/no-tts turns, barge_in self-tags the turn IT opens (the reducer only emits barge_in from 'speaking', i.e. after the interrupted turn already closed cleanly on its own tts_first_audio - so barge_in never retroactively mutates an already-logged record). Registered in the same change that lands the file; ratchet applies from landing. 2026-07-31: registered components/settings-layout - the users/offices/pedidos pages moved OUT of the top-level nav INTO a settings tab group, so a new layout became the only route to three admin surfaces; registered in the same change that lands the file, ratchet applies from landing. Its role gating is asserted as VISIBILITY only, deliberately: the refusal is the API's and is proven in api/tests, and a web-side claim to authorize would leave a dropped server check looking covered. 2026-08-03 (CS7): registered components/sync-outcome-panel - the Citius sync outcome panel's state mapping, its copy, and the transport that feeds it. Three groups: (a) the PURE mapping (which of Completa/INCOMPLETA/Falhou/never a SyncStateView renders as, the incomplete reason chosen in the strength order of the proof, the streak note), including the rule that 'Completa' is never shown when the state row and the embedded report disagree - a stale row cannot produce a false all-clear; (b) the panel itself against a mocked transport (three distinct data-outcome hooks, a failed panel that never prints a per-run landed count, a 404 that renders nothing, the needs-human / needs-egress run notices); (c) the REAL transport module over a fake fetch, which pins the load-bearing guarantee that a body failing SyncStateView.safeParse becomes an error rather than a rendered sync claim. Registered in the same change that lands the file; ratchet applies from landing. 2026-08-03 (C3): registered components/integration-lessons-panel - the per-integration lessons textarea. Its assertions are almost entirely about the two ways a textarea loses somebody's work: the ceiling REFUSES rather than trims (over-limit text is rendered in full, the counter states the overage, the save is blocked and the transport is never called), and a concurrent save is SURFACED rather than resolved (the draft stays exactly as typed, the stored version is shown beside it, and both exits - keep theirs / write mine over it - are explicit clicks, with the guarded save carrying the loaded updatedAt and only the overwrite dropping it). Plus the read-only branch, which is a claim about who may see raw bytes: editable:false means the api returned the SCRUBBED view, so no save is offered at all. Registered in the same change that lands the file; ratchet applies from landing. 2026-08-05: registered integration-user-scoped-skill - it landed on disk with commit 105e10b (a user-built integration invisible in \"Minhas Integra\u00e7\u00f5es\") without a ledger row, so the count census had been red ever since; found and reconciled during the ekoa-dev parity run, unrelated to the change that found it. 2026-08-06: registered components/chat-stripes-featured - the chat empty-state featured card FORKED the app instead of opening it, so a user who clicked \"use this\" got a second copy in their gallery and no way to tell which one their edits went into (ekoa-dev c4f7f2c6; /artifacts had already been fixed to edit in place, so the two surfaces disagreed about what \"use this\" means). The load-bearing assertion is NEGATIVE - api.artifacts.fork is never called - because asserting only the navigation would still pass with a fork firing alongside it, which is exactly the shape the bug had. Registered in the same change that lands the file; ratchet applies from landing. 2026-08-08 (WS4a): registered attachment-upload-refs - pins the client-side half of the uploadId/attachmentId mix-up fix (useAgentExecution.ts's execute() was sending the composer chip's local id instead of the server-issued upload reference in FileAttachment.path; every build-with-attachment silently referenced an id the server never minted). Registered in the same change that lands the file; ratchet applies from landing. 2026-08-08 (WS4b): registered lib/file-picker, components/chat-panel-composer, components/chat-runtime - the composer drag-drop/paste-to-attachment feature and its FC-400 onReferencePicked wiring (chat-panel.tsx, page.tsx) landed with three new unit files. lib/file-picker covers stageFiles/stagePastedText and the shouldStageAsTextAttachment two-signal threshold (length>800 OR non-blank lines>6), including its boundary cases. components/chat-panel-composer covers the in-session composer's drop/paste wiring, the bridge-connected 'Referenciar (local)' hint (a drop/paste has no filesystem path, so it can only ever upload - the hint hangs off it instead of pretending otherwise), and the previously-dead onReferencePicked wiring into ComposerAttachMenu (chat-panel.tsx had a Paperclip menu that could never actually attach a local-bridge reference). components/chat-runtime is the first coverage at all for the ~1100-line chat-runtime.tsx provider - send-while-building queueing, the plain chat-send payload reaching POST /chat/runs, and the FC-400 reference-mint success/failure paths - a zero-test gap flagged after the provider shipped the uploadId/attachmentId bug WS4a's fix (attachment-upload-refs, above) closes. Registered in the same change that lands the files; ratchet applies from landing. 2026-08-11: registered components/chat-error-retry (finding run-error-text-leak: the terminal error bubble offers Retry iff the shared RUN_ERROR_RETRYABLE table says retrying can help). 2026-08-13: registered lib/artifact-app-url - the dashboard's own 'Usar' action opened a freshly built (unshared) artifact's BARE /apps/<slug>/ link, which answers 410 'Link ja nao disponivel'; getArtifactAppUrl now routes an unshared artifact through the owner preview token (Q-05) and keeps shareable links bare so a copied share URL never carries the owner's JWT. Registered in the same change that lands the file; ratchet applies from landing. 2026-08-17: registered the schedules pair - schedules-recurrence-text (the ONE humaniser that turns a ScheduleSpec into a sentence: every cadence at interval 1 and >1, both languages with PT-PT first, and the weekday edge sets - all seven, the working week, the weekend, and a Sunday-containing set that proves reading order is Monday-first even though the contract numbers Sunday as 0; relative-time WORDING is compared against ICU rather than frozen, so what is pinned is our unit selection, not ICU's copy) and schedules-store (the cache contract the pages depend on: pendingTasks re-derived from orgRuns on every write that touches them, mutations updating the cached array from the RESPONSE, a failed call leaving the cache untouched, and preview's failures returned to the FORM instead of the page-wide error). Registered in the same change that lands the files; ratchet applies from landing. 2026-08-19: registered the SIX files the count census had been red on (disk 65 vs ledger 59), closing `suite-ledger-unit-census-drifted-red-again` in docs/findings.md. FIVE were pre-existing and not this branch's: automations-needs-credentials (landed with 55572ee, the needs_credentials run state) and components/schedule-detail-page + components/schedule-form-error + components/schedules-page + lib/schedules-authority (landed together with f6b5233, the schedules adversarial-review fix batch) - both commits are on main and neither touched this list. The SIXTH, components/run-status-badge, is this branch's own (commit 6e2096a, the P4 schedule-outcome badge), registered in the change that found the census red, which is what the same-change rule requires. This is the THIRD occurrence of the identical drift (see the two entries above); it keeps recurring because `gate:ledger` is not in `npm run ci:lane`, so nothing forces the entry at merge - recorded as the open half of that finding rather than fixed here, because putting a gate that shells out to Playwright and the node drivers into the per-PR lane is a CI decision with its own blast radius and belongs in docs/decisions.md.",
    "mock_update_needed": [
      "orchestration-hydrate-reconcile",
      "components/data-backups-panel",
      "orchestration-queue-stop"
    ],
    "retired_as_static_audit": [
      "artifacts-page-wiring",
      "conversation-types (FC-109: deleted in W4 with its subject, the FC-107/108 client-side mode classifier + conversation-types module; \u00a712.5)"
    ]
  },
  "node_drivers": {
    "source": "ekoa-dev/cortex/tests/e2e/",
    "test_client": {
      "file": "helpers/test-client.ts",
      "targetGate": "G1",
      "note": "ported first to the ch03 REST surface; the 14 drivers re-seed on it"
    },
    "drivers": [
      {
        "name": "app-auth",
        "targetGate": "G6"
      },
      {
        "name": "app-files-upload",
        "targetGate": "G6"
      },
      {
        "name": "citius-integration",
        "targetGate": "G6"
      },
      {
        "name": "erp-auth-ui",
        "targetGate": "operator-run salomao",
        "note": "was G6->G9->CUTOVER. Targets the brasilsalom\u00e3o ERP TENANT FORK (default APP_ID a machine-local fork UUID; emails @brasilsalomao.pt) with app-sso email/password gating + CRM + KYC + ops. The ported featured erp-imobiliario is a simpler financial+clientes ERP with NO app-sso email login (uses an accessCode gate; passwordSignIn/whoami/input[type=email] absent), CRM, or KYC - that fork is NOT in the ported reference catalog (spec \u00a714.1 reference-access) and is reconstituted at cutover (\u00a714.4, OUTSIDE this run). The G9 green ran against a non-committed local fork (ERP_APP_ID override); no committed content can satisfy it. Retargeted G9->CUTOVER (a post-run milestone) at G12 when the fresh e2e:server surfaced it. See RUN_LOG DEVIATION (2026-07-08, Phase 12). 2B-S5 proven-import: retargeted CUTOVER -> operator-run salomao (NOT cutover; docs/decisions.md) \u2014 runs against the locally imported prod instance with EKOA_E2E_SALOMAO_ID set; skips cleanly (exit 0 + SKIP) when it/the api is absent, never a false green."
      },
      {
        "name": "erp-crm-persistence",
        "targetGate": "operator-run salomao",
        "note": "was G6->G9->CUTOVER; same brasilsalom\u00e3o ERP tenant-fork gap (prospects/propostas CRM not in the featured erp-imobiliario; fork out of the ported catalog, reconstituted at cutover). See RUN_LOG DEVIATION (2026-07-08, Phase 12). 2B-S5 proven-import: retargeted CUTOVER -> operator-run salomao (NOT cutover; docs/decisions.md) \u2014 runs against the locally imported prod instance with EKOA_E2E_SALOMAO_ID set; skips cleanly (exit 0 + SKIP) when it/the api is absent, never a false green."
      },
      {
        "name": "erp-kyc",
        "targetGate": "operator-run salomao",
        "note": "was G6->G9->CUTOVER; same gap (KYC dossier surface not in the featured erp-imobiliario; fork out of the ported catalog, reconstituted at cutover). See RUN_LOG DEVIATION (2026-07-08, Phase 12). 2B-S5 proven-import: retargeted CUTOVER -> operator-run salomao (NOT cutover; docs/decisions.md) \u2014 runs against the locally imported prod instance with EKOA_E2E_SALOMAO_ID set; skips cleanly (exit 0 + SKIP) when it/the api is absent, never a false green."
      },
      {
        "name": "erp-ops-persistence",
        "targetGate": "operator-run salomao",
        "note": "was G6->G9->CUTOVER; same gap (contratos/clausulas ops surface not in the featured erp-imobiliario; fork out of the ported catalog, reconstituted at cutover). See RUN_LOG DEVIATION (2026-07-08, Phase 12). 2B-S5 proven-import: retargeted CUTOVER -> operator-run salomao (NOT cutover; docs/decisions.md) \u2014 runs against the locally imported prod instance with EKOA_E2E_SALOMAO_ID set; skips cleanly (exit 0 + SKIP) when it/the api is absent, never a false green."
      },
      {
        "name": "ifthenpay",
        "targetGate": "G8",
        "note": "was G4; G4 passed partial-domain (RUN_LOG G4) without the action-runner/platform-caller machinery this driver's execute step needs, and the driver still speaks the retired action API (FIXED-2) \u2014 retargeted to G8 (integration-execution) at the 2026-07-06 resume; REST adaptation owed there. See RUN_LOG DEVIATION."
      },
      {
        "name": "invoicexpress",
        "targetGate": "G8",
        "note": "was G4; same as ifthenpay \u2014 rides the G8 integration-execution machinery; REST adaptation owed there. See RUN_LOG DEVIATION (2026-07-06 resume)."
      },
      {
        "name": "pipedream",
        "targetGate": "G8",
        "note": "was G4; the Pipedream REST layer (ch03 \u00a73.8.16) did not land in the G4 partial-domain pass \u2014 module + adapted driver owed at G8. See RUN_LOG DEVIATION (2026-07-06 resume)."
      },
      {
        "name": "integration-automation",
        "targetGate": "G8"
      },
      {
        "name": "legal-research",
        "targetGate": "G6"
      },
      {
        "name": "onboarding",
        "targetGate": "G7B",
        "note": "SKIP-gated on /health claudeAuth"
      },
      {
        "name": "whatsapp-inbound",
        "targetGate": "G8",
        "note": "queue-acceptance at G5, full delivery at G8"
      },
      {
        "name": "action-registry",
        "targetGate": "operator-run C5",
        "note": "feature-run operator-run 20260712-150958: registry round-trip (issue -> visible execute -> destructive confirm -> cancel). Green from landing; ratchet applies."
      },
      {
        "name": "assistant-modes",
        "targetGate": "operator-run D3",
        "note": "feature-run operator-run: scripted 3-mode assistant gate (automate/teach/answer incl. the CITED turn). Burns LLM turns - run deliberately, not in CI."
      },
      {
        "name": "assistant-panel",
        "targetGate": "operator-run D2",
        "note": "feature-run operator-run: panel presence/mount/console gate on a served app."
      },
      {
        "name": "tour-playback",
        "targetGate": "operator-run E2",
        "note": "feature-run operator-run: zero-token tour playback + rebuild selector stability (burns 1-2 builds)."
      },
      {
        "name": "assistant-billing",
        "targetGate": "operator-run G1",
        "note": "feature-run operator-run: billing-truth probe - identity-exact assistant-chat attribution to the owner ledger; tours/actions dual-proven free (burns builds + 2 turns)."
      },
      {
        "name": "fees-knowledge",
        "targetGate": "operator-run F2",
        "note": "feature-run operator-run: knowledge-during-build live proof (narration + build-ingest + cited answer; per-run token EKF-<runstamp>; burns 1 build + 1 turn)."
      },
      {
        "name": "panel-perf",
        "targetGate": "operator-run G2",
        "note": "feature-run operator-run: panel lazy-load perf gate - launcher immediate + no panel-runtime fetch before interaction, one fetch on click, tour still plays after lazy mount, zero assistant POSTs (burns 1 build)."
      },
      {
        "name": "edit-journey",
        "targetGate": "operator-run H5",
        "note": "security block H5 (authored by H5, run by the operator): served-app admin EDIT-MODE journey - whoami true -> opt-in (client-only) -> H1-gated follow-up patch run -> preview/diff (versions) -> approve -> rollback restores (forward-restore); AND a role:'user' session cannot reach edit (whoami false + POST /jobs follow-up 403 canEditApps). Budget: up to 2 builds (setup + patch-run, or 1 with EDIT_APP_ID) + 1 rollback restore."
      },
      {
        "name": "request-changes-journey",
        "targetGate": "operator-run H5",
        "note": "security block H5 (authored by H5, run by the operator): request-changes queue journey - a user files from inside an org-shared app (route+screen context) -> lands in the OWNER org queue -> org-admin sees it with context -> convert starts an H1-gated patch run + links the jobId; folds in H4's live cross-org proof (a different-org user filing -> 404, no injection). Budget: 1 setup build + 1 follow-up build STARTED then cancelled (convert asserted at the API level, not awaited); 1 build with REQCHG_APP_ID."
      },
      {
        "name": "gateway-claude-code",
        "targetGate": "OPERATOR-RUN",
        "note": "S6 (run 20260717): stock Claude Code end-to-end over the LLM gateway. Needs a credentialed live stack + the claude CLI (verified by hand during the run), so it reports awaiting OPERATOR-RUN like the operator-run drivers - never books a skip as a false green."
      },
      {
        "name": "zoho-proxy",
        "targetGate": "operator-run",
        "note": "2B-S2 (Zoho served-app proxy): FULLY HERMETIC driver - self-launches an inline capturing mock Zoho (http.createServer, no shared mock helper in dev either), esbuild-bundles api/src/integrations/zoho-sign.ts under plain node (every external dep injected, express marked external), builds the backend with test-double deps, and asserts the real create->submit->status happy path + last-page (0-based) abs-only Signature field + embedded-URL minting + not_connected + error sanitization (no token/secret in a thrown message). Needs NO api/mongo/browser and passes standalone (node api/tests/e2e/zoho-proxy.e2e.mjs). Registered at operator-run (awaiting at G12) because the suite-ledger runner gates ALL due drivers on a reachable live-api base, which this self-contained driver does not need; the per-PR-lane hermetic proof of the same surface is the vitest contract test api/tests/contract/zoho-sign.test.ts. Ratchet applies from landing."
      },
      {
        "name": "zoho-sign-integration",
        "targetGate": "operator-run",
        "note": "2B-S2: REST-adapted from the retired /api/v1/action intent driver (sanctioned per the ifthenpay/invoicexpress 'REST adaptation owed' precedent). Against a running api it logs in, asserts the zoho-sign DEFINITION loads (api_key + test_connection/list_requests/get_request + client_id/client_secret/grant_code/refresh_token/dc) and the AI-builder load rebuilds an editable package exposing test_connection; self-launches an inline mock Zoho on MOCK_PORT. DOCUMENTED SKIP: the builder Tests-tab path does not run the provider credential resolver (S1-scope gap), so the grant-code exchange the dev driver asserted at that step is proven instead on the executor path by tests/integrations/zoho-sign-token.test.ts + zoho-proxy.e2e.mjs; the driver reports a bonus PASS if the builder path is ever wired. SKIPs cleanly (exit 0 + SKIP) when the api is unreachable - never a false green. Ratchet applies from landing."
      },
      {
        "name": "salomao-erp-zoho-swap",
        "targetGate": "operator-run salomao",
        "note": "2B-S4 (provider swap Adobe->Zoho): REST/env-adapted from cortex/tests/e2e/salomao-erp-zoho-swap.e2e.mjs (no prod id committed - the imported instance id comes from EKOA_E2E_APP_ID/EKOA_E2E_SALOMAO_ID). Two proof layers: (1) PLATFORM SWAP, provable against a bare running api - the /api/zoho-sign proxy + the pluggable /api/signature/send facade + the /api/adobe-sign router are all MOUNTED (each answers its own gate 400, not the terminal unmounted 404), i.e. Zoho is the live provider on the platform; (2) ERP-INSTANCE SWAP - the served SALOMAO bundle calls /api/zoho-sign and NEVER /api/adobe-sign, and the proxy drives a real create->submit->embedded-URL flow for the owner while Adobe stays facade-only (connected:false). Layer 2's halves SKIP cleanly (never a false green) until the SALOMAO instance import (2B-S5) + a connected zoho-sign config exist. Registered at operator-run salomao (AWAITING in the per-PR census lane; driven in the 2B-S6 verification run against the credentialed live stack with EKOA_E2E_APP_ID). Its per-PR-lane hermetic counterpart for the same facade-swap surface is the vitest contract test api/tests/contract/legal-plane.test.ts (signatureSend real schema + provider:'zoho-sign' routes to the live Zoho backend) + api/tests/legal/signature.test.ts. Ratchet applies from landing."
      },
      {
        "name": "salomao-erp-import",
        "targetGate": "operator-run salomao",
        "note": "2B-S5 (SALOMAO import machinery): REST/env-adapted from cortex/tests/e2e/salomao-erp-import.e2e.mjs (no prod id committed - the imported instance id comes from EKOA_E2E_SALOMAO_ID; the retired /api/v1/action login + hard-coded id-prefix match are dropped for the open served-app + app-data planes). Proves the import over a running api: (1) the imported SALOMAO ERP is SERVED at /apps/<slug>/ as a real built app (HTML + bundle, not the Building placeholder); (2) BYTE-SAFETY - the featured erp-imobiliario is intact alongside it (the import never disturbs the featured app the 37 byte-compat specs use); (3) DATA LANDED - app-data seeded from the prod dump is readable under the imported id via GET /api/app-data/<coll> (X-Ekoa-App-Id=EKOA_E2E_SALOMAO_ID), which is the 2B-S5 importArtifact.data outcome and simultaneously proves the id is the imported instance (needs EKOA_E2E_SALOMAO_COLL; a clean SKIP otherwise, never a guess). Registered operator-run salomao (AWAITING in the per-PR census lane; driven in the 2B-S6 verification run with EKOA_E2E_SALOMAO_ID set). Its per-PR-lane hermetic counterparts for the same machinery are the vitest contract test api/tests/contract/artifact-family.test.ts (importArtifact applies bundle.data when present; unchanged when absent) + the converter unit test api/tests/migration/convert-dev-bundle.test.ts (prod envelope -> shared ArtifactBundle incl data; non-UTF-8 refusal). SKIPs cleanly (exit 0 + SKIP) when the api is unreachable, EKOA_E2E_SALOMAO_ID is unset, or the instance is not served - never a false green. Ratchet applies from landing."
      },
      {
        "name": "zoho-signature-durability",
        "targetGate": "operator-run",
        "note": "2B-S6 (signature-chain + webhook-durability probe): FULLY HERMETIC driver - self-launches an inline capturing mock Zoho (http.createServer), esbuild-bundles api/src/integrations/zoho-sign.ts under plain node (every external dep injected, express external), and proves the whole e-signature chain + inbound-webhook trust model against that ONE mock (the single source of signature truth, as prod Zoho is): (A) SEND CHAIN - HTML->PDF render, mandatory Signature field on the LAST page (0-based page_no === total_pages-1, abs box only), embedded URL minted with PT (pt) locale, redirect_pages pointed at the fragment-free /api/zoho-sign/return bounce; (B) /return HOST GUARD - the exact minted bounce `to` 302s back to the ekoa.io portal URL while a non-ekoa host / non-https ekoa host / malformed target are refused 400 (never an open redirector), asserted by mounting the real zohoSignRouter; (C) WEBHOOK IDEMPOTENCE - a genuine completion (mock marks the client SIGNED) advances the proposta to Assinada EXACTLY once, the replay is a no-op; (D) FORGERY-PROOF - a forged 'completed' whose owner-scoped re-fetch shows unsigned does NOT advance, an unknown requestId is never re-fetched; (E) CONCURRENT DOUBLE-DELIVERY (the 2B-S3 TOCTOU carry-forward) - two genuinely-concurrent completions forced into the worst-case interleave leave the proposta in the correct terminal state (Assinada + conversionPending + eSignature SIGNED) with NO corruption (the advance is a convergent full-overwrite; the interleave may write twice but never corrupts; count asserted bounded 1..2). Needs NO api/mongo/browser; passes standalone (node api/tests/e2e/zoho-signature-durability.e2e.mjs). Registered at operator-run (AWAITING at G12) on the zoho-proxy precedent - the census runner gates ALL due drivers on a reachable live api, which this self-contained driver does not need. Its per-PR-lane deterministic counterparts are api/tests/integrations/sign-webhooks.test.ts (now incl. the concurrent double-delivery case) + api/tests/contract/zoho-sign.test.ts; the send-chain proxy sibling is api/tests/e2e/zoho-proxy.e2e.mjs. Ratchet applies from landing."
      }
    ]
  },
  "contract_tests_from_ruleset": {
    "note": "23 direct-handler rule-set files rewritten as REST contract tests at their domain gates; mapping recorded in api/tests/contract/README.md",
    "targetGatesByDomain": {
      "G2": [
        "auth-device",
        "settings-defaults",
        "phase2/user-isolation-settings"
      ],
      "G3": [
        "memory-handler",
        "memory-consolidation",
        "phase2/user-isolation-integrations",
        "sessions-onboarding-singleton",
        "phase2/admin-usage-page"
      ],
      "G4": [
        "integrations-session-intents"
      ],
      "G5": [
        "event-sourcing/trigger-target",
        "event-sourcing/ifthenpay-callback"
      ],
      "G6": [
        "app-data-backups-handler",
        "update-from-bundle-intent",
        "featured/featured-update",
        "featured/list-instances",
        "featured/seeder",
        "featured/set-featured-authz",
        "artifact-backend/handler",
        "artifact-backend/delete-teardown"
      ],
      "G7": [
        "phase2/specialist-removal"
      ],
      "G8": [
        "automation/handler"
      ],
      "G4_knowledge": [
        "knowledge/knowledge-handler",
        "knowledge/knowledge-sources"
      ]
    }
  },
  "module_tests_146": {
    "note": "carryover module tests travel with their module port (module-travels-with-tests rule). Group -> target gate.",
    "groups": [
      {
        "group": "legal engines/golden/spine",
        "count": 20,
        "targetGate": "G6"
      },
      {
        "group": "knowledge subsystem",
        "count": 12,
        "targetGate": "G4"
      },
      {
        "group": "automation subsystem",
        "count": 12,
        "targetGate": "G8"
      },
      {
        "group": "memory",
        "count": 9,
        "targetGate": "G3"
      },
      {
        "group": "services long tail",
        "count": 33,
        "targetGate": "G6",
        "note": "spread G4-G6 by service; SSRF/sanitizer/browser-pool land with first consumer"
      },
      {
        "group": "persistence parity gates",
        "count": 6,
        "targetGate": "G2"
      },
      {
        "group": "event queue + delivery",
        "count": 8,
        "targetGate": "G5"
      },
      {
        "group": "listener rail (2A-S1)",
        "count": 4,
        "targetGate": "G5",
        "note": "2A-S1 (ekoa-dev port): the durable poll->enqueue listener runtime upstream of the existing event queue. tests/integrations/platform-poll.test.ts (M365 item-high-water $skip paging, Gmail poll-time pageToken, MAX_ITEMS_PER_TICK 500 stalled continuations, first-poll=now/no-backfill, cursor-advance-only-after-enqueue, UNIQUE dedup, partial-failure stall - the dev sync-sqlite deps async-adapted over the REAL event_queue + listener_state stores). tests/events/listener-supervisor.test.ts (deps-injected supervisor: empty-set start/stop no-op, platform poll enqueues across ticks, cancel-safe stop=no-work-after-stop, failing-listener isolation via bumpFailure without starving a healthy sibling). tests/integrations/odata-interpolation.test.ts ({{$top}} $-aware interpolation regression - the fix already lives in http-template.ts). tests/contract/triggers-schema.test.ts (shared Trigger gains kind:webhook|listener + pollConfig; triggerView emits a migration-free kind-absent=>webhook shape). Hermetic + LLM-free; runs green in the per-PR vitest lane from landing (module_tests are enforced by npm test, not this census runner). Ratchet applies. The dispatch-hydration + user-defined (IMAP) poll transport land in later 2A slices."
      },
      {
        "group": "discovery spine (P2)",
        "count": 12,
        "targetGate": "G8",
        "note": "2026-08-19: registered the P2 discovery -> capture -> replay -> self-heal suites. They landed across three earlier rounds of feat/p2-discovery-spine WITHOUT a ledger row, which is the module-travels-with-tests rule broken in the direction that hides it (npm test ran them; the ledger did not know they existed). Registered here in the round that closes the compilation defects. tests/automation/network-capture.test.ts (the hosted redaction boundary and the model-free learner: what a hole may be, the component-wise URL templating that keeps an argument out of an origin and out of a query parameter name, and the refusal to compile a recipe whose argument the pass could not locate - which would be a constant answering the first run's question forever). tests/automation/injected-call-replay.test.ts (the replay ladder, per-call posture, the write gate incl. scripted DOM verbs, drift, the argument-escape attempts, and the node-http rung driven with NO injected transport so guardedFetch actually runs). tests/automation/replay-mount.test.ts (the mount's wiring: replay-first, fall-through, the coverage refusal for a mutating action, the evidence lifecycle at both ends, and the three SecretRegistry hops asserted by CONSEQUENCE against real stores). tests/automation/self-heal.test.ts (drift classification and the tenant-scoped supersede). tests/automation/browser-session-capture.test.ts (the capture drain). tests/automation/discovery-replay-acceptance.test.ts (THE acceptance, entered at executeUserIntegrationAction: learned once, replayed with zero model calls at the chokepoint's three methods and no new run record, plus the mutating-action refusal against two actions on the same automation and the same captured traffic). tests/security/discovery-replay-isolation.test.ts (one org's learning never runs for another; posture does not travel between origins). tests/contract/local-browser-capture.contract.test.ts (the daemon wire additions, incl. that the schema has no field a header VALUE could occupy). Hermetic + LLM-free (the acceptance counts model calls and requires zero on replay); enforced by npm test, not this census runner. The daemon half cannot be exercised from api/** (lint-enforced zone) and lives in clients/bridge/test/browser/ - inject-inheritance (real Chromium, real server), replay-wiring, recorder-lifetime, inject and capture. Ratchet applies from landing. ROUND SIX adds tests/automation/composition-root-action-seam.test.ts - the ONE production line that points at this slice was a surviving mutant. server.ts binds automationBackedActionHandler(); replacing that binding with the pre-P2 inline mapping (which type-checks, and drops integrationKey, actionName, writeAssent and mutates) left the whole lane GREEN with `named` false on every call - i.e. the slice dead in production and nothing red. The acceptance constructs the handler itself, so it pins the MAPPING and not the BINDING; the only other guard greps server.ts for the identifier, which a rebinding of that identifier satisfies. This boots the REAL buildApp after resetting the seams and enters at the bound executeIntegrationAction, asserting the CONSEQUENCES of the fields crossing: a stored recipe is replayed (impossible without the action identity - and the pre-P2 mapping fails there with unknown_automation instead), and an approved write's gate opens (impossible without writeAssent), with its own unapproved control. Verified by applying that exact rebinding before landing: 2 of its 3 cases go red while the acceptance and the poll suites stay green. The same round grew replay-mount (the answer correlation, the two hosted capture bounds, the fail-closed `mutates` reading at the seam), browser-session-capture (the session accumulator's bound), the acceptance (same envelope, same answer, the badge call, the narrow-recipe clear) and tests/integrations/user-defined-poll.test.ts (a REPLAYED listener tick, driven through the real handler rather than a hardcoded envelope literal). ROUND SEVEN adds TWO files and one module. tests/contract/integrations-recipes.test.ts - the OWNER'S CONTROL over what the machine learned, through the real app: GET /api/v1/integrations/recipes (a SUMMARY projection, asserted by the ABSENCE of the stored document's internals) and DELETE /api/v1/integrations/:key/actions/:actionName/recipe, both auth:user, both bodies against the new shared schemas, tenancy proved against a same-org peer of a PRIVATE definition / an org-admin / another org each with the owner as its control, the delete idempotent, the malformed-segment 400 and the unauthenticated 401 on both, and the raw evidence asserted GONE against integration_captured_calls itself. It exists because three of a recipe's failure modes clear themselves (the replay visibly refuses) while a recipe that keeps answering ok and answers WRONGLY had no exit at all: putRecipe refuses to overwrite, a supersede needs a drift that cannot fire while the calls keep returning 200 with an unchanged shape, nothing expires it. Admission is covered by integrations-capability.test.ts's stack walk over this router. tests/integrations/recipe-lifecycle.test.ts - the decisions of the NEW tier-3 module src/integrations/recipe-lifecycle.ts (forgetRecipe): which clear leads to which discard, the tenant key the discard is asked for, the asymmetric failure posture (a discard that throws is swallowed and logged; a clear that throws is not), and that the actor is passed only when the caller supplied one. Its two REAL callers are proved where they live, never here. The same round grew network-capture and injected-call-replay (the answer-bearing call must carry every hole - the recipe-wide rule is satisfied by ANY call while the answer is ONE, so a page serving the same document from a filtered search and from a constant summary compiled a recipe whose answer could never change; refused at the compile AND at the replay, where an older build's recipe arrives already stored), replay-mount (that refusal at the mount plus the three clear/discard cases), discovery-replay-acceptance (a stored constant-answer recipe refused, cleared and its evidence collected; and the evidence collected on the arguments-uncovered clear against the REAL default clearRecipe) and tests/integrations/recipe-store.test.ts (clearRecipe answers with the recipe it DROPPED - the boolean it used to answer with is what orphaned a pass's evidence - plus the new visibleTo actor gate, re-asserted inside the CAS). ROUND EIGHT adds no test FILE and closes the verification round's three defects inside the files above, so `count` is unchanged at 11. tests/automation/discovery-replay-acceptance.test.ts gains the BLOCKER's end-to-end reproduction - a fixture page that also fetches an ordinary `GET /api/view?state=<38-char page-state token>`, which no redaction leg touches and the compile leaves literal, so the REAL recipe store REFUSES the recipe by THROWING (`looksLikeLiteralSecret`). The evidence collector handled every exit that RETURNED a verdict and none that threw, so the pass's evidence stayed and, because the refusal is a property of the pass, a fresh pile landed on EVERY run (2, 4, 6 documents in `integration_captured_calls`) with nothing able to reach any of them. The case loops three runs asserting the WHOLE collection empty each time (a keyed assertion would be green while the pile grew, since each run mints a new captureId), spies the real `appendCapturedCall` so 'empty' is a collection and not an empty fixture, and carries its own control (the same action on the same fixture WITHOUT the page-state call learns, and its evidence survives). The same file gains the FOURTH REMOVAL PATH through `saveAuthoredDefinition`: an ordinary builder save that RENAMES an action drops its recipe (`carryRecipesForward` re-attaches by action NAME) and now takes its capture with it, with a second action on the same row as the not-swept control. tests/automation/replay-mount.test.ts pins the same throw at the seam on BOTH write routes (put and supersede), the supersede case also asserting the LIVE recipe's evidence survives. tests/integrations/recipe-store.test.ts grows the fourth path at the store, over the real collections: removal and rename each collect, and a save that KEEPS an action keeps its evidence. tests/integrations/recipe-lifecycle.test.ts adds the shared pairing `discardEvidenceOfRemovedRecipes` (per-recipe action key; a recipe naming no evidence is skipped rather than asked for; one discard that throws does not abandon the batch) and its header now names where each of the four paths is proved. tests/security/recipe-no-values.test.ts adds the supersede's `reason` to the persistence-boundary proof - it was destructured OUT before `assertCarriesNoValues`, exempting it from the registry leg, the literal-secret leg and `LIMITS.stringChars` at once - with the same token in `goal` as its control and an ordinary reason still superseding. TWO ASSERTION DEFECTS closed in the same round: tests/automation/injected-call-replay.test.ts could not fail on the property the slice is NAMED for - its `recipe()` helper DEFAULTED `answersWith` to `{callIndex: calls.length-1}`, which is the pre-fix reading, the one case overriding it asserted only outcome and URLs, and `session()` answered one canned body for every call so no `data` assertion could have discriminated. The helper now REFUSES to default a multi-call fixture, the stub answers per URL, and `result.data` is asserted in both directions (a recipe naming call 0 of 2 kills `calls[calls.length-1]`; one naming call 1 of 2 kills `calls[0]`). tests/automation/network-capture.test.ts's registry-leg case put its value under `?auth=` - a name `SECRET_KEY_PATTERN` matches - so the name-pattern leg alone satisfied it and the URL's registry leg could be deleted with the lane green; the parameter is now `sid` and the body fields `echo`/`seen`, and each leg is asserted on its own field. ROUND EIGHT CLOSE-OUT adds ONE test FILE, so `count` moves to 12, and closes the round's two remaining unproven gates. tests/security/captured-evidence-last-gate.test.ts - the capture store's LAST gate, `assertNoLiveSecret`, survived a full no-op: replacing its body with an unconditional `return` left all 202 tests across the twelve suites that can reach `appendCapturedCall` at all green - measured against those files, not inferred. Not an equivalent mutant - `redactCapturedCall` redacts the URL and the two bodies and copies `method`, `contentType` and BOTH header-name arrays through verbatim, while the recipe store's `assertCarriesNoValues` only ever sees the REQUEST header names of the calls the COMPILE KEPT. So a `responseHeaderNames` entry, a content-type parameter, and anything on an exchange the compile dropped (a login POST caught by `bodyIsSecretShaped`, a URL with userinfo that `templateUrl` refuses, or the 25th distinct call while `persistEvidence` keeps 48) reach the collection with the store's whole-document refusal as the only thing in the way. The suite drives `runAutomationForAction` against the REAL stores with a deliberately LOW-ENTROPY credential (21 characters over two character classes: under `looksLikeLiteralSecret`'s floor and a valid header name, so no shape rule can see it), and asserts by COUNTING DOCUMENTS - because `persistEvidence` is tolerant per append, the recipe still stores `ok`, `stored` is true, the `finally` discards nothing, no removal path names the pile, there is no TTL and `listCaptureIds` has no production caller, so a document that lands carrying a live value is durable forever. `method` is asserted at the store rather than through the learn pass and the file says why (`redactCaptures` upper-cases it and `internalApiCalls` keeps only the seven verbs, so today's compile cannot deliver a poisoned one - but `appendCapturedCall` is exported and the gate is the STORE's). Verified by the mutation: 8 of its 10 cases go red under the no-op, the two survivors being its two controls. The same round-close grows tests/security/recipe-no-values.test.ts with an independent killer for `assertCarriesNoValues`'s header-name GRAMMAR leg: its one case planted a REGISTERED value, which the registry leg catches first, so the whole `forEach` deleted with 198 files / 2837 tests across security, integrations, automation and contract still green. The new case uses `:authority` (an HTTP/2 pseudo-header the capture path drops deliberately) and a 72-character single-cased token name - neither registered, neither secret-shaped - killing the character class and the `{1,64}` bound separately, with no registry present at all."
      },
      {
        "group": "dispatch hydration (2A-S2)",
        "count": 1,
        "targetGate": "G5",
        "note": "2A-S2 (ekoa-dev port): email hydration on the artifact-backend delivery rail. NEW integrations/event-sources/email-hydrate.ts exports the FROZEN EmailInput envelope + the Graph/Gmail read_email normalizers (read-failure => THROW => the durable queue retries; NEVER a silent preview/snippet body). NEW integrations/event-sources/dispatch-input.ts is the deps-injected artifact-backend input builder extracted from dev's monolithic dispatchEvent (email source => hydrate to EmailInput; every other source keeps ekoa-code's generic { event, trigger } envelope unchanged); wired into the invokeArtifactBackend delivery target ONLY at the composition root (server.ts), calling read_email through callPlatformIntegration bound to the trigger's org (OAuth refresh in core). tests/events/dispatch-target.test.ts (12 cases, ported+adapted from dev's event-sourcing/dispatch-target.test.ts): builder email-hydrate vs generic-envelope branch + failure-propagates-not-preview; hydrateEmailInput Graph normalize + read-failure-throws-not-truncated + no-id-degrades-to-preview; Gmail normalize (payload headers, base64url text/plain preference, epoch-ms receivedAt, permalink webLink) + snippet fallback; PLUS a regression block for the fresh-context-review wiring bug: the durable queue stores the enqueued item as JSON TEXT (enqueueListenerEvent/webhook ingress store rawBody.toString('utf8')), so hydrateEmailInput now parses a STRING payload before hydrating - the regression drives the REAL wired path (buildArtifactBackendInput + real hydrateEmailInput + stubbed platform call) with a byte-identical JSON.stringify(item) payload and asserts read_email IS called and the EmailInput is POPULATED (not the silent empty degrade the bug delivered), the string-path read-failure still THROWS, and a corrupt non-JSON payload THROWS rather than masquerading as an empty success. The automation-vs-artifact target routing + retry/dead semantics stay in events/delivery.ts (covered by events/delivery.test.ts, unchanged). Hermetic + LLM-free; enforced by npm test, not this census runner. Ratchet applies. WhatsApp fan-out lands in slice 2A-3."
      },
      {
        "group": "whatsapp inbound rail (2A-S3)",
        "count": 1,
        "targetGate": "G5",
        "note": "2A-S3 (ekoa-dev port): the WhatsApp inbound rail. NEW integrations/event-sources/whatsapp-hydrate.ts carries dev's PURE extractWhatsAppMessages near-verbatim (text/caption/PT media markers/button+interactive replies/batch fan-out/never-throws) plus two ekoa-code-seam additions: hydrateWhatsAppMessages (parses the queue's JSON-TEXT payload before flattening - a raw STRING would flatten to [] and silently swallow every real message, the 2A-S2 wiring-bug class; non-JSON THROWS so the delivery retries) and whatsAppDedupKey (the wamid-SET-derived ingress dedup key). events/service.ts handleIngress now honours webhookConfig.secretSource.credentialField on the POST path (ONE resolveWebhookSecret shared with the GET-callback path): Meta signs every WhatsApp webhook with the app secret, so the OWNER's decrypted app_secret is the only secret that can verify - an unresolvable secret FAILS CLOSED (500, nothing enqueued, no credential named, no secret echoed), never falling back to the trigger secret or an empty key. dispatch-input.ts gains buildArtifactBackendInputs (plural): whatsapp fans out to one MessageInput per inbound message, every other source delegates unchanged to the singular builder; ZERO inputs (a statuses-only delivery receipt notification) is a dispatched NO-OP, not an error and not a dropped event. server.ts loops the inputs at the composition root. tests/events/whatsapp-webhook.test.ts (29 cases, ported+adapted from dev's event-sourcing/whatsapp-webhook.test.ts) drives the REAL /hooks/:triggerId router over the wire (raw bytes + the real X-Hub-Signature-256 header + real Mongo stores): SIGNED-with-app_secret ACCEPTED; FORGED/wrong-secret, TAMPERED-body-with-valid-signature, absent-header and TRIGGER-SECRET-signed all REJECTED 401 with nothing enqueued (the trigger-secret case is the mutation-proof that the credential field is the source); missing credential 500 fail-closed; disabled 410-after-valid-signature ordering preserved; non-whatsapp triggers still verify against their own secret. DEDUP: byte-identical replay AND a re-serialised retry (different bytes, same wamid) both dedupe, while a later batch [m1,m2] after [m1] is still dispatched so m2 is never lost; statuses-only falls back to the body hash and is enqueued exactly once. DISPATCH: the stored payload is asserted to be the exact JSON TEXT bytes, the real delivery pipeline fans a batch out to one invocation per message, a statuses-only row ends `delivered` with ZERO backend invocations, a mid-batch backend failure re-arms the retry schedule, and a corrupt payload THROWS. Hermetic + LLM-free; enforced by npm test, not this census runner. Ratchet applies. NOT covered: live Meta verification (needs a Meta Business app + a registered webhook - external credentials, tracked in the plan's external-creds table)."
      },
      {
        "group": "user-defined listener path (2A-S4)",
        "count": 1,
        "targetGate": "G5",
        "note": "2A-S4 (ekoa-dev port + hardening): the NON-platform half of the listener rail. NEW api/src/integrations/event-sources/user-defined-poll.ts polls through the REAL user-defined integration executor (executeUserIntegrationAction), driven by the integration PACKAGE's listenerConfig. tests/integrations/user-defined-poll.test.ts is deliberately NOT a mock of the wired path: the injected `call` is the exact lambda server.ts binds (executor closed over the trigger's org+owner, automation seam included) with ONLY the HTTP transport faked, the definitions registry is the REAL on-disk one (a fixture copy of api/assets/integrations + one synthetic poll-demo package, so `imap` is the SHIPPED package), and cursor/queue are the REAL listener_state + event_queue stores over mongodb-memory-server - so the payload assertions see the queue's JSON *TEXT*, which a pre-parsed fixture would have hidden. 24 cases: the establishing poll adopts the provider cursor and enqueues NOTHING (no backfill) / throws when the response has ITEMS but no cursorField (nothing delivered, nothing discarded); THE NO-BACKFILL BOUNDARY IS INITIALIZATION, not the first successful poll - the review's exact loss sequence (empty first response -> a real message arrives -> it must be DELIVERED, not swallowed) is a committed regression, plus the ARMED phase staying armed and an unrecognised persisted cursor being refused rather than re-running the discard; steady state sends the stored cursor as `since` and enqueues one durable row per item, claimable by the ordinary dispatcher; the trigger's pollConfig.actionName override; a failed action becomes a throw carrying the executor code (backoff + bumpFailure audit, never silent); CURSOR-AFTER-ENQUEUE proven by OBSERVED ordering plus a throwing enqueue and a missing-dedup-key stall that both leave the cursor exactly where it was; dedupKey DETERMINISM (the same item re-serialised with reordered keys + an extra field still collides on UNIQUE(trigger,dedupKey); a numeric uid stringifies stably); CANCEL-SAFETY in flight and mid-batch; AUTOMATION-BACKED poll actions (the citius shape) - a poll with NO automation seam fails loudly with automation_required (the review-found composition-root omission), a poll WITH the seam runs and resolves the listenerConfig paths against the run OUTPUT rather than the {runId,status,summary,output} envelope (a plain body that merely has an `output` key is NOT unwrapped), and a STATIC GUARD asserts every executeUserIntegrationAction call site in server.ts passes runAutomationBackedAction - the machine-caught form of that bug class; and the HONEST DEGRADE for the DEFERRED IMAP transport - the shipped imap package declares transport:'imap', the executor refuses it with code unsupported_transport BEFORE any decrypt and without one HTTP call, the tick throws 'not available in this version ... runs HTTP-backed and automation-backed actions only', and a guard case fails if someone deletes the imap listener contract instead of shipping the transport. tests/events/listener-supervisor.test.ts gains the ROUTING case (a non-platform trigger goes down the user-defined branch via the required callUserIntegration dep, driven by the shipped imap listenerConfig) - no new FILE, an extension of the 2A-S1 file. Hermetic + LLM-free; green in the per-PR vitest lane from landing (module_tests are enforced by npm test, not this census runner). Ratchet applies. citius still cannot poll for two reasons OUTSIDE this rail - logged as `citius-listener-blocked` in docs/findings.md, closing with 2A-S6."
      },
      {
        "group": "docx engine core (2C-S1)",
        "count": 2,
        "targetGate": "G6",
        "note": "2C-S1 (ekoa-dev port): the pure-buffer native Word track-changes engine ported near-verbatim to api/src/services/docx-redline.ts + docx-comments.ts, backed by @adeu/core pinned EXACTLY 1.28.0 (NO caret - the wrapper works around @adeu/core's DOCUMENTED bugs, so a version bump invalidates the whole wrapper; jszip ^3.10.1 is a runtime dep, fflate a devDep; workspace hoist verified @adeu/core@1.28.0 resolves for api). tests/services/docx/docx-redline.test.ts (native w:ins/w:del track changes attributed author+pinned w:date, word/comments.xml wiring incl. [Content_Types].xml + document.xml.rels registration and matching commentRangeStart/End/Reference anchoring, formatting preservation - untouched paragraphs + styles.xml/numbering.xml byte-identical, acceptAllRevisions clean output, ATOMIC batch rejection carrying adeu's occurrence-list + match_mode guidance VERBATIM, CriticMarkup projectDocx, validateDocx container checks) and tests/services/docx/docx-comments.test.ts (w15:done resolution in word/commentsExtended.xml - thread-wide flip whichever [Com:n] is clicked, reply-then-resolve in ONE atomic batch, idempotent no-op accounting, the package-level traps that corrupt a .docx silently: duplicate commentsExtended relationships, a missing/second commentsExtended part, a spurious commentEx for a multi-paragraph comment, and comment-anchor survival across accept - @adeu/core drops the anchor markers on accept so the wrapper re-anchors onto the surviving text and never touches a pre-existing orphan). contrato-fixture.ts (a PT-PT contract built with the docx lib) is shared scaffolding, not a census file. The legal-grade assertions encode empirically-validated OOXML behaviour and were ported UNCHANGED - only import paths + type-level noUncheckedIndexedAccess guards on TEST PLUMBING (regex-capture access, fflate record indexing, a catch-union cast) were adapted, never an assertion; the SOURCE guards likewise preserve behaviour. Hermetic + LLM-free; green in the per-PR vitest lane from landing (module_tests are enforced by npm test, not this census runner). Ratchet applies. The document-source lifecycle, docx-fetch, app-docx routes + shared contract, agent tools through seams, and the scaffold source-mode + web e2e land in later 2C slices (2..7)."
      },
      {
        "group": "document-source lifecycle (2C-S2)",
        "count": 1,
        "targetGate": "G6",
        "note": "2C-S2 (ekoa-dev port): the per-appId .docx source lifecycle placed BESIDE api/src/apps/app-files.ts as api/src/apps/document-source.ts. Manages two well-known blobs + a meta sidecar under <EKOA_DATA_DIR>/app-data/{appId}/docx (setSource / getStatus / getCurrent / getProjection / getClean / applyEdits / applyReview over the 2C-S1 services/docx-redline engine). PORTED FROM dev cortex/src/services/document-source.ts; the ONE deviation is the appId path-safety guard: dev's persistence/app-data isValidCollection+isReservedScope pair is REPLACED with ekoa-code's app-files.ts ingress rule VERBATIM - collectionName.safeParse(appId).success AND !appId.startsWith('usr.') - applied INSIDE docxDir(), which is the SOLE place an app path is built so the guard cannot be bypassed (every fs path routes through it). Kept VERBATIM (load-bearing correctness/safety): the per-appId promise-chain WRITE LOCK serializing writes, atomic temp-file+rename, the 25 MB setSource choke (single ingest chokepoint), the PT-PT error messages, and applyReview (owner-resolved SERVER-SIDE via the artifacts+users stores, fallback 'Ekoa'). tests/apps/document-source.test.ts: docxDir path-safety (14 rejection families - ../, absolute, windows \\, usr.<x>, __<x>, out-of-charset, empty, >100 chars - PLUS a 200+ case deterministic generative sweep since fast-check is not a repo dep, PLUS accept+containment for valid ids, PLUS setSource enforcing the guard on the real write ingress), the 25 MB PT-PT cap (no link on reject), per-app write serialization (two parallel batches both survive; setSource+applyEdits serialize), and applyReview owner attribution vs 'Ekoa' fallback (store singletons spied - hermetic, no Mongo). contrato-fixture.ts reused from tests/services/docx/ (shared scaffolding, not a census file). Hermetic + LLM-free; enforced by npm test, not this census runner. Ratchet applies. The docx-fetch ingest branches, app-docx routes + shared contract, agent tools through seams, and the scaffold source-mode + web e2e land in later 2C slices (3..7)."
      },
      {
        "group": "docx-fetch ingest (2C-S3)",
        "count": 1,
        "targetGate": "G6",
        "note": "2C-S3 (ekoa-dev port, SSRF-sensitive): the binary .docx fetcher behind the redline pipeline ported to api/src/integrations/docx-fetch.ts (dev cortex/src/services/docx-fetch.ts). Resolves a lawyer link into real .docx bytes over four source shapes - direct https URL (FULLY LIVE), OneDrive/SharePoint share (Microsoft Graph shares API), Google Drive/Docs, and workspace cloud by provider+fileId/query (fetchDocxFromCloud). Seam adaptations from dev: TOKEN ACCESS is the INJECTED { getStatus, getAccessToken } seam (the same shape app-cloud-files.ts uses) built via createDocxFetcher(deps) - NOT a token-store import; the CLOUD branches DEGRADE HONESTLY (getStatus not-connected => the PT 'Ligue a Microsoft 365/Google Workspace' error; getAccessToken throws a not-connected Error) - never a silent failure or fake success. Kept VERBATIM (security/correctness crux): the 25 MB TRIPLE enforcement (declared Content-Length reject + streaming byte-count cap + final buffer-size guard), the streaming CANCEL the instant the cap is passed (readBodyCapped), and PER-HOP SSRF re-validation - dev's hand-rolled isSafeUrl-per-redirect loop is MAPPED onto ekoa-code's shared guardedFetchFollow (services/url-fetcher.ts), which re-runs assertSafeUrl + a resolved-IP re-check on EVERY hop (strictly stronger than dev; a redirect from an allowed host into loopback/private/metadata is refused). The Graph/Drive branches hit fixed trusted hosts (graph.microsoft.com / app-cloud-files download), so a raw fetch there is correct - the SSRF guard is for the request-derived direct URL. tests/integrations/docx-fetch.test.ts (ported near-verbatim; assertions updated ONLY to thread the injected access token through the cloud calls, never weakened): isProbablyDocxUrl classification, SSRF pre-check + redirect-hop-to-private rejection + >5-hop chain rejection (real guardedFetchFollow, deterministic offline), direct-URL filename/content-type/magic checks, the 25 MB triple incl. a lying/absent Content-Length that streams over the cap + the cancel-not-drain assertion, Graph share resolution + M365-not-connected honest degrade, Google Drive + Google-not-connected honest degrade, and fetchDocxFromCloud id/query resolution. Hermetic + LLM-free; enforced by npm test, not this census runner. Ratchet applies. The app-docx routes + shared contract + diagrams, agent tools through seams, and the scaffold source-mode + web e2e land in later 2C slices (4..7)."
      },
      {
        "group": "app-docx routes + shared contract (2C-S4)",
        "count": 3,
        "targetGate": "G6",
        "note": "2C-S4 (ekoa-dev port): the served document-base app's REST window onto its linked Word doc. NEW api/src/apps/app-docx.ts router ported from dev cortex/src/routes/app-docx.ts (GET /api/app-docx/status|projection|current, POST /api/app-docx/clean|edits + the OPTIONS preflight), mounted at '/' in server.ts AFTER the global JSON body parser (its /edits + /clean bodies are JSON). Two seam deviations from dev, both improvements KEPT: (1) admission is app-files.ts admitApp VERBATIM INCLUDING the owner-activation gate (Amendment 2, fail-closed CONV-2) - a deactivated (403 ACCOUNT_DISABLED) / billing-locked (402 BILLING_LOCKED) artifact owner cannot read or mutate the doc on ANY route, and a slug header resolves to the canonical id before the gate; an unregistered id resolves to null and keys on itself (carried old-plane behavior); (2) the document-source service functions (getStatus/getProjection/getCurrent/getClean/applyReview) are the INJECTED seam wired ONLY at the composition root (server.ts binds the real apps/document-source.ts fns) - the router hard-wires no service singleton, and error taxonomy is mapped by the error's own name/shape (adobe-sign's property-based precedent, so no runtime import of document-source/docx-redline): NoDocumentSourceError -> 404, RedlineBatchError -> 422 {error,failures}. docx-fetch (2C-S3) is NOT consumed by these read/derive+review routes (dev's 158-line app-docx.ts links sources via the ekoa-docx MCP tools, not a route) - noted as a deviation, no route fabricated to use it. shared/src/served-app.ts gains real descriptors + zod: AppDocxStatusResponse/AppDocxProjectionResponse/AppDocxEditsResponse (+AppDocxReviewReport, AppDocxEditsRequest) for the JSON routes; appDocxCurrent/appDocxClean are kind:'binary' (z.unknown(), like app-files). All five servedApp.appDocx* keys added to schema-coverage COVERED (EXPECTED_PENDING_COUNT unchanged at 49); mount-coverage auto-excludes them (outside /api/v1). tests/apps/app-docx-routes.test.ts (faithful behavioral port over a real mounted router + real document-source + mongo-memory-server for resolveApp, seeded via setSource/applyEdits: status/projection/current/clean/edits happy+404+422 paths VERBATIM from dev, PLUS the owner-activation gate 403/402/slug-resolves block) and tests/contract/app-docx.test.ts (every 2xx body validated against its named shared schema + the descriptor's response schema; current/clean binary magic; non-2xx: header/404 flat {error}, 422 flat {error,failures}, 403/402 vs the shared ErrorEnvelope). THIRD file tests/security/app-docx-authz.test.ts is a TRIPWIRE added after the fresh-context review corrected an overstated security claim in the router header: this plane authenticates NO CALLER (same trust model as /api/app-files - whoever holds the app id can read and mutate that app's document); admitApp gates only the resolved OWNER's activation. It PINS today's unhardened behavior - anonymous header-only /projection read of the full text, anonymous /current+/clean byte download, anonymous PERSISTED /edits whose comment lands in word/comments.xml attributed to the ARTIFACT OWNER's username (an outsider's edit recorded in a legal .docx as authored by the lawyer), and the remanence case where deleteArtifact leaves the blobs so resolveApp is null => artifactBacked false => the owner-activation gate is SKIPPED on the orphaned document - so a future hardening FLIPS the suite visibly instead of closing the gap silently; it also asserts positively the half that IS enforced (403/402/no-record fail-closed) and carries a MOUNT PROBE over the real buildApp (all five routes answer their own 400 admission gate) because every other app-docx test mounts the router itself, so deleting the server.ts mount would otherwise leave the suite green. The gap is recorded as a surface extension of the existing HIGH finding served-app-data-unauthenticated-writes in docs/findings.md + docs/security.md (same root cause, same operator decision, no separate fix timeline). Hermetic + LLM-free; enforced by npm test, not this census runner. Ratchet applies. Diagrams updated (FIXED-12): docs/diagrams/module-map + request-flow AS-BUILT annotations for the app-docx router + document-source/docx-redline seam. Agent tools through seams + the scaffold source-mode + web e2e land in later 2C slices (5..7)."
      },
      {
        "group": "docx agent tools through seams (2C-S5)",
        "count": 1,
        "targetGate": "G6",
        "note": "2C-S5 (ekoa-dev port): the three ekoa-docx agent tools. dev's cortex/src/adapters/docx-mcp.ts does NOT port as-is - it builds its own createSdkMcpServer from @anthropic-ai/claude-agent-sdk and ONLY api/src/llm/ may import that (FIXED-3/8/13, the chokepoint is the crux of this slice). The three tools are re-expressed as ordinary SdkToolSpecs in agents/sdk-tools.ts (docxToolSpecs: docx_read / docx_source_set / docx_apply_edits), named as DOCX_TOOLS in the agents/tools.ts policy table (BUILD row only - they are artifact-bound) and mounted by the chokepoint on the ONE 'ekoa' in-process MCP server, so the wire names are mcp__ekoa__docx_* (dev's separate mcp__ekoa-docx__* server is gone). Handlers return STRINGS per the spec contract: dev's plainResult -> the raw projection text, dev's textResult -> JSON.stringify(payload, null, 2). NEW DocxToolSeams in agents/seams.ts is the ONLY route to the collaborators (apps/document-source.ts lifecycle - tier 5, never a sibling import; services/docx-redline.ts projectDocx; integrations/docx-fetch.ts's fetcher built over the workspace-credential seam shared with the served-app cloud plane), wired in server.ts and nowhere else; the honest default answers 'Word document support is not wired in this deployment.' as tool CONTENT. LOAD-BEARING and ported exactly: a RedlineBatchError comes back as tool CONTENT ({status:'rejected',applied:false,failures:[{index,error}]}), never a throw, so the agent reads adeu's occurrence guidance and self-corrects (detected by the error's own name+shape - app-docx.ts's property-based precedent - so the tools module stays collaborator-free). PROJECTION_LEGEND + MODIFY_GUIDANCE are copied BYTE-FOR-BYTE (verified by diffing the declarations; all 58 model-facing string literals in dev's tool region are present verbatim). build.ts binds appId = artifactId, userName = input.username (revisions attributed '<name> (Ekoa)') and allowedDirs = [projectDir] FROM THE RUN, never from tool arguments. tests/agents/docx-tools.test.ts is the faithful port of dev's cortex/tests/docx/docx-mcp.test.ts, run over the REAL document-source + redline engine on a temp EKOA_DATA_DIR + the shared contrato fixture (source_set by path, the 3-line legend, projection reads, dry_run not touching the stored blob, a committed native w:ins attributed to the acting user, ambiguous strict target rejected as CONTENT, the no-appId refusals, provider-branch validation + honest service errors, provider+query link reporting the chosen file); only the url/cloud ingest is a seam stub, replacing dev's vi.mock of the fetch module. Added on top of the port: the DOCX_TOOLS/translateAllowedTools mount proof (chat + text-attachments must NOT carry them), a deterministic generative sweep over the path jail (300+ candidates; fast-check is not a repo dep - the document-source suite set the precedent), the RedlineBatchError->content mapping pinned independently of the engine's message, and the not-wired default. DESCOPED + journaled (docs/decisions.md 2026-07-25): the chat-with-attachments read-only docx_read subset dev also mounted - ekoa-code's text-attachments run class mounts no in-process tools and has no attachment-path plumbing today; the DESCOPE is pinned by the no-appId test. Hermetic + LLM-free; enforced by npm test, not this census runner. Ratchet applies. Diagrams updated (FIXED-12): 02-module-map + 04-agent-job AS-BUILT annotations. The scaffold source-mode + web e2e and the docs/ledger wrap-up land in 2C slices 6..7."
      },
      {
        "group": "docx gate - native Word proof + re-upload round-trip (2C-S7)",
        "count": 1,
        "targetGate": "G6",
        "note": "2C-S7 (the 2C verification gate, docs/word-track-changes.md \u00a77). ONE new committed suite: tests/apps/docx-word-gate.test.ts. The engine suites (2C-S1) prove the OOXML contract on buffers with a PINNED timestamp; this one proves the same contract on the bytes the PRODUCT ships - the real apps/document-source.ts lifecycle on a temp EKOA_DATA_DIR (wall-clock revision dates, the per-app write lock, atomic temp+rename), i.e. exactly what GET /api/app-docx/current and POST /api/app-docx/clean hand a lawyer. Two gate sub-checks live here. (b) NATIVE WORD PROOF: unzip the produced .docx and assert the four things Word's review pane needs - every w:ins/w:del carries the acting author AND a parseable ISO-8601 w:date (asserted as a CONTRACT, not a pinned constant, because the service layer deliberately does not pin timestamps), word/comments.xml exists and is WIRED ([Content_Types].xml override + document.xml.rels relationship + every commentRangeStart/End/Reference id resolving to a real w:comment), word/commentsExtended.xml carries w15:done thread-wide (root AND the reply created in the SAME atomic batch) with w:comment left clean and exactly one commentsExtended relationship, and the CLEAN download (getClean) is free of <w:ins/<w:del/<w:delText while keeping the accepted text and dropping the deleted sentence. A TRIPWIRE test in the same block pins the divergence the gate uncovered: @adeu/core 1.28.0's accept_all_revisions ALSO strips the whole comment family (comments/commentsExtended/commentsIds/commentsExtensible + the in-document anchors), so the clean copy carries no review thread - Word itself keeps comments on accept-all, and ekoa-dev's doc claimed ours did too (never tested; dev's acceptAllRevisions is byte-identical). The behavior is KEPT deliberately (a final copy leaving the firm should not carry internal review notes) and recorded as docx-clean-drops-comments in docs/findings.md, so this test is what makes a future engine bump or a deliberate fix visible instead of silent. (d) RE-UPLOAD ROUND-TRIP: the produced redlined .docx is re-linked with setSource into a SEPARATE app (what a lawyer does after sending the file out) - the container must be accepted with revisions, comments and commentsExtended already in it, the projection must carry the same [Chg:n]/[Com:n] ids and the (RESOLVED) state, and - the substantive half - an accept driven by an id read off the RE-INGESTED projection must land, leave the other changes pending and NOT orphan the nearby comment thread (the anchor-repair guarantee). Repeated over a REPACKAGED copy (DocumentObject.load+save, what any editor in the chain does to the ZIP), so 'survives' does not just mean 'the same bytes came back'. Hermetic + LLM-free: fixture built in-process, no Mongo, no network; enforced by npm test, not this census runner. Ratchet applies from landing. The other gate legs are elsewhere BY CONSTRUCTION and never faked: (a) the served accept/comment/reply/resolve round-trip is the 2C-S6 playwright spec document-redline (run green 3/3 against a booted dev-api --built stack at this gate); (c) the LibreOffice conversion smoke is scripts/docx-libreoffice-smoke.mjs - deliberately NOT a vitest file, because a test that self-skips when soffice is absent is a green that proves nothing (run PASSED here on LibreOffice 26.2.4.2, both the working and the clean .docx converting to non-empty PDFs); (e) the live agent path needs a provisioned model credential + the dashboard and is recorded NOT RUN with its exact operator procedure in docs/word-track-changes.md \u00a77.1 - its tool-level equivalent is already deterministic in tests/agents/docx-tools.test.ts."
      },
      {
        "group": "crypto/jwt/git/device-auth",
        "count": 4,
        "targetGate": "G2"
      },
      {
        "group": "auth/infra root (managed OAuth, sanitizer, cancel, tokens, bases, Adobe)",
        "count": 12,
        "targetGate": "G7",
        "note": "claude-auth-* with llm/ at G7; error-sanitizer at G5; base-loader at G6"
      },
      {
        "group": "zoho-sign e-signature (BSM Adobe->Zoho swap)",
        "count": 1,
        "targetGate": "operator-run",
        "note": "2B-S1 (ekoa-dev port): tests/integrations/zoho-sign-token.test.ts - Zoho Sign OAuth token core (grant-code exchange+burn via ctx.onCredentialUpdate, refresh+in-memory cache, N-concurrent singleflight, dc->host mapping com/eu, sanitized token errors) + the executor's PROVIDER_CREDENTIAL_RESOLVERS wiring (resolveProviderCredentials('zoho-sign') injecting api_base+access_token so the shipped api/assets/integrations/zoho-sign/config.json httpConfig resolves). Hermetic: in-process Zoho mock via ZOHO_ACCOUNTS_BASE_OVERRIDE/ZOHO_API_BASE_OVERRIDE; runs green in the per-PR vitest lane from landing (module_tests are enforced by npm test, not this census runner). Ratchet applies. The live send/status/sign-url/document proxy + webhook + swap e2e land in later 2B slices (operator-run gate)."
      },
      {
        "group": "sign inbound-webhook trust model (zoho + adobe)",
        "count": 1,
        "targetGate": "G6",
        "note": "2B-S3: tests/integrations/sign-webhooks.test.ts - the security-critical trust model of the PUBLIC, credential-free /api/{zoho,adobe}-sign/webhook routes, tested directly on the pure deps-injected handleZohoWebhook + handleAdobeWebhook (no mongo/express/real provider). Proves (1) FORGED-PAYLOAD NO-OP: a payload claiming completion never advances - unknown id is never re-fetched, an owner-scoped re-fetch that shows the client unsigned is ignored (payload lied), and a fail-closed re-fetch error (exactly the notConnected Adobe backend) does not advance; (2) REPLAY IDEMPOTENCE: the same real completion delivered twice advances the proposta to 'Assinada' exactly once (guarded on stage). Plus the zohoClientSigned/adobeClientSigned state oracles (whole-request/agreement complete OR the named client action/member signed; a different signer's SIGNED never counts). Green in the per-PR vitest lane from landing; ratchet applies. The live send->sign->webhook durability probe is the 2B-S6 operator-run gate."
      },
      {
        "group": "branding token gates",
        "count": 4,
        "targetGate": "G6"
      },
      {
        "group": "phase-2 rule files (billing hard cap, slugs, branding, site)",
        "count": 11,
        "targetGate": "G6",
        "note": "billing-hard-cap at G7"
      },
      {
        "group": "share/fork link contracts",
        "count": 3,
        "targetGate": "G6"
      },
      {
        "group": "app pipeline",
        "count": 4,
        "targetGate": "G6"
      },
      {
        "group": "artifact-backend runtime",
        "count": 5,
        "targetGate": "G6"
      },
      {
        "group": "locale audits (move to web/)",
        "count": 3,
        "targetGate": "G9"
      },
      {
        "group": "operator-run feature suites (bases/tours/assistant/actions/knowledge-ingest/capability-stub)",
        "count": 17,
        "targetGate": "operator-run",
        "note": "vitest+contract suites landed by feature run 20260712-150958 (merged to main 2026-07-13): base-loader, build+domain-scoping, artifact-type(+contract), action-manifest(+contract), action-runtime, app-assistant(+contract), assistant-tools, assistant-panel, serving-tours, tour-writer, tour-player(+behavior), build-knowledge-ingest, capabilities-stub, jobs contract ext. All green in the per-PR vitest lane from landing; ratchet applies."
      },
      {
        "group": "chat-sheets B1 (sheets contract + canonical-context unit)",
        "count": 2,
        "targetGate": "G12",
        "note": "mega-run 20260717-190134 slice B1: tests/contract/sheets.test.ts (GET/PATCH/POST /sessions/:id/sheets* - derived list, rename, user revision; 2xx bodies vs the shared Sheet schemas + error envelopes + cross-user 404) and tests/agents/context-sheets.test.ts (applyLatestSheetRevisions + listSessionSheets derivation/materialisation + loadHistory canonicalisation; the traceId/memoriesUsed provenance writer is pinned in chat-thinking.test.ts). Green in the per-PR vitest lane from landing; ratchet applies."
      },
      {
        "group": "reply-summary B2 (post-run hook unit)",
        "count": 1,
        "targetGate": "G12",
        "note": "mega-run 20260717-190134 slice B2: tests/agents/reply-summary.test.ts (the decision-B.E post-run hook in isolation over the fake chokepoint transport - fresh-sheet turn emits ONE FAST user_work reply-summary call + ONE schema-valid reply_summary notification; revision turn feeds the model the edit instruction + compact diff basis, never the whole reply; model failure / unparseable output emit NO event and never throw; compactDiffBasis caps). Pipeline threading (event ids == the persisted turn's derived sheet ids, notifications channel not the run stream) is pinned in chat-lifecycle.test.ts; the event schema representability in shared/src/contract.test.ts. Green in the per-PR vitest lane from landing; ratchet applies."
      },
      {
        "group": "steer/Conduzir (mid-run message injection, 2026-08-07)",
        "count": 2,
        "targetGate": "G12",
        "note": "Conduzir: the queued-message banner's steer path - inject a queued message into the IN-FLIGHT run instead of waiting (Agent SDK streaming-input mode at the chokepoint). tests/llm/steering.test.ts (SteerQueue order/close/push-after-close; the REAL default transport over a mocked SDK query: streaming input engaged only when steerable, a steered message becomes a second user turn in the SAME query, input closes on the first result with nothing unconsumed so an un-steered run exits after one turn, usage BILLS across all results; runAgent handle.steer accept-window). tests/agents/steering.test.ts (steerLiveRun over the fake transport: entry.steer wiring, steered message persists as a REAL user turn on the session transcript, OWNER-ONLY - stricter than cancel, an admin steering would be impersonation - and terminal/unknown runs refuse with steered:false, the queue-and-flush fallback). Contract halves live in contract/chat.test.ts + contract/jobs.test.ts (steer endpoints validate vs ChatRunSteerResponse/JobSteerResponse, empty message 400 envelope, schema-coverage COVERED). Green in the per-PR vitest lane from landing; ratchet applies."
      },
      {
        "group": "brand-logo image fit (dimension probing + banner demotion + oversized rescue, 2026-08-07)",
        "count": 1,
        "targetGate": "G12",
        "note": "Live defect 2026-08-07 (ekoa.io/info): the og:image social banner was stored as the brand logo. tests/services/branding/image-fit.test.ts (probeImageDims via sharp; isSocialBannerShape truth table - 1200x630/1024x512/16:9 flagged, marks/lockups/thumbnails never; logoShapeScore ordering; downscaleToStorableLogo bounds a 2048x2048 png to 1024 keeping png, never enlarges, rejects non-rasters - the rescue for real logos served as multi-MB PNGs that the flat download cap used to silently drop). The selection-side halves (banner-flagged candidate drops below every source tier, measured shape beats byte size, dimension-less candidates keep byte-size behaviour) are pinned in the existing tests/services/branding/brand-assets.test.ts. Green in the per-PR vitest lane from landing; ratchet applies."
      },
      {
        "group": "agent-revision routing B5 (chip -> revision on the SAME sheet)",
        "count": 0,
        "targetGate": "G12",
        "note": "mega-run 20260717-190134 slice B5: no new vitest FILES - extensions to existing suites (count stays 0 to keep the file census honest). chat-lifecycle.test.ts gains the reviseSheetId pipeline tests (reply persists as an AGENT revision on the SAME sheet with instruction = user message; the persisted turn back-references sheetId/revisionId/revisionNumber; reply_summary carries the revised sheet's ids + revision ordinal; the hook receives the REVISION-turn input - instruction + compact diff, never the whole reply; unknown sheet falls back to fresh). context-sheets.test.ts gains the carrier-turn rule (a metadata.sheetId assistant turn derives no second sheet and is dropped from model-bound history - locked 5+7). contract/chat.test.ts + shared/src/contract.test.ts pin the extended ChatRunCreateRequest.reviseSheetId and the reply_summary.revision field; contract/chat.test.ts also pins run-creation session ownership (another user's / unknown sessionId -> uniform 404, the B1 sessions/sheets idiom). The e2e half is the summary-cards-chip spec (band4); the client-side summary ID-routing/buffering (codex B5 fixes 1+2) is pinned by frontend_unit orchestration-reply-summary. Green in the per-PR vitest lane from landing; ratchet applies."
      },
      {
        "group": "voice relay C1 (WS STT/TTS against stub providers)",
        "count": 4,
        "targetGate": "G12",
        "note": "mega-run 20260717-190134 slice C1: tests/voice/stt-session.test.ts (scripted stub STT turn over a real WS upgrade - ready/speech_started/interim/final/utterance_end in order, every wire message safeParsed against VoiceSttServerMessage, per-stage latency line with audio_in_first/first_interim/utterance_end + deltas, org+user attribution on the live session record, close_stream drain, plain-PCM counted-but-silent, bad control JSON -> PT error without dropping the session), tests/voice/tts-stream.test.ts (say -> valid WAV header + tone frames + audio_end; mid-stream {clear} barge-in aborts - cleared confirms and NO audio/audio_end follows; superseding say clears the in-flight turn; idempotent bare clear; tts_first_audio latency; ttsChars/turns on the attributed record; provider fallback logged, never silent), tests/voice/upgrade-auth.test.ts (missing/garbage token + validly-signed unknown-subject rejected BEFORE the socket opens on both paths, fail closed via the verifySseToken chokepoint, structured voice.auth_failure; valid active user admits), tests/voice/inactivity-timeout.test.ts (VOICE_INACTIVITY_TIMEOUT_MS knob - idle socket gets the PT-PT VOICE_TIMEOUT error + clean close + voice.session.timeout log; client activity resets the timer). helpers.ts is scaffolding, not a census file. shared/src/contract.test.ts gains the voice WS union representability block (both directions + off-REST-surface pin). Green in the per-PR vitest lane from landing; ratchet applies."
      },
      {
        "group": "voice metering C2 (non-token usage ledger + voice audit vocabulary)",
        "count": 2,
        "targetGate": "G12",
        "note": "mega-run 20260717-190134 slice C2: tests/billing/voice-usage.test.ts (recordUsageCounters on billing/tracker.ts, the single metering writer - voice_stt_ms + voice_tts_chars as SEPARATE per-org-per-session counters keyed voice:<orgId>:<sessionId>, org+user attributed, idempotent upsert, sanitised amounts, zero-usage writes nothing; billing truth: voice usage never writes token_events or moves the token meter/credit, and a mixed token+voice workload leaves token arithmetic identical) and tests/voice/metering.test.ts (real WS sessions over stub providers with mongo-mem: STT close bills the whole ungated capture at the known rate (sttMsOfBytes) into usage_events THROUGH the tracker; turn_committed annotates voice.turn with the transcriptMessageId ref - uncommitted turns flush ref-less at close; TTS close meters submitted chars and each say lands a voice.tts row with provider/lang/sheetId refs; rows are refs-only (no transcript/say text), registoEntry renders them schema-valid with usageCounts + targetIds; static single-writer check: voice/ never references a ledger store). shared/src/contract.test.ts gains turn_committed + say.sheetId cases and the RegistoEntry voice-vocabulary/usageCounts representability test. Green in the per-PR vitest lane from landing; ratchet applies."
      },
      {
        "group": "voice TTS text pipeline + context note C5 (say -> sanitize -> normalize -> chunk -> per-sentence synthesis)",
        "count": 3,
        "targetGate": "G12",
        "note": "mega-run 20260717-190134 slice C5: tests/voice/speakable.test.ts (the C3 PT-PT/EN number-normalization suite moved VERBATIM from web/__tests__/voice with the module's relocation to api/src/voice/text/speakable.ts - relocation decision in docs/decisions.md 2026-07-18; assertions unchanged, ratchet continuity), tests/voice/tts-text-pipeline.test.ts (sanitizeForSpeech strips fenced code incl. unclosed, tables, images, bare URLs/autolinks; unwraps links/emphasis/inline code/headings/quotes/lists; identity on plain prose. chunkSentences: PT abbreviations + initials + 'p. ex.' never split, dotted numbers/versions never split mid-token but a digit-ended sentence still ends, paragraph boundaries, over-long clause split lossless. speakableChunks composition per lang incl. the pt-BR-uses-PT limit and [] for code-only. WIRING over a real WS tts-stream: a capturing provider registered under the CONFIGURED 'google' key receives exactly the pipeline's sentences in order with attribution, one RIFF container per sentence on the wire against the stub too, and a nothing-speakable say completes speaking->audio_end with zero audio frames and no error), tests/agents/voice-context-note.test.ts (voiceContextNote PT-PT content: read-aloud/spoken-prose/name-artifacts, explicitly preserves full reasoning + detail with a brevity-regression guard; assembleRunContext voiceActive:true injects it after the EKOA identity for chat and leads non-chat, absent by default; the client wire signal is the documented C7 seam on StartChatRunInput.source). metering.test.ts single-writer walk made recursive (voice/text/ subdir). Green in the per-PR vitest lane from landing; ratchet applies."
      },
      {
        "group": "voice-source wire C7 (closing the C5 seam on StartChatRunInput.source)",
        "count": 0,
        "targetGate": "G12",
        "note": "mega-run 20260717-190134 slice C7: no new vitest FILES - extensions to existing suites (count stays 0, B5 precedent). chat-lifecycle.test.ts gains the voice-sourced-run test (input.source:'voice' -> the system prompt carries voiceContextNote's 'Sess\u00e3o de voz ativa' text; a plain run over the SAME session gets none; the paired voice.turn activity row - written independently by the voice WS session itself, api/src/voice/index.ts auditTurn, C2 - carries source:'voice' + the transcriptMessageId ref, proving both halves hold for one logical voice turn without duplicating C2's own metering.test.ts). contract/chat.test.ts + shared/src/contract.test.ts pin the extended ChatRunCreateRequest.source (optional literal 'voice'; any other string/empty string rejected -> 400 envelope). The client wire (page.tsx handleChatSend/handleSendMessage + chat-panel.tsx sendVoicePathRef threading source:'voice' on direct sends) and the e2e half are pinned by web/e2e/voice-proof.spec.ts (band5_live_proof). Green in the per-PR vitest lane from landing; ratchet applies."
      },
      {
        "group": "portal certid\u00e3o connectors E2/E3 (retrieval-by-access-code: comercial/predial/civil)",
        "count": 1,
        "targetGate": "G12",
        "note": "mega-run 20260717-190134 slices E2/E3: tests/legal/portal-connectors.test.ts (fetchCertidao parse-unit tests per source against committed fixtures api/tests/e2e/fixtures/portal-certidao-{comercial,predial,civil,invalido}.html - company/property/civil record shapes; empty-code, invalid/expired-code, non-2xx, and thrown-fetch all resolve to a clean PT-PT 'indispon\u00edvel' error, never a throw; retrieveCertidao end-to-end over mongodb-memory-server - attaches a documentos row (attachPortalDocument) AND a document.retrieved eventos row (attachPortalEvent) + audit, org-scoped, access code never persisted/logged; a failed retrieval attaches nothing (no blob saved, no row written); wrong-org caller rejects with PortalOrgMismatchError). contract/legal-plane.test.ts gains POST /api/legal/portal/certidao coverage (happy path validated against the shared descriptor + rendering on the GET read surface, field validation 400s, bad-code 503, gate reuse). shared/src/contract.test.ts gains PortalCertidaoRequest/Response representability (the 3-source subset enum, required-field 400 shapes, an extended-record passthrough). Green in the per-PR vitest lane from landing; ratchet applies."
      },
      {
        "group": "portal receiving surface E1 (retroactive registration)",
        "count": 1,
        "targetGate": "G12",
        "note": "2026-07-18 retroactive: tests/legal/portal.test.ts (mega-run 20260717-190134 slice E1: attachPortalDocument/attachPortalEvent org-scoped writes onto the documentos/eventos owner-spine satellites + the single logActivity seam refs-only, PortalOrgMismatchError on a cross-org caller, listPortalDossierRecords round-tripping origem:'portal' rows back into PortalDocument/PortalEvent and skipping non-portal rows) was landed at E1 but never added to this ledger - found while registering the E4 entry below. Green since E1's landing (commit d3bcb1f); ratchet applies from this registration."
      },
      {
        "group": "insolv\u00eancia watcher E4 (Citius p\u00fablico, poll + idempotent watch.hit)",
        "count": 1,
        "targetGate": "G12",
        "note": "mega-run 20260717-190134 slice E4 (BRIEF \u00a78 item 4): tests/legal/insolvencia-watch.test.ts (fetchInsolvenciaPublicacoes parse-unit tests against committed fixtures api/tests/e2e/fixtures/citius-insolvencia-v{1,2}.html reusing citius.ts's parsePublicacoes/decodeHtml verbatim - empty subject, non-2xx, and a thrown fetch all resolve to a clean PT-PT 'indispon\u00edvel' error, never a throw; pollInsolvencyWatches end-to-end over mongodb-memory-server - a registered watch polls -> ONE watch.hit PortalEvent attached via attachPortalEvent (eventos row + refs-only portal.watch.hit activity row, watched name never reaches the audit metadata), org-scoped (PortalOrgMismatchError on a cross-org caller); idempotent: a re-poll of the SAME fixture emits zero new events (seenRefs dedup on the watch row itself, not events/queue.ts - see the module doc comment for why); a poll against a fixture with one new publication emits only the delta; a watch with no registered rows emits nothing without throwing; one subject's fetch failure never blocks a sibling subject on the same watch). contract/legal-plane.test.ts gains POST /api/legal/portal/insolvency/poll coverage (happy path + idempotent re-poll validated against the shared descriptor + rendering on the GET read surface, missing-processoId 400, no-watch 200-empty, gate reuse). shared/src/contract.test.ts gains InsolvenciaPollRequest/Response representability. DECISION recorded in api/src/legal/insolvencia-watch.ts's doc comment: no scheduler/cron is wired this slice (none exists cleanly for a public-tier connector; the declared automation listenerConfig belongs to the out-of-scope signed-in eTribunal integration) - pollInsolvencyWatches is a plain callable an operator/cron invokes directly, plus the manual POST route above for deterministic test/gate triggering. Green in the per-PR vitest lane from landing; ratchet applies."
      },
      {
        "group": "Part E gate E5 (BRIEF \u00a78: one dossi\u00ea, certid\u00e3o + watcher event, both rendering)",
        "count": 1,
        "targetGate": "G12",
        "note": "mega-run 20260717-190134 slice E5 (BRIEF \u00a78 item 2, the Part E gate): tests/legal/e5-portal-gate.test.ts - the single committed end-to-end driver the gate names literally, distinct from the per-connector unit suites (E1-E4, each on its own synthetic processoId) and legal-plane.test.ts (real router but stub deps): mounts the real legalRouter over REAL deps wired onto a real CollectionsEngine (mongodb-memory-server), creates ONE processos row as the dossi\u00ea, POSTs /api/legal/portal/certidao (comercial, committed fixture) which attaches a PortalDocument, registers an insolv\u00eancia watch directly on the owner spine + POSTs /api/legal/portal/insolvency/poll (committed fixture) which attaches a watch.hit PortalEvent onto the SAME dossi\u00ea, then GETs /api/legal/portal and asserts BOTH render together (documentos length 1 + eventos length 2, one document.retrieved + one watch.hit), validated against the shared PortalCertidaoResponse/InsolvenciaPollResponse/PortalDossierRecordsResponse descriptors, asserting the exact PT-PT strings the gate names verbatim ('Certid\u00e3o permanente comercial', 'Nova publica\u00e7\u00e3o para a contraparte Contraparte Exemplo, Lda') plus the underlying documentos/eventos row shape DocumentosTab.jsx/CronologiaTab.jsx render directly (origem:'portal', tipo/titulo fields); a final case proves org isolation (a second owner's app reading the SAME processoId sees empty arrays, confirmed on the raw owner spine too). E5 also verified DGSI/DRE (api/src/legal/research.ts, unmodified this run - docs/autothing/runs/20260717-190134-9d4c1cbf/memos/e5-dgsi-dre-verify.md) and added a PT-PT ORIGEM_LABEL entry for origem:'portal' in the legal-dossie scaffold's doc-helpers.jsx (badge previously fell back to the raw string 'portal'). Green in the per-PR vitest lane from landing; ratchet applies."
      },
      {
        "group": "security/privacy suites (Cofre Phase 0)",
        "count": 34,
        "targetGate": "G8",
        "note": "Cofre discovery-gate Phase 0 remediation (R-1/R-2/R-6): the NAMED security/privacy suite class CLAUDE.md's QA process mandates, which did not exist before this change - api/tests/security/ held four authz files and no class registered them. THIRTY-FOUR new files. All are hermetic, LLM-free, and green in the per-PR vitest lane from landing (module_tests are enforced by npm test, not this census runner). Ratchet applies. redaction.test.ts (24): the value-keyed SecretRegistry - one case per regression of the two divergent maskers it replaces, including the http-template `***...1234` plaintext-SUFFIX leak, the api-call raw-literal-only match that let URL-encoded/base64/JSON-escaped forms through, and the silent sub-4-char skip that BOTH copies shared (now surfaced on registry.unmaskable instead of dropped). action-path-containment.test.ts (32) + action-file-primitives.test.ts (11): ekoa_action file containment. The resolver suite pins symlink escape, the not-yet-created-leaf WRITE escape a lexical ENOENT fallback would allow, and the credential-bearing denylist applied to the REAL path so a benign label cannot launder ~/.ssh/id_rsa; the primitives suite proves the wiring through executeRecipe - the escape that matters is the one a MODEL-authored recipe can reach - plus per-owner and per-ORG isolation and a workspace-identifier traversal guard. origin-binding.test.ts (20) + api-call-origin-binding.test.ts (8): credential-to-origin binding (I6). The executor suite runs the gate's actual exfiltration scenario end-to-end and asserts fetch was NEVER called, that the attacker-chosen URL is not echoed into the persisted record, and that an integration declaring no usable host REFUSES rather than defaulting open - unbound never means unrestricted. Plus, from the remediation's later slices: screenshot-plane.test.ts (14) \u2014 the per-step screenshot plane is authenticated and TENANT-scoped, answering 404 not 403 across tenants so existence is not an oracle, refusing an unattributable legacy run rather than serving it, and covering the retention sweeper and a traversal-escape attempt on the erasure path; page-value-leaks.test.ts (44) \u2014 the nonMemorable memory class (excluded even at core tier, where rows are otherwise ALWAYS injected) plus proof the action cache writes no fill/navigate/select VALUE into the term-scored content, and the secret-shaped input-name vocabulary pinned in BOTH directions because a false positive silently refuses an ordinary input; bridge-key-split.test.ts (11) \u2014 per-pairing signing secrets that differ per pairing, are not the platform JWT secret, are encrypted at rest, do not cross-verify, survive a redial, vanish on revoke, and refuse an EMPTY key at both sign and verify; credential-boundaries.test.ts (19) \u2014 the seven boundaries the completeness pass found with ZERO coverage while other findings were argued down on the basis that they already handled it (scrubCredentials, the template-vars {{input.credentials}} refusal in all four syntactic forms, redactSecretValuesIn/redactSecretsDeep/redactUrl/redactHeaders/redactBody and the maskValue compat alias). And the WS-B centrepiece: cofre-policy-lock.test.ts (28) \u2014 unwrap() is the ONE function that turns a reference into a value, and it is fail-closed on four INDEPENDENT grounds, each asserted separately: default deny with no grant, TTL expiry, this_run grants consumed by RUN IDENTITY (a different run, or no run in context, refuses) rather than by a clock, origin binding enforced INSIDE unwrap, owner-scoping that refuses a same-org colleague AND a foreign-org admin with the SAME NOT_FOUND so the seam is not an existence oracle, ciphertext-only at rest with a view that has no value field, lock-now/lock-all (idempotent, and lockAll never touches another user's grants), and I7 \u2014 every non-this_run duration on a certificate_identity REFUSED at the service as well as in the schema. This seam is what WS-K's KMS envelope and the parked passkey-PRF crypto-lock install behind, so a bypass would turn both into repo-wide refactors; the suite exists to make a bypass fail loudly. And i9-env-injection.test.ts (29) \u2014 the I9 primitive, plus a STATIC GUARD that envWhitelist is deleted rather than merely unused: it was a list of variable NAMES accepted FROM THE PLANNER (a model) that buildEnv resolved against the CORTEX SERVER's OWN process.env \u2014 holding provider keys, ENCRYPTION_KEY, JWT_SECRET and DB creds \u2014 and shipped to a user machine, so envWhitelist:['JWT_SECRET'] was a platform compromise expressed as an ordinary step field. Deleted, not hardened, because local_command is unreachable end-to-end today (setDaemonConnectionResolver stays on its honest default at the composition root) so deletion cost nothing and becomes impossible once that path is wired. The suite also pins that a failure is never a PARTIAL environment, and that PATH/LD_PRELOAD/NODE_OPTIONS/BASH_ENV/IFS/*_PROXY are refused as injection TARGETS \u2014 writing those turns an environment injection into code execution or silent re-routing. And screenshot-masking.test.ts (12) \u2014 the PIXEL plane. llm/client.ts anonymises prompt+systemPrompt and forwards images VERBATIM, and a test PINNED that forwarding as the contract, so the pipeline covered text and not pixels while docs/security.md described it as covering 'all model-bound text' (true, and false-by-omission). Fixed browser-side because a Cortex-side transform on a finished PNG is OCR-and-hope: credential regions are masked by LOCATOR at capture time so the pixels are never rendered into the buffer, with a SOLID mask colour (a blurred credential is still a credential to anyone with the original font metrics) and PT-PT field names covered. The decisive case is the FAILURE MODE: a masking failure returns null \u2014 no screenshot \u2014 and can never degrade to an unmasked capture; suppression during a credential window takes no picture at all, which is stronger than masking the field. The pinning test in tests/llm/client.test.ts is re-framed in place as plumbing-only with a pointer to where the pixel plane is actually protected. SEPARATELY registered under contract_tests_from_ruleset by its own gate entry: tests/contract/cofre.test.ts (10) drives the six new /api/v1/cofre endpoints through the REAL app, proving the VALUE is write-only across the whole surface, that a foreign item answers a uniform 404 rather than a 403 (a 403 confirms existence), and that the I6 and I7 refusals surface as proper 4xx ERROR ENVELOPES rather than 500s. The six descriptors move from PENDING to COVERED in the schema-coverage gate in the same change, which is the mechanism that forces a new endpoint to travel with a contract test. And browser-context-lifecycle.test.ts (5) \u2014 dispose() closed only the PAGE, so every run leaked its browser context, and with it the whole cookie jar of an authenticated session, for the lifetime of the process. Separately the module docblock described 'the owner's PERSISTENT context' with concurrent runs sharing cookies while the composition root has always returned browser.newContext(); reading the docblock instead of the composition root produced wrong audit conclusions twice, so the ACTUAL model (fresh per session, no jar shared across runs or owners) is now pinned by test rather than asserted by comment. And canvas-token-class.test.ts (8) \u2014 found by the F-1 streaming pass and VERIFIED EMPIRICALLY with a throwaway probe before being written up: the canvas screencast token is signed with the platform secret and carried NO class marker, so auth/jwt.ts (whose guard knew only ekoa-bridge) ACCEPTED it, and requireAuth passed it end to end because the jti exists, it is unrevoked, getActivation(sub) resolves and iat is fresh. Every route authorizing on req.user.sub ALONE never reads role/orgId, so a leaked 600-second canvas token was a platform bearer token for gateway-key MINT (returning a long-lived API key), the bridge token endpoint and the Cofre item routes. Fixed with an ekoa-canvas audience mirroring the bridge guard; the suite pins BOTH directions of the refusal and includes a VALIDLY SIGNED pre-audience token so the traceId branch is exercised rather than a signature failure. And typist.test.ts (13) \u2014 the ONE credential-touching browser capability. Each property asserted separately: the value is typed as out-of-band CDP key events and locator.fill() is NEVER called (fill sets the DOM value directly, observable to any page script); fill-and-submit are ONE unit so control never returns to the agent with a filled password field on the page, which is what makes read-back impossible by construction; origin is checked BEFORE the unwrap so a wrong-origin page never causes a decrypt; the Cofre lock applies exactly as it does to any other consumer; suppression is a PRECONDITION - the typist REFUSES to fill when it cannot confirm the screencast is stopped, and the decisive assertion there is that NOTHING was typed; the credential window resumes even when the fill throws; and an unknown form (no password field, or one still present after submit) pauses for the relay rather than being handed to the fixer to guess at. The selector vocabulary is asserted to be fixed DATA carrying nothing executable (I5). And envelope-encryption.test.ts (12) \u2014 WS-K's K-1. The wrapping is a SEAM (data/kms.ts) with a local implementation today and a Cloud KMS one that plugs in with no call-site change, so turning on real KMS is configuration rather than a refactor. The suite pins the properties that matter: a v2 row cannot be decrypted under another TENANT; a fresh data key per record means two encryptions of the same value differ; tampering fails GCM auth; a malformed v2 string is refused rather than guessed at; a wrapper failure SURFACES rather than degrading to a weaker key; and \u2014 the adoption property \u2014 a legacy v1 row still decrypts unchanged, so this needed no migration flag day. One case states the v1 weakness as a TEST rather than a comment: a v1 row decrypts regardless of the tenant argument, because it was never tenant-bound, which is exactly why K-4's migration exists. And egress-policy.test.ts (14) \u2014 per-run egress (E-3/WS-I). The gate first read the global localBrowserEnabled flag as a 'silent datacenter fallback' and its verifier corrected that (production halts honestly in awaiting_daemon); the real defect is narrower and still real \u2014 the choice is GLOBAL and UNDECLARABLE. The suite pins that a run never leaves by a route it did not ask for: the DEFAULT is refuse, a machine in ANOTHER ORG is never a candidate (filtered at construction, not after), an offline machine / one not advertising the capability / one with no advertised endpoint are all ignored, a PINNED machine refuses when that specific one is unavailable, and 'datacenter-fallback' is a DISTINCT outcome from a declared 'datacenter' so a run record shows the difference between an explicit choice and a degraded one. And cofre-sessions.test.ts (10) \u2014 WS-G. A captured storageState walks past the password AND the MFA prompt, so it is CREDENTIAL-EQUIVALENT: stored as a Cofre item through the same org-bound envelope, LOCKED by default (a capture does not grant use), origin-bound so it cannot be replayed against another host, and absent from the item view. Origins are DERIVED from the cookies rather than guessed by the caller \u2014 a caller that guesses either breaks the session or over-binds it \u2014 and a capture with no derivable origin is REFUSED at capture time, where the reason is obvious, rather than becoming replayable anywhere. This closes the gap where routes/integrations.ts answered available:false while a shipped CITIUS asset promised the user the session would be guardada cifrada. And bridge-replay.test.ts (13) \u2014 J-2. A bridge token is a 600s bearer credential AND attachLiveConnection retires the incumbent on redial (correct for a daemon reconnecting after a blip); compose the two and a captured token did not merely replay, it EVICTED the real daemon and received that pairing's delegate frames. The decisive case is therefore not 'the replay is refused' but 'the replay is refused AND the real daemon still holds its socket' \u2014 a fix that rejected after handleUpgrade would pass the first assertion and still have lost the daemon. Tokens now carry a jti spent exactly once; a token with NO jti is refused outright rather than admitted unspendable (the bypass a jti-if-present design would leave); the ?token= query form is gone, pinned by a case proving the SAME token works in the header so the refusal is about transport, not validity; and the token-epoch check reaches the bridge, including the half that connect-time checks cannot give \u2014 an ALREADY-OPEN socket is dropped when the epoch bumps, the account deactivates or billing locks, because a WS is authorised once and then lives for hours. Single-use compatibility with the shipped daemon was VERIFIED by reading ../ekoa-bridge (mints fresh before every dial, no caching), not assumed, and all three controls were plant-tested red before landing. And bridge-secret-delivery.test.ts (10) \u2014 J-3. secret.deliver is the only frame carrying credential material. The nonce is REDEEMED BEFORE the unwrap, so a replay causes no decrypt and no second copy on the wire; the suite asserts the wire length, not just the rejection, because a design that refused after sending would satisfy a rejects-check and still have leaked. A delivery is bound to the pairing it was authorised for (so a caller that can influence the pairing id cannot redirect a credential to another of the org's machines), an offline machine is a REFUSAL rather than a delivery that never left, and the redeem-first trade-off \u2014 a failed send consumes the authorisation, so retry means a NEW invocation \u2014 is pinned as a decision on record rather than something a later reader 'fixes' back into a replay window. The daemon-side half (RAM only, absent from config.json and the ledger, zeroized) is a COUNTERPART obligation flagged C6/C7 in docs/bridge-counterpart-changes.md and tested in that repo; this suite does not pretend to prove it. And write-approval.test.ts (18) \u2014 J-7, two authorisation bugs sharing one theme: a control that looked like a control. (1) The daemon gates a first write on confirmed===true and its header says the user assents Cortex-side; nothing Cortex-side checked it and agents/sdk-tools.ts literally INSTRUCTED the model to set it, so \u2014 because delegateToLocal passes the model's TaskProgram through verbatim and SIGNS it with the pairing secret \u2014 Cortex's signature laundered a model self-assertion into an authorisation the daemon trusts. The suite pins the module AND the wiring, and the decisive delegation case asserts NOTHING WAS DISPATCHED rather than merely that the call returned denied: a check that ran after the signature would be no check at all. Approvals are per-file, single-use, non-transferable across users or machines, and expiring, because a confirmation is an answer to a question asked seconds ago. A malformed task is treated as carrying no approval, never as an accidental allow. (2) The command shape wildcarded exactly the dangerous part: `cat ~/notes.txt` stored as `cat <FILE>` matched `cat ~/.ssh/id_rsa`, and `curl -s <URL>` turned one approved API call into an approved EXFILTRATION primitive. command-shape.ts had already made this argument once for `bash -c` (a shell body is arbitrary code, never a class); J-7 finishes it \u2014 there is no argument class that can be safely wildcarded, so the shape is the exact command, and pre-J-7 wildcard rows are refused rather than left to match. Approvals also gained tenant scoping, machine binding and a 90-day TTL, none of which existed. The second bug is LATENT (local_command is unreachable end-to-end \u2014 the composition root leaves setDaemonConnectionResolver on its default), which is exactly when it is cheapest to fix and easiest to forget. Both controls plant-tested red before landing. And bridge-audit.test.ts (8) \u2014 J-6. Cortex persisted NOTHING about bridge invocations: only kind==='read' ledger rows left the machine, into a 15-minute in-memory Map built to render a trust chip. The plane with the most physical access to a user's data \u2014 the one reading their files and running commands on their computer \u2014 was the only one with no durable record. Dispatch and settlement are SEPARATE rows on purpose: a dispatch with no matching settlement is what a machine that went dark mid-task looks like, and one row written at the end would erase that. A REFUSED dispatch is recorded too, because a run of them is what an attempted bypass looks like from the outside. The other half of the suite is what must NEVER be written: EgressLedgerRow carries `path`, and the standing 18.2/FC-407 invariant is that those rows are never persisted hosted-side because a path is itself sensitive (client names live in folder names; for a legal practice a directory listing is privileged). BridgeRegistoMetadata is .strict() with no path field, and the suite pins BOTH directions \u2014 a caller that passes a path has its metadata dropped wholesale rather than written with the field stripped, and the schema itself rejects path/relPath/pattern \u2014 so a later 'just add the path, support needs it' fails a test rather than a reviewer's attention. Tool extraction records NAMES from a closed set, never arguments: a grep pattern is user or model text that can embed anything. The audit write is fire-and-forget with its rejection swallowed (a delegation must not fail because a bookkeeping row could not be written), so the suite POLLS for rows rather than sleeping a fixed interval \u2014 a sleep tuned to one machine is a flake on a slower one, and this suite asserts on absence too, where a short sleep would read as a pass. And attended-ceremony.test.ts (12) \u2014 J-5. Portuguese legal portals authenticate with a smartcard in a physical reader, and the general solution is a card stack in Cortex: PKCS#11, driver matrices, .pfx custody \u2014 a large surface holding the most sensitive credential a lawyer owns, on a server with no business seeing it. The rail instead asks the machine that ALREADY HAS the reader to open the browser and hold it while the human completes the ceremony; what returns is the session. The load-bearing assertion is a NEGATIVE one: no certificate material transits, what comes back is cookies-only (a card session cannot be re-minted from it \u2014 if it leaks it expires, where a certificate would not), and a static gate asserts api/src imports NO card library. That gate matches IMPORTS rather than prose on purpose: attended.ts NAMES pkcs11 and .pfx in its docblock precisely to explain what it avoids, and punishing the explanation would push the reasoning out of the file where it belongs; the matcher is self-tested against a planted import so an empty result means 'none present' rather than 'the regex never fires'. Three refusals are pinned \u2014 an unopened ceremony, a machine answering for another, and the dangerous one: a session for a DIFFERENT ORIGIN, which without the check would mint a perfectly valid, correctly-encrypted, correctly origin-bound Cofre item for the WRONG SITE, quietly usable later by anything looking a session up by label. Offline is a refusal rather than a queued promise, because a ceremony needs a human standing at the machine. One ceremony yields one session (a duplicate push is refused, consumed before the store write). The session records establishedBy machine + residential egress, which is what WS-I reads at checkout: a card-established session replayed from a datacenter IP is exactly the pattern portals flag. And bridge-ingress-redaction.test.ts (15) \u2014 H-4. When Cortex delivers a credential to a machine (J-3), a bash step there can trivially echo it (env | grep, a failing curl printing its own argv), and frames were parsed and dispatched RAW \u2014 into the persisted run record, the SSE stream, and for provider_request the model. The daemon cannot fix this: it does not know which string in its own output is a secret, and asking it to guess is the pattern-matching-a-value problem value-keyed redaction exists to avoid. Cortex knows, because Cortex delivered it. The decisive case drives a REAL frame through a REAL socket and asserts on what the awaiting delegation receives \u2014 added after a plant proved the isolated cases stayed green with the call site REMOVED, i.e. the suite was testing a filter nobody called. Scope is per pairing (a value delivered to one machine does not filter another's frames, so a different machine holding it stays visible) and released when the socket drops. Structural ids and ledger_row pass through untouched: substituting inside a join key corrupts it, and a path is never persisted hosted-side anyway. RESIDUAL pinned in both directions rather than assumed away: the name-pattern leg understands JSON keys and key=value pairs, so a colon-separated `Authorization: Bearer x` in free text survives it and is only caught when Cortex delivered the value \u2014 widening that leg would fire on ordinary prose and a filter that mangles output is one people route around. And capability-grants.test.ts (12) \u2014 I-3. A machine ADVERTISES what it can do in its hello frame, and selection read that list as if it were an authorisation: a pairing claiming egress.residential became a candidate to route a tenant's traffic through. Advertisement is a SELF-ASSERTION by the machine \u2014 the same category of control as J-7's model-set `confirmed`, wrong for the same reason: a daemon that is compromised, misconfigured, or simply running a newer build than its owner expected could widen its own privileges by claiming more. A capability is now usable only where the machine advertises it AND the org granted it, with DEFAULT DENY \u2014 a freshly paired machine can do nothing, which is the direction that fails safe, since inheriting whatever the machine claims makes a new machine maximally privileged exactly while nobody is watching. The intersection is asserted in both directions (a grant for something no longer advertised does not resurrect it; an advertisement with no grant is not enough) and org is part of the grant KEY rather than a filter applied after. Re-granting a revoked capability revives it, deliberately unlike a PAIRING revoke, which is a terminal tombstone \u2014 treating a capability revoke as terminal would push admins toward re-pairing the machine, which is strictly worse. Plant-tested: restoring the advertised list to selection turns three cases red. And ciphertext-v2-migration.test.ts (9) \u2014 K-4, the evidence that was missing when the migration was journaled as landed while having no entry point, no gate and no test (it had never compiled, let alone run). K-1 could be adopted with NO FLAG DAY precisely because v1 rows keep decrypting, and the cost of that grace is that a v1 row stays under the FLAT GLOBAL key and is not tenant-bound \u2014 so K-1's tenant binding was a property of NEW WRITES until this migration actually ran over the old ones. The decisive case is therefore not 'the row changed shape' but 'the row can no longer be decrypted under the WRONG tenant', with the pre-migration defect stated as a test rather than a comment. Also pinned: idempotent re-runs, ONE undecryptable row counted but never fatal (an abort on the first bad row would leave every later row on the flat key, the opposite of the point), a row with no org skipped rather than mangled since there is no tenant to bind it to, and \u2014 the shape fix \u2014 that the gate is READ-ONLY, byte-identical afterwards, because the original noV1CiphertextRemains() called the MIGRATION to answer its question and so wrote to production to read it. And pipedream-master-switch.test.ts (8) \u2014 a REAL PRODUCT BUG found by repairing the e2e estate, not by reading code. PATCH /api/v1/settings persists through patchOrgSettings, which writes the ORG document (orgs[orgId].settings); the enforcement read isPipedreamEnabled() went to settings.get('default') \u2014 a different collection, and a document nothing ever writes \u2014 so it always read null, and on null returned `undefined !== false` = TRUE. Both halves matter: the wrong store alone would make the switch unreliable, but the FAIL-OPEN default made it inert in exactly one direction. An admin could never turn Pipedream OFF \u2014 they got a 200, the API reported pipedreamEnabled:false, the UI toggle snapped back, and runPipedreamAction's `disabled` guard never fired once. A third-party egress integration that cannot be disabled is the failure mode worth a test. The default is now DENY, matching mergedSettings (what the API and UI report): a config read that fails must not enable an outbound path nobody asked for. Pinned: default-deny for a fresh org, both directions of the toggle, agreement between mergedSettings and getPipedreamStatus (the divergence was invisible from either side alone), a stale legacy settings['default'] row NOT re-enabling it, per-org isolation, and the action-path guard firing both when disabled and by default. Seven of the eight go red against the original code. Two pre-existing tests in tests/integrations/pipedream.test.ts were DISABLING the feature by writing settings['default'] \u2014 i.e. they passed by exercising the bug's own plumbing \u2014 and now use the real store. And the WS-E/WS-F automation block: step-declaration-routing.test.ts (13) \u2014 E-2/E-4. The router used to pick 'the newest live socket for this owner', so a step could not say which machine it needed, what that machine had to be able to do, or what should happen when the answer was none; a step written for the machine with the card reader would silently run on the laptop without one. The declaration makes placement expressible and the DEFAULTS make it safe \u2014 a step declaring nothing gets no capabilities, the cloud target, no credentials and offlinePolicy 'fail', so it asks for nothing and STOPS rather than meaning 'anything goes'. Placement is pinned in every direction: a newer socket does not win a capability it lacks, a PINNED target is that machine or nothing (no substitute, because the run named a specific computer for its reader/VPN/residential line), the target capability is required IN ADDITION to requiredCapabilities, an ungranted machine is never selected however loudly it advertises (I-3), and neither org nor owner boundaries are crossed on either target kind. No match returns undefined so the caller applies the offline policy \u2014 placement reports a fact about the fleet, the response to it belongs to the run. And typist-recipes-and-routing.test.ts (14) \u2014 F-3/F-4. A recipe may say WHERE the fields are and may never say what to do, because the typist is the only primitive handling a DECRYPTED credential against a LIVE page: anything steering it sits inside the credential's trust boundary. The loader refuses script-shaped selectors, refuses an UNRECOGNISED field rather than ignoring it (a recipe growing a `script` key must fail, not be silently dropped for a later version to read), and is ALL-OR-NOTHING because a registry that quietly drops entries leaves nobody able to say which sites are covered. F-4 splits `signature` from `login` so I8 holds: signing is legal authorship bound to the card in the reader, so it routes ATTENDED, while a login relays (forcing presence for a password pushes people toward sharing it) and an unclassified state claims the WEAKER route. The load-bearing half is that a credential-adjacent failure NEVER reaches the rehearsal fixer \u2014 handing an unfamiliar login form to an LLM means its output decides which field receives the password \u2014 and the guard is matched by error NAME and by message, because a persisted StepRecord keeps the message and not the class. And typist-non-memorable.test.ts (4) \u2014 F-5, pinned BEFORE the typist is wired, which is exactly when 'writes zero memory rows' is true only by accident of being unreachable. The assertion is blunt on purpose: `memories` is EMPTY, not 'contains no secret' \u2014 a row that omits the value still records that this user logs into this portal. Use tracking survives on the item (lastUsedAt/lastUsedBy, metadata only); a gap is asserted rather than assumed, since the typist emits no cofre_item_used Registo row though A-6 defines one. And session-checkout.test.ts (12) \u2014 G-4/G-5. A captured session is not a token usable anywhere: it was established from a particular vantage point, and replaying one captured on a lawyer's home connection from a datacenter IP is the exact pattern fraud systems exist to flag \u2014 same cookie, different continent \u2014 with a failure mode that is an account lock or a silent shadow-ban rather than a clean error. Health is decided BEFORE egress, because reporting 'no route' for something that needs re-authentication sends the operator to look at the network when the problem is the credential. The re-establishment route follows PROVENANCE (card ceremony -> attended, plain password -> typist) and UNKNOWN provenance is treated as attended: quietly re-running a typist against a portal that wanted a smartcard fails like a bad password, which is how accounts get locked. Another machine's residential line is never offered as a substitute, and the refusal carries what was REQUIRED so the run applies its own offline policy \u2014 a checkout helper that silently downgraded to datacenter would take that choice away. And daemon-seam-wiring.test.ts (8) \u2014 the plan's 'single most dangerous ordering mistake available', now taken deliberately. Wiring setDaemonConnectionResolver makes local_command and daemon browser steps reachable end to end; its gate was H-1, H-4, J-4 and J-7, all in. The property this suite exists for is that WIRING GRANTS NOTHING: an ungranted capability is refused BEFORE a frame is sent (a refusal after dispatch would already have asked a user's computer to run something), a grant is re-read PER INVOCATION so a mid-run revoke stops the next step rather than the next run, and another org's grant never authorises this one. Every invocation settles \u2014 refused, unreachable, timed out, or failed by a disconnect sweep \u2014 so a run never hangs on a machine, and a late result for a settled invocation is dropped rather than resolving twice. And key-custody-posture.test.ts (7) \u2014 A-8. data/kms.ts recorded an honesty requirement as a COMMENT ('security.md must not claim the KMS property while LocalKeyWrapper is the one in use') and a comment cannot fail a build, so it is a test now. The naive check \u2014 the wrapper id published in security.md equals currentKeyWrapper().keyId \u2014 is necessary but NOT sufficient alone, because these tests never run the composition root: a KMS wrapper installed in server.ts would leave currentKeyWrapper() reporting the module default here, and the doc could be wrong about production while the check passed. So the second assertion is that NOTHING in api/src or api/scripts calls setKeyWrapper, which is what makes the module default the thing every environment actually runs. Deliberately no file-level allowlist, not even for kms.ts: the matcher excludes the declaration by SHAPE, so exempting the declaring file would only blind the gate to a self-install in the one file where it is easiest to write. Both matchers are pure and self-tested against planted text, and all three drift directions were verified to go red before landing (doc claims kms while local is installed; a call planted in kms.ts; a call planted in server.ts, the realistic K-2 shape). K-2 wiring real KMS turns this suite red BY DESIGN \u2014 that is the forcing function for updating the published posture in the same change."
      },
      {
        "group": "artifact-backend runtime composition wiring (salomao S1)",
        "count": 1,
        "targetGate": "G12",
        "note": "S1 (salomao ERP migration, closes findings artifact-backend-runtime-never-wired): api/tests/apps/backend-runtime-wiring.test.ts - buildApp registers a non-Null WorkerThreadRuntime (the delivery pipeline's invokeArtifactBackend reaches a real runtime), disposeArtifactBackendRuntime restores the fail-closed Null runtime on teardown and factory re-composition, and the capability surface granted at the composition root matches the pinned set (appData CRUD, llm.classify/llm.complete via the llm/ public entry, notify.inApp, notify.email; NO integration seam). Hermetic (mongo-mem), LLM-free. Ratchet applies."
      },
      {
        "group": "trigger listener-kind inference at create (salomao S2)",
        "count": 2,
        "targetGate": "G12",
        "note": "S2 (salomao ERP migration): api/tests/events/trigger-create-inference.test.ts - createTrigger stamps kind:'listener' + pollConfig { actionName from platformListenerConfig, intervalMs: pollIntervalMs ?? 60000 } for microsoft-365 / google-workspace (explicit kind:'webhook' overridden - a platform provider has no webhook ingress, honouring it persists a dead row); non-platform stays kind-ABSENT (migration-free) / explicit-listener-with-caller-pollConfig unchanged; proves the created row matches the supervisor's find({kind:'listener'}). api/tests/contract/triggers-listener-create.test.ts - the exact Ligacoes-card body => 201 kind:'listener' + pollConfig validated with shared TriggerCreateResponse; additive pollIntervalMs override (Rule 7; min 1000, sub-floor => 400 ErrorEnvelope, nothing persisted); old-shape non-platform requests byte-compatible (201 webhook, secret returned once); list view carries entrypoint/kind/pollConfig (entrypoint absent-never-null on webhook rows). Both hermetic (mongo-mem), LLM-free. Ratchet applies."
      },
      {
        "group": "migration-grade import fidelity (salomao S3)",
        "count": 1,
        "targetGate": "G12",
        "note": "S3 (salomao ERP migration, closes findings import-ignores-the-bundle-slug): api/tests/apps/import-app-data-fidelity.test.ts - importArtifact honours a free well-formed bundle.slug (atomic reservation) and reports fellBack on a taken/invalid one; preserveId adopts bundle.id only on explicit opt-in, 409-refused on collision with side-effect rollback (slug reservation + project dir), 400 on absent/malformed id; importDumpReport per-collection fault isolation - reserved '__*' (the real prod dump's __files) and shared-scope 'usr.*' collections skipped BY NAME in the report, per-row failures counted with first error, oversized rows isolated not wholesale-fatal; engine importCreate preserves supplied valid createdAt/updatedAt VERBATIM while the public create keeps re-stamping (importCreate is not route-reachable); the import response's additive importReport validates against the shared schema (contract half extended in artifact-family.test.ts). Hermetic (mongo-mem), LLM-free. Ratchet applies."
      },
      {
        "group": "old-stack credential transcryption + state conversion (salomao S4)",
        "count": 1,
        "targetGate": "G12",
        "note": "S4 (salomao ERP migration): api/tests/migration/convert-dev-state.test.ts - the operator tool convert-dev-state.mjs decrypts old-scheme credential bundles (AES-256-GCM colon-joined iv:tag:ct, key utf8 truncate/pad-32; the OLD encrypt implemented IN-TEST with synthetic values only) and re-encrypts new v1 (round-trip pinned against the REAL api/src/data/crypto.ts decrypt); platform rows re-keyed _id 'platform-<orgId>-<provider>' + orgId (the exact shape getValidPlatformTokens requires), zoho-sign org-scoped for findConfigForOwner, enabled:true with reauth state cleared, dc preserved; zoho-agreements id->_id underscore-file conversion with optional --rewrite-app-id; adobe rows REFUSED loudly by design; secrets never echoed in output or errors. Also pins the import-tool fix: decrypt-sample reads credentialsCiphertext (the runtime field name) - a dev-shaped row (hyphen file / id key / old ciphertext) is loudly refused, never silently imported as zero rows (import-tool.test.ts extended). Hermetic, LLM-free. Ratchet applies."
      },
      {
        "group": "app-files blob migration tool (salomao S5)",
        "count": 1,
        "targetGate": "G12",
        "note": "S5 (salomao ERP migration): api/tests/migration/migrate-app-files.test.ts (9 tests) over the operator tool api/scripts/migrate/migrate-app-files.mjs - old-stack blob store (cortex '__files' app-data rows + files/{uuid} blobs) to new-stack per-blob {uuid}.json sidecars (api/src/apps/app-files.ts; a raw blob copy without sidecars serves 404). Drives the REAL CLI as a subprocess (real exit codes) over temp dirs with synthetic fixtures only. Happy path pins the sidecar BYTE-identical to what appFilesStore.save() writes (JSON.stringify of AppFileMeta in key order id,name,size,type,createdAt; createdAt preserved from the old row, updatedAt dropped; --src proven untouched); dump-JSON metadata source (collections.__files) with bare-array collections ignored + ambiguous-dump refusal; orphan-blob and orphan-row refusals write NOTHING (all-or-nothing plan-before-write) with --allow-orphans proceeding while reporting; idempotent re-run (0 copied / 2 unchanged, mtime-stable) and differing-target refusal unless --force; row/blob size-mismatch integrity refusal (Content-Length is served from meta.size) with --force writing the ACTUAL blob size; --dry-run touches nothing (not even the data root); EKOA_DATA_DIR default resolution (config.ts:342). Hermetic + LLM-free; green in the per-PR vitest lane from landing. Ratchet applies."
      },
      {
        "group": "P4 execution-locality suites (api)",
        "count": 7,
        "targetGate": "G12",
        "note": "The api-side suites the P4 locality slice landed, registered together because the per-file api lists in this ledger are the module_tests groups and nothing else censuses them (the count census covers playwright specs, node drivers and web unit files only). SEVEN NEW FILES (the P4 work also grew several pre-existing suites - automation/session-establishment.test.ts, automation/service.test.ts, automation/credential-gate.test.ts, schedules/supervisor.test.ts, security/session-checkout.test.ts, bridge/hello-advertisement.test.ts - which stay ledgered where they already were). All are hermetic, LLM-free, and green in the per-PR vitest lane from landing; ratchet applies from here. automation/locality.test.ts + automation/engine-locality.test.ts - the pure locality decision and its cross-product census, plus the engine's use of it end to end. automation/config.test.ts - the localBrowserEnabled kill switch pinned in both environments. automation/local-browser-context.test.ts - the provider BODY driven with a recording browser. security/locality-isolation.test.ts - the tenancy claims of egress selection, driven off the REAL egressCandidatesForOrg rather than hand-built rows. ROUND EIGHT adds security/egress-endpoint-authorisation.test.ts - a machine's self-asserted egress address is not an authorisation: the shape table, the attacker-proxy attack, the changed-endpoint reconnect, and what the registry will store at all. Verified by mutating the source before landing. And automation/composition-root-locality.test.ts - the two server.ts bindings that were SURVIVING MUTANTS (each deleted, whole suite green): the real buildApp is booted with the seams at their defaults, and each is then observed answering as the real implementation - the fleet resolver returning the `[]` an unbound `null` cannot state, and the browser provider rendering the resolved proxy onto newContext. No new file for the neutral-block cooldown: it lands as four cases inside the EXISTING schedules/supervisor.test.ts (already ledgered under the schedules group), and it rewrites one assertion there that pinned the unbounded behaviour - 'every one of them told the owner' asserted one notification per fire, which was the defect."
      },
      {
        "group": "action evidence (S1)",
        "count": 7,
        "targetGate": "G8",
        "note": "2026-08-20 (slice S1, branch feat/s1-s3-integration-surface): the ACTION EVIDENCE model - one live row per (org, integration, action) in the new tenant-scoped `integration_action_evidence`, superseded wholesale by each validated run. THREE FILES. (1) tests/integrations/action-evidence.test.ts - the STORE's own behaviour: the supersede is structural (the deterministic `_id` is the tuple and nothing else, asserted by counting documents), two actions of one integration keep separate rows, an empty key term is refused rather than writing an un-scoped row, the excerpt/step caps are real and truncation is RECORDED (with a fits-comfortably control so the flag means something), the last gate refuses a live credential value anywhere INCLUDING a step excerpt and refuses by NOT WRITING, the retention pins name automation runs only, and a superseding run RELEASES the previous pin. It also pins GRADUATION at the pure-function level. (2) tests/integrations/action-evidence-capture.test.ts - the CAPTURE, entered at the real `executeUserIntegrationAction` with only the transport faked, so it proves production reaches the store rather than that the store works: a 2xx records the request summary plus a capped response sample stamped with the action SHAPE, the credential never survives from header or query, an upstream that echoes the caller's own key is redacted before the gate rather than refused by it, a 4xx/5xx/transport error records NOTHING and does not replace the last good sample, and a throwing store never turns a succeeded call into a failed one. (3) tests/security/action-evidence-isolation.test.ts - the Rule 5 suite of the memvault-isolation class. MUTATION-VERIFIED, measured rather than asserted, and where a filter is NOT individually observable this ledger says so: dropping `getEvidence`'s stored-orgId re-check reddens 1; dropping the empty-org guard reddens 1; dropping the orgId term of the deterministic `_id` reddens 6; disabling the last gate reddens 2; the two `listForIntegration` orgId terms (query term and post-filter) are EQUIVALENT MUTANTS - each alone survives because the other enforces the same predicate on the same field, and only removing BOTH reddens 3, which the store docblock records. Capture path: removing the api-call capture call reddens 8; dropping the response-body redaction reddens 1; dropping the shape stamp reddens 1; making the FAILURE branch also record reddens 2. Composition root: each of `recordActionEvidence` and `collectRunEvidence` deleted from server.ts's one `executorDeps` bundle reddens tests/automation/composition-root-action-seam.test.ts, as does reverting the bundle to the pre-S1 automation-only deps - the binding is proved, not assumed. Sweeper: disabling the pin guard in screenshot-plane.ts reddens the new retention case (the pinned and unpinned runs share the SAME long-ago mtime, so only the pin can produce the result, and the unpinned run is still asserted removed). Graduation: deleting the evidence gate from `promoteToTrusted` reddens 5 across the guardrail and achieve suites. Hermetic, LLM-free, enforced by npm test rather than the census runner. ROUND TWO (2026-08-20, the S1 verification pass: one blocker, three majors, three minors) adds THREE FILES, so `count` moves 3 -> 6, and CORRECTS two claims above. Every count below is MEASURED - the mutation applied to the source, the named suites re-run, the source restored and checksum-verified against its pre-mutation md5. (4) tests/integrations/action-evidence-removal.test.ts - THE BLOCKER. The collection shipped with NO removal path: `discardEvidence` had zero production callers while its docblock named two it did not have ('Reached when the action itself is gone (a definition write that dropped it), and by the erasure path'). Reproduced end to end - a definition with [doomed, survivor], evidence for `doomed` carrying client PII (Processo 1234/24.5T8LSB - Cliente: Maria Silva), then the ordinary builder save with [survivor] only, which is the SAME call site that already collects the sibling collection's evidence via discardEvidenceOfRemovedRecipes(recipesDroppedBy(...)). The action went, the row and its PII stayed, and pinnedRunIdsForRetention() still named the run - so its screenshots of an authenticated client-portal session were exempt from the 7-day sweep PERMANENTLY, since the pin releases only on supersede or discard and neither can happen again for an action nobody can run. S1 as first landed converted a bounded retention into an unbounded one. The removal paths are now ENUMERATED FROM THE CODE in action-evidence-store.ts's header (grep the writers of the definition document: IntegrationDefinitionStore.create and IntegrationRecipeStore, and the recipe store only map()s the existing actions array so it cannot drop one) - exactly ONE path, no definition-delete path at all, and the not-paths named so nobody re-derives them. The suite enters at the REAL saveAuthoredDefinition and the REAL create(..., 'replace'), NEVER at the collector, and counts DOCUMENTS. MUTATION-VERIFIED: deleting the collector call reddens 6; making actionsDroppedBy filter on `recipe !== undefined` (the plausible reuse of recipesDroppedBy, which would skip the commonest evidence-bearing action there is - a plain api-call) reddens 6; dropping the orgId term from discardEvidenceForAction reddens 4 across this file and (1). (5) tests/automation/action-evidence.test.ts - the module that shipped with NO SUITE. 123 lines, no test file, not in this entry, three surviving mutants measured. The one that matters made `excerptOf` return JSON.stringify(step.error.details) - precisely the exclusion the module's own docblock names ('NOTE WHAT IS NOT READ: StepRecord.error.details (an arbitrary debug payload with no redaction contract)'), and the one field no redaction leg governs (the executor's redaction covers the request summary and the response body; the store's last gate only fires against the values the run RESOLVED). The exclusions are asserted BY CONSEQUENCE - a marker planted only in error.details / logTail / visionReasoning must not appear anywhere in the collected evidence, with a control proving the excerpt that IS read still arrives, so 'absent' cannot be satisfied by a collector that stopped reading anything. MUTATION-VERIFIED: reading error.details reddens 2; reading logTail reddens 1; slice(-MAX_STEPS) instead of slice(0, MAX_STEPS) reddens 1. (6) tests/automation/composition-root-screenshot-pins.test.ts - THE SURVIVING MUTANT this repo already had a test class for. Dropping `pinnedRunIds` from the sweepExpiredScreenshots call in bootState left 46/46 green: both halves were pinned in isolation and nothing joined them, because bootState was entered by NO test. Closed twice over. STRUCTURALLY: sweepExpiredScreenshots' `pinnedRunIds` is now a REQUIRED option, so dropping or renaming it stops compiling (the same make-it-not-compile move promoteToTrusted's required `evidence` argument makes). BY TEST: this file enters at the REAL bootState against the real collection and a real screenshot tree, both run dirs sharing ONE long-ago mtime so only the pin can produce the result, with a no-evidence control proving the sweep is alive. server.ts now exports sweepScreenshotsSparingPinnedEvidence (the one production composition) and bootState AWAITS it - it was fire-and-forget, which is why no caller could wait on it. MUTATION-VERIFIED: passing new Set() instead of the real pins reddens 3; removing the pin read's .catch degrade (so a mongo blip becomes an UNPINNED sweep) reddens 1. THE TWO CORRECTIONS TO THE ROUND-ONE TEXT ABOVE. FIRST, THE KEY IS NOT (org, integration, action). It is (org, OWNER, integration, action). findConfigForOwner resolves a credential per (orgId, ownerUserId) and action-consent.ts's idFor keys an approval on (orgId, scope.userId, ...), so within ONE org an org-visible definition run by two people is two third-party accounts and two people's client data. Reproduced: the peer's run wrote the SAME _id and destroyed the owner's sample, and trustAuthoredAction - which reads this collection - let user A promote an action to `trusted` (and so auto-runnable by achieve) on user B's run against B's OWN account. The module argued only that a sample must not cross ORGS; its claim that 'a pointer inherits the rule that already exists' holds for the PNG behind the screenshot plane's org+owner check and NOT for the excerpt copied into the org-keyed row beside it. The isolation suite gains a whole per-owner half plus the migrated-pre-owner-row case (a row carrying no ownerUserId at all is served to NOBODY). RE-MEASURED: dropping ownerUserId from the deterministic _id reddens 12 across (1)+(3)+(4); dropping getEvidence's owner re-check reddens 2 and its org re-check 1; the two listForIntegration owner terms are EQUIVALENT MUTANTS (each alone survives, both together redden 4); making trustAuthoredAction read the definition AUTHOR's row instead of the acting user's reddens 2 in tests/integrations/integration-achieve.test.ts, which gains the org-admin-promotes-on-somebody-else's-run case and its satisfiable control. SECOND, 'dropping the empty-org guard reddens 1' was true but was hiding an unfailable case. That case ran five assertions of which only ONE could fail: with no row stored under the empty org, the point read, the list read and the discard all answered empty whether or not isTenantScoped existed. It now PLANTS rows under the empty org and the empty owner and is split so each leg fails on its own filter. RE-MEASURED: dropping the guard from getEvidence reddens 1, from listForIntegration reddens 1 - and discardEvidence's own guard is RECORDED AS MASKED rather than claimed: it calls getEvidence, whose guard fires first, so it SURVIVES alone and only the pair reddens the discard leg (2). The suite header, the store docblock and this entry all say so. TWO MINORS closed inside existing files, so no file count changes for them: RunStepEvidence.title held StepRecord.status (a field StepRecord does not have, so every step of the S2 detail page would have been titled with its status, and nothing asserted the field at all) and is now `status` - renaming it back reddens 3 across (5) and composition-root-action-seam; and capEvidence discarded capText(...).truncated for the api-call REQUEST body while ApiCallEvidence.request had no truncated field, so an oversized request body was cut silently AND cut where the executor's '... [truncated, N more bytes]' marker is, slicing off the one thing that said so - dropping the flag again reddens 1. The same round fixed FIXTURE HONESTY in composition-root-action-seam: its run record used status 'succeeded' and tier 'deterministic', neither of which is a member of RunStatus/StepStatus/StepTier, hidden by an `as never` cast. ROUND THREE (2026-08-20, the S1 verification pass repeated: one major, four minors) adds NO FILE - `count` stays 6 - and CORRECTS the round-two text above, because round two was right about the METHOD and wrong about the UNIT. Every count below is MEASURED: the mutation applied to the source, the named suites re-run, the source restored and checksum-verified against its pre-mutation md5. THE MAJOR. The round-two collector ran discardEvidenceOfRemovedActions({orgId: input.orgId, ...}) - the org that WROTE the definition - while every evidence row is keyed by the org that RAN the action, which the `global` tier makes a different org. Reproduced as a failing spec before any code changed: a super-admin publishes org A's definition at visibility 'global'; getForActor grants the cross-org tier, so a user in org B resolves it, connects THEIR OWN credential and runs a browser-steps action; the row lands under {orgId: orgB, ownerUserId: uB} holding org B's client PII plus a pin naming run-consumer; org A re-authors without the action and the deleteMany filtered on org A matches nothing. Measured after the replace: 2 rows left, both org B's, PII intact, pinnedRunIdsForRetention() still returning run-consumer - the permanent sweep exemption the round-two BLOCKER was raised to end, surviving round two untouched for every consumer of a global definition. A SECOND trigger used the transition the round-two header dismissed BY NAME as not-a-removal-path: setVisibility global->org strips every action from every consumer org at once, and org->private does the same to every peer inside the author's own org, with no super-admin in the story. FIXED by replacing the action-set diff with a RECONCILIATION keyed on the ROW: discardEvidenceOfUnresolvableActions asks, per (orgId, ownerUserId), which action names that owner still resolves - through getForActor, the same resolver the executor uses - and drops the rest; definition-store calls it from BOTH writes that narrow reach (the replace branch and setVisibility); actionsDroppedBy is deleted. (4) tests/integrations/action-evidence-removal.test.ts is rewritten around it - 21 cases, entered at the REAL saveAuthoredDefinition, create(..., 'replace'), setVisibility and deleteConfig, never at the collector, the one exception being a final describe that enters the collector directly to pin its FAILURE POSTURE and says so. MUTATION-VERIFIED: restoring the per-writing-org scope reddens 2 (both cross-org cases, while EVERY same-org case stays green, which is exactly the shape of the shipped bug); deleting the setVisibility call reddens 2; dropping the resolution check so the collector deletes blindly by (key, action) reddens 8, including the control that a tenant resolving the same action name through its OWN definition keeps its row and the control that a WIDENING private->org transition collects nothing; resolving with userId:'' instead of the row's owner reddens 1 (the org->private peer case, which is what proves the per-OWNER unit inside one org); inverting the fail-posture so a failed resolution deletes reddens 1. THE FOUR MINORS, all closed inside existing files. (a) discardEvidence STILL had zero production callers, and the round-two header said the erasure gap 'is recorded as a gap in docs/findings.md' when the only erasure entry there was about the screenshot TREE. Wired instead of re-recorded: service.ts deleteConfig calls discardEvidenceOfDisconnectedConfig, whose scope is a DISCRIMINATED owner ({userId} | 'every-owner-in-org') because a legacy org-shared config carries no custodian and is the credential findConfigForOwner hands every member; discardEvidence itself is now the reconciler's removal primitive. MEASURED: deleting the call reddens 2; always taking the org-wide arm reddens 1. (b) pinnedRunIdsForRetention did find({}) with no filter and no projection over rows of hundreds of KB that grow as orgs x owners x integrations x actions with no TTL - a multi-gigabyte materialisation AT BOOT whose OOM abort is not a rejection, so the caller's .catch degraded nothing and the real failure mode was a boot crash loop, not the documented 'degrades to pin nothing'. Store.find gains an ADDITIVE third `projection` option and the query gains {'evidence.kind':'automation'}. (1) tests/integrations/action-evidence.test.ts gains a case asserting BY CONSEQUENCE over what the driver handed back: dropping the projection reddens 1 (a planted sample comes back in the returned documents), dropping the query term reddens 1 (two documents instead of one). RECORDED RATHER THAN CLAIMED: the loop's own ev.kind check is not the masked half of a pair - deleting it does not COMPILE (it is the type discrimination), and replacing it with a cast SURVIVES, because an api-call row carries no runId at all. (c) FIXTURE HONESTY, the same class as round two's: the removal suite's automation fixture used status 'succeeded', which is not a member of RunStatus (the production writer collectRunEvidence copies RunRecord.status straight through; the member is 'completed'). Typed string?, asserted by nothing - not unfailable, but a stand-in waiting to be copied into a case that DOES assert run status. Now 'completed', with the production writer named in the fixture comment. (d) UNFAILABLE, closed: (6) tests/automation/composition-root-screenshot-pins.test.ts could not distinguish `await sweep(...)` from `void sweep(...)` - the mutant left 5/5 green because bootState awaits slower things afterwards, so the sweep won the race either way. The round-two report, the server.ts docblock and the decisions entry all presented the await as what makes the sweep observable; what actually makes it observable is the REQUIRED pinnedRunIds option plus that suite. Pinned the only way it can be, structurally: bootState now READS the returned counts (the retention log line moved from the composition function to the call site), so `await -> void` fails tsc with 5 errors (MEASURED). The suite header now states what it does NOT pin. ALSO: (3) tests/security/action-evidence-isolation.test.ts gains the Rule 5 coverage for the module's SECOND deliberately cross-tenant reader, listOwnerRefsForKey - it must hand back org + owner + action name and no tenant's sample, bounded by the projection rather than by a promise; dropping its integrationKey term reddens 1 in (1). ROUND FOUR (2026-08-20, the S1 verification pass repeated a third time: one blocker, one major, five minors) adds ONE FILE, so `count` moves 6 -> 7, and RETIRES the round-three mechanism above rather than tuning it - round two was too NARROW and orphaned a consumer's evidence, round three widened it and DELETED ACROSS A TENANT BOUNDARY, and three rounds is evidence about the SHAPE. Every count below is MEASURED - the mutation applied to the source, the named suites re-run, the source restored and md5-verified against its pre-mutation checksum. THE BLOCKER, on two independent axes, both reproduced end to end before anything changed. (a) LIVE ROW vs FROZEN SNAPSHOT: a consumer of a published definition resolves publishedSnapshot through crossOrgView -> publishedViewOf, which the replace branch carries forward deliberately and setVisibility re-promotes without re-scrubbing, while the reconciler asked getForActor for the LIVE row - so org A un-publishing, dropping an action and re-promoting deleted org B's only copy of its own client PII sample and its screenshot pin for an action org B COULD STILL RUN (asserted by RUNNING it through the real executor after the write, never by asking a resolver). (b) RUNNER vs CUSTODIAN: an org-shared credential resolves the definition as the CUSTODIAN and 'never as the reader' (definitionActorForCredential), while the evidence key is stamped with the RUNNER - who cannot see the custodian's private row, so the answer was the empty set and every peer's evidence went on a re-save that DROPPED NOTHING. FIXED BY CHANGING THE SHAPE, under one standing rule: A WRITE BY ONE ORG NEVER DELETES ANOTHER ORG'S DATA. The cross-tenant listing listOwnerRefsForKey is DELETED rather than narrowed (a cross-tenant listing is what made a cross-tenant delete expressible), and discardEvidenceOfUnresolvableActions plus definition-store's resolvableActionNames go with it. In their place: the READER collects its own on the executor's unknown_integration / unknown_action refusals (discardOwnerEvidence, whose scope type REQUIRES both tenancy terms and has no org-wide arm); the WRITE collects inside its own tenant through a seam taking (orgId, integrationKey), implemented by NEW api/src/integrations/evidence-reconcile.ts and bound by bindDefinitionEvidenceReconciler() in buildApp; and everything else fails towards RETAINING, bounded by NEW sweepExpiredEvidence at boot (EVIDENCE_RETENTION_DAYS = 90, run BEFORE the pin read so an expiring row releases its screenshot pin on the SAME boot), the owner's NEW erasure control DELETE /api/v1/integrations/:key/actions/:actionName/evidence, and deleteConfig's credential erasure. (7) tests/integrations/action-resolution.test.ts - THE NEW FILE, for NEW api/src/integrations/action-resolution.ts: the ONE resolution (findConfigForOwner -> definitionActorForCredential -> resolveDefinition), extracted from the executor and CALLED BY IT, so a retention decision cannot believe something a run does not. 8 cases pinning the three axes a re-derivation got wrong, each as a CONTRAST inside the same case (the consumer resolves the dropped action while getForActor's live answer does not; the peer resolves the custodian's row while their own view is null; the empty set is asserted distinct from null). MUTATION-VERIFIED: resolving as the reader instead of the custodian reddens 4 (2 here + 2 in the removal suite); collapsing the three-way answer (null -> the empty set) reddens 3. (4) tests/integrations/action-evidence-removal.test.ts is REWRITTEN around the rule - 33 cases, entered at the REAL saveAuthoredDefinition, create(..., 'replace'), setVisibility, publishDefinition, executeUserIntegrationAction and deleteConfig, never at a collector except one labelled describe pinning the failure posture and the cap. MUTATION-VERIFIED, and the cross-tenant one is measured as the THREE-PART RESTORATION of round three that it actually is: widening listOwnerRefsInOrg + deleting the reconciler's ref.orgId guard + keying the discard from ref.orgId instead of from the required org parameter reddens 6, including the frozen-snapshot case, the demotion case and the re-publish case. THE PARTS ARE RECORDED SEPARATELY BECAUSE THEY ARE NOT INTERCHANGEABLE: widening the listing alone reddens 2 (its own cases in (1) and (3)) and dropping the ref guard alone reddens 1 (its own case), and NEITHER produces a cross-tenant deletion on its own - the discard is keyed from the REQUIRED org parameter, which is the structural guarantee, and the guard is defence in depth over the listing. Inside listOwnerRefsInOrg the two org terms (query term + post-filter) ARE an equivalent-mutant pair: each alone leaves 117/117 green (measured) and only removing BOTH reddens 2, which the method's docblock records. The rest, all measured: deleting the reader's own collection from the unknown_action branch reddens 2 and from the unknown_integration branch reddens 2; dropping ownerUserId from discardOwnerEvidence's filter reddens 6 and dropping orgId reddens 2; deleting reconcileEvidenceOfOrg from create's replace branch reddens 8 and from setVisibility reddens 1; restoring deleteConfig's 'every-owner-in-org' arm reddens 1; disabling sweepExpiredEvidence reddens 3; removing the MAX_RECONCILED_OWNERS break reddens 1. AND THE FIXTURE HONESTY THAT LET THE BLOCKER HIDE: three global-tier cases were UNFAILABLE because the fixture built its global row with definitions.create({visibility:'global'}) instead of the production requestPublish -> publishDefinition, so NO SNAPSHOT existed and publishedViewOf silently fell back to live content - i.e. the fixture described a world in which live and published can never disagree, which is the only thing that mattered. Both this file and (7) now publish through the real flow and prove reachability by RUNNING the action after the write. A FOURTH unfailable assertion was found and fixed while measuring: the frozen-snapshot case originally asserted retention AFTER the re-run that proves reachability, and a successful run rewrites the row at the same deterministic id - so a collector that had just destroyed it looked identical. The retention assertion now precedes the re-run, and that reorder is what moved the three-part mutant from 5 to 6. THE FIVE MINORS, closed inside existing files. (i) deleteConfig's 'every-owner-in-org' arm erased peers whose OWN credential was never the deleted row (findConfigForOwner answers rows.find(c => c.ownerUserId === owner) BEFORE the shared fallback) - now {everyOwnerExcept: [...]} computed from the configs still present after the delete, with an empty-exclusion control in (1) so 'spares the peer' is the exclusion doing it and not an arm that quietly stopped deleting. (ii) publishSnapshot on a RE-PUBLISH narrows every consumer at once and was dismissed as 'widening only'; it is named as a narrowing write everywhere and now meets the same own-org rule. (iii) api/src/data/stores.ts still documented the key as (orgId, integrationKey, actionName) - the SIXTH copy of a claim the round-three commit says it corrected in five places, and the one a future author reads first. (iv) resolvableActionNames' role:'user' was an EQUIVALENT MUTANT (76/76 green as super-admin) under a docblock justifying it as the least-privileged reading - the method is deleted with the collector it served. (v) the round-three reconcile was an uncapped cross-collection scan plus one sequential DB read PER OWNER awaited inside every definition save - the listing is now org-scoped and MAX_RECONCILED_OWNERS (25) stops the fan-out. COMPOSITION ROOT: tests/automation/composition-root-action-seam.test.ts gains two cases - deleting discardOwnActionEvidence from the executorDeps bundle reddens 1, and deleting bindDefinitionEvidenceReconciler() from buildApp reddens 1. RULE 7: the new endpoint is additive (integrations.discardActionEvidence, auth 'user', so the key-reachable OpenAPI surface and the generated cortex-cli are unchanged - both regenerated in the same commit and clean), covered over the wire by three cases in tests/contract/integrations-achieve.test.ts, with its COVERED entry leaving EXPECTED_PENDING_COUNT unmoved. MEASURED: removing the route reddens 2, and pointing its key at a different owner reddens 2. RULE 5: tests/security/action-evidence-isolation.test.ts drops the cross-tenant-listing describe for an org-scoped one and gains the reader-collection tenancy case (right owner + wrong org, right org + wrong owner, each reaching nothing, with the matching pair as the control). tests/automation/composition-root-screenshot-pins.test.ts gains the ordering case - an EXPIRED evidence row must release its pin on the SAME boot, which is the only thing that distinguishes sweeping evidence before the pin read from sweeping it after - plus the retention sweep's own degrade-on-throw case. ROUND FIVE (2026-08-20, the S1 verification pass repeated a fourth time: one blocker, two majors, one minor closed by deletion) adds and removes NO FILE - `count` stays 7 - and RETIRES the round-four MECHANISM above rather than tuning it - as round four retired round three's and round three retired round two's. Every count below is MEASURED: the mutation applied to the source, the named suites re-run, the source restored and md5-verified against its pre-mutation checksum. THE FINDING IS THE SEQUENCE, NOT ANY ONE ROUND. Four collectors, five defects, one cause: every attempt answered 'is this action gone?' SYNCHRONOUSLY - at one instant, from one vantage - and deleted a row whose lifetime is DURABLE. round two orphaned every consumer of a global row (too narrow); round three deleted across a tenant boundary (too wide); round four's reader-side collector deleted on TRANSIENT unreachability (a mongo blip, a rotation, a half-applied definition write and a package restored a second later are the identical branch); round four's write-time seam deleted every peer's sample on an org->private flip that was REVERTED a minute later; and server.ts's boot sweep did pinnedRunIdsForRetention().catch(() => new Set()) and SWEPT ANYWAY, deleting every screenshot behind a LIVE, unexpired evidence row across every tenant at once (the tree is <root>/<automationId>/<runId> and carries no org) on one failed read - which the same file's docblock called 'the one failure mode that destroys data' while the suite case below it asserted {removed:2, pinned:0} as the expected result. ROUND FOUR'S VANTAGE WAS THE BEST IN THE SYSTEM AND ITS SCOPE REQUIRED BOTH TENANCY TERMS; that is the lesson, not a mitigating detail. FIXED BY SUBTRACTION. DELETED: api/src/integrations/evidence-reconcile.ts (whole module, with MAX_RECONCILED_OWNERS and bindDefinitionEvidenceReconciler); definition-store.ts's DefinitionEvidenceReconciler seam, its setter, its __reset*ForTests and its three call sites (create(...,'replace'), setVisibility, publishSnapshot); action-executor.ts's discardOwnActionEvidence seam and its two refusal-branch call sites; the store methods discardOwnerEvidence and listOwnerRefsInOrg with their ActionEvidenceOwnerRef / OwnerEvidenceScope types; resolvableActionNamesForOwner; and the two server.ts bindings. INVERTED: a failed pin read now SKIPS the screenshot sweep for that boot (the pin set is a precondition, not an embellishment - one boot of retained PNGs beats an unrecoverable deletion). WHAT ENDS A ROW IS NOW THREE DURABLE SIGNALS AND NOTHING ELSE: TIME (sweepExpiredEvidence at boot, EVIDENCE_RETENTION_DAYS=90 - the collector now, not a backstop), THE OWNER (DELETE /api/v1/integrations/:key/actions/:actionName/evidence, plus deleteConfig's credential erasure - the same durable signal one step out, kept deliberately and NOT a reachability guess), and A NEWER SAMPLE (recordEvidence's structural supersede). WHAT IS DELETED IS SOURCE, NOT SUITE: evidence-reconcile.ts had its own describe inside (4) rather than a file of its own, so retiring the module removes cases and not a suite. All seven files stay, five of them changed. (4) tests/integrations/action-evidence-removal.test.ts is REWRITTEN around the new rule - 18 cases, every one entered at a REAL production caller (saveAuthoredDefinition, create(...,'replace'), setVisibility, publishDefinition, executeUserIntegrationAction, deleteConfig) because there is no collector left to enter, each 'the row survived' case paired with a control that would also fail if the mechanism had merely stopped working (a re-run that succeeds, a supersede that lands, a sweep that removes). Its two headline cases are REPRODUCTIONS of the round-four defects: 'org -> private -> org: the peer's sample is still there, and they can run again' (byte-identical row compared across the flip, with the refusal in the middle asserted so the flip is not a no-op) and 'TRANSIENT unreachability: the integration blips away, refuses, comes back, and the sample survives'. MUTATION-VERIFIED, each mutant a faithful restoration of the collector it replaced: restoring the write-time collector at create's replace branch reddens 2, and its round-three cross-tenant variant (the orgId term dropped) reddens 4 including the frozen-snapshot and re-publish cases; restoring the tier-flip collector on the private transition reddens 1; restoring the reader-side collection on both refusal branches reddens 3. (6) tests/automation/composition-root-screenshot-pins.test.ts has its pin-failure case INVERTED - it asserted the destructive behaviour in as many words - and now asserts ON DISK that the run behind the live evidence row survives, with a next-boot-collects control so 'skip' is a deferral and not a leak; restoring .catch(() => new Set()) reddens 2. (7) tests/integrations/action-resolution.test.ts keeps axes 1 and 2 (live row vs frozen snapshot, runner vs custodian) read off resolveOwnerActionSurface directly, and REPLACES axis 3. THE EQUIVALENT MUTANT IS RECORDED AS DELETED RATHER THAN PINNED: resolvableActionNamesForOwner's `if (!surface) return null` was an equivalent mutant - substituting new Set(), which deletes every row of that owner, left the suite green (240/240 as reported by the reviewer who found it; NOT re-measured here, and confirmed from the code instead - the arm was reachable only with three non-empty terms AND an incoherent custodian, so no case constructed it) including the axis-3 describe that exists precisely to pin 'could not find out' against 'resolves nothing' - and the function is gone with both of its callers, so the arm is not pinned. What survives is resolveOwnerActionSurface's own null one tier down, now pinned WHERE IT ACTS: at the executor, as a credential_invalid refusal handed to the caller. MEASURED: `?? reader` in place of that arm reddens 2. tests/automation/composition-root-action-seam.test.ts trades its two round-four binding cases for ONE that asserts the composition binds NO collector, through the real buildApp-composed executor and the real definition store, with one row per trigger so neither mutant has nothing to delete; restoring either collector reddens 1. (3) tests/security/action-evidence-isolation.test.ts drops the two describes for the deleted methods and gains the Rule 5 describe for discardEvidenceForDisconnectedConfig, now the WIDEST delete in the module: both arms, both tenancy terms, and an empty-term case made FAILABLE by planting rows under exactly the empty keys it names (asserting `orgId: ''` erases nothing against ordinary rows is unfailable - the query matches nothing whether or not the guard exists, and the guard-deletion mutant survived it, measured). MEASURED: dropping the orgId term reddens 2; removing the empty-term guards reddens 1 against the planted rows and 0 without them. (1) tests/integrations/action-evidence.test.ts drops the listOwnerRefsInOrg and discardOwnerEvidence describes with the methods. RULE 7: nothing additive and nothing breaking on the wire - no schema, descriptor, route or auth class moves, so OpenAPI and the generated cortex-cli are byte-identical and EXPECTED_PENDING_COUNT is unmoved. Docs: architecture.md's action-evidence section is rewritten around the three signals; decisions.md carries the trade explicitly; findings.md opens evidence-orphan-window-until-ttl, WIDENED from the round-four entry and stated as the accepted cost rather than implied to be closed. ROUND SIX pins the NUMBERS the round-five trade rests on, none of which any case could tell from any other value. EVIDENCE_RETENTION_DAYS was unpinned in the DELETING direction: 90 -> 1 left all 13 S1 suites green (246/246) while the widening direction reddened 4, because the one surviving-row case stamped its row ONE DAY before the sweep and so pinned the window to >= 1 day. tests/integrations/action-evidence.test.ts now restates 90 as a LITERAL and stamps two rows HALF A DAY either side of the cutoff - whole-day offsets let a 90 -> 89 mutant survive on the strict $lt - so any integer change reddens it. MEASURED: 90 -> 89, 90 -> 91 and 90 -> 1 each redden exactly that case. MAX_EVIDENCE_STEPS and MAX_EVIDENCE_EXCERPT_CHARS were unpinned the same way in BOTH suites that assert them (every case built CONST + N and asserted toHaveLength(CONST), true of every value); both are now literals in tests/integrations/action-evidence.test.ts and tests/security/action-evidence-isolation.test.ts, following the discipline tests/automation/action-evidence.test.ts already states for the collector's mirror of the same caps. MEASURED: 50 -> 7 reddens 2, 8_000 -> 111 reddens 3, and the step case's new first-steps-by-index assertions kill a slice(-MAX_EVIDENCE_STEPS) mutant (reddens 1). THE RULE THIS LEAVES: a test that imports the constant it is checking pins that constant's NAME, never its VALUE. No new files and no new tenancy legs - the isolation suite is edited only in its bounds describe. Docs: decisions.md carries the round-six entry; findings.md records the retention window and the two caps as FIXED and notes on the still-OPEN evidence-orphan-window-until-ttl entry that its central bound is now enforced. ROUND SEVEN closes the claim those numbers were attached to. The STEP cap cut every trace SILENTLY and the flag the contract promised could not fire on the production path: collectRunEvidence slices to MAX_STEPS BEFORE the executor seam and returned no truncation field at all, so capEvidence's `evidence.steps.length > MAX_EVIDENCE_STEPS` compared 50 > 50 on every real row - measured end to end through the real collector, store and Mongo, a 200-step run stored steps.length = 50 and truncated = undefined. The 62-step case that looked like coverage hands recordEvidence a shape the production writer structurally cannot produce, and is now labelled as the module's OWN ceiling rather than the production path. The signal is carried with the slice (CollectedRunEvidence.truncated -> RunEvidenceCollector -> AutomationEvidence) and pinned END TO END in tests/automation/composition-root-action-seam.test.ts: a 200-step RunRecord through the real chain plus a 50-step control. MEASURED: forcing the collector flag false reddens 3 across 2 suites, > -> >= reddens 2, dropping the executor forward reddens ONLY the end-to-end case, dropping `|| evidence.truncated` reddens 2. DEFAULT_SCREENSHOT_RETENTION_DAYS was the exact mirror of round six's finding in the sibling constant: both suites passed retentionDays: 7 explicitly while the production caller rides the default, so 7 -> 1 and 7 -> 36500 both left 247/247 green. tests/security/screenshot-plane.test.ts now restates 7 as a literal and straddles the cutoff by half a day WITHOUT passing retentionDays, and sweepExpiredScreenshots gains the non-positive guard its sibling sweepExpiredEvidence always had (0/negative/NaN put the cutoff at or after now and deleted every unpinned run directory). MEASURED: 7 -> 1 and 7 -> 36500 each redden 1; removing the guard reddens 1. tests/integrations/action-evidence-removal.test.ts adds the one claim discardEvidenceForDisconnectedConfig's $nin note makes and nothing pinned - a MIGRATED row carrying no ownerUserId goes when the shared credential that produced it is disconnected, through the real deleteConfig, with a peer holding their own credential and another tenant as controls. MEASURED: adding $exists: true to the $nin arm reddens exactly that case. RULE 7: nothing moves on the wire - no schema, descriptor, route or auth class changes, so OpenAPI and the generated cortex-cli are byte-identical and EXPECTED_PENDING_COUNT is unmoved. FIXED-12: the stored shape of an automation evidence row changes additively, so 05-data-model and 02-module-map each carry an append-only AS-BUILT (f) note. ROUND EIGHT closes a property that was pinned three ways on one rail and ZERO ways on the other. Deleting `if (!automationResult.success) return null;` from action-executor.ts's evidence closure left the whole estate green - 14 files, 258/258, measured twice - because the only suite binding the real evidence seams (tests/automation/composition-root-action-seam.test.ts) points at `auto-never-runs`, an automation that does not exist: that refusal is `unknown_automation`, carries no `data`, runIdOf answers undefined, and the mutant is a no-op there. In production the failed leg carries a REAL run id, so the failed trace supersedes the last successful sample at the same _id and pins that run's screenshots. tests/integrations/action-evidence-capture.test.ts now carries the automation rail: a REAL automation owned by the caller driven to automation_failed through the PRODUCTION seam mapping (automationBackedActionHandler) with the real collector and the real store bound, the engine stood in for at its own ActionRunDeps.run seam and writing its run record through the PRODUCTION writer (automationRunStore.create then update - the pair runOrRehearse makes), with a successful control, a first-run-fails case, and DEEP EQUALITY of the whole row before and after so validatedAt and the screenshot pin are covered rather than just the run id. AND THE GRADUATION GATE WAS RESTING ON THAT LINE: promoteToTrusted/ValidatedRunEvidence read PRESENCE plus `shape` and carried no success signal, so ActionEvidenceDoc gains `outcome` ('succeeded'|'failed') DERIVED in the store from the stored sample - 2xx window for api-call, RunStatus's one success member `completed` for automation, `failed` for an absent status - and promoteToTrusted refuses anything but 'succeeded', absent included. Derived rather than carried on purpose: a term the executor passed in would restate the write site's own belief. Pinned at three levels: derivation (the store suite's new `the outcome term is read off the sample` describe, with the 2xx window straddled at BOTH edges), gate (tests/integrations/action-evidence.test.ts and tests/security/authored-action-guardrails.test.ts), and end to end through trustAuthoredAction (tests/integrations/integration-achieve.test.ts). MEASURED: deleting the executor guard reddens 2; deleting the promotion's outcome check reddens 4 across 3 suites; outcome: outcomeOf(evidence) -> outcome: 'succeeded' reddens 4 including the end-to-end case; 200 -> 199 and 300 -> 301 each redden the edge case; the automation branch forced to 'succeeded' reddens 1. Each restored, git diff clean. Three claims corrected in the same round, all in the retaining direction and all recorded as OPEN rather than quietly fixed: EVIDENCE_RETENTION_DAYS's \"every successful run rewrites validatedAt\" is false for a REPLAYING browser-steps read (evidence-of-a-replaying-action-ages-out-while-the-action-is-in-daily-use); the org-shared erasure rationale is false under ordering (evidence-of-a-shared-credential-survives-its-disconnection); and the MIGRATED-row fixture's provenance describes a migration that cannot have happened, since this collection has never shipped. capEvidence's per-STEP `|| step.truncated` disjunct is disclosed as an EQUIVALENT MUTANT (the ...step spread already carries the flag, so no test can kill it) so a future round does not mistake it for the carrier the way the run-level disjunct was. NO NEW FILES - the count stays 7. RULE 7: `outcome` is an internal document field on a collection with no production reader and no response schema, so nothing moves on the wire, OpenAPI and the generated cortex-cli are byte-identical and EXPECTED_PENDING_COUNT is unmoved. FIXED-12: the stored shape changes additively, so 05-data-model carries an append-only AS-BUILT (g) note; no module, seam or flow moves, so 02-module-map needs none. ROUND NINE (2026-08-21) - THE TRIGGER, not the constant. `the retention rail` in tests/automation/composition-root-screenshot-pins.test.ts (three cases, no new file) pins that the sweep RUNS. It did not: sweepExpiredEvidence and sweepExpiredScreenshots had exactly one caller chain in the estate (sweepScreenshotsSparingPinnedEvidence -> bootState -> boot()), with no setInterval and no Mongo TTL index anywhere in the repo, in a deployment built not to restart - so round six's constant pin (90 -> 89 / 90 -> 91 both redden) enforced a number nothing fired, and five documents including this ledger's own predecessor called the bound ENFORCED. The cases: (1) enters the REAL bootState, asserts the rail is armed and hasRef() === false, expires a row AFTER boot (asserting the un-swept state first, as the control) and waits for a TICK to collect it and release its screenshot pin - collection with NO restart; (2) arms with NO interval argument (production's exact call) under fake timers and straddles the literal 6 * 60 * 60 * 1000 by one millisecond either side; (3) two overlapping ticks - peak concurrency 1, the second answers null. MEASURED: arming nothing reddens 2; dropping timer.unref?.() reddens 1; 6h -> 5h reddens 1 and 6h -> 7h reddens 1 (both directions); removing startRetentionSweepRail from bootState reddens 1; removing the re-entrancy guard reddens 1. Each restored byte-identical. ALSO round nine, in tests/integrations/action-evidence-capture.test.ts (no new file): `the failure dump and the success sample cut the same body at the same point` ties MAX_BODY_DISPLAY_BYTES to MAX_EVIDENCE_EXCERPT_CHARS - the docblock had promised they could not drift over two independent 8_000 literals, and mutating either alone left the estate green. Pinned behaviourally (one oversized body through the real executor twice, 2xx and 5xx) rather than by comparing the constants, which is a tautology over any equal pair. MEASURED both ways: +1_000 reddens, 111 reddens; a bare re-split to 8_000 stays green and is disclosed as not-a-drift, while re-split plus moving the store's constant to 111 reddens, which is what shows the tie is load-bearing rather than an equivalent mutant. The isolation and removal suites changed only in COMMENTS, TEST NAMES and one fixture id: round eight logged the MIGRATED-row provenance sweep CLOSED without grepping, and the retired claim survived in the security suite's header, two comments and a test NAME. Renamed to `an OWNERLESS row` in both suites; a maintainer grepping \"migrated\" no longer lands on a test asserting that a deployment holds pre-owner rows. NO NEW FILES - the count stays 7. RULE 7: the rail is a process-internal timer; no descriptor, route, auth class or zod shape moves, the shared/ edit is a docblock, OpenAPI and the generated cortex-cli are byte-identical, EXPECTED_PENDING_COUNT unmoved. FIXED-12: retention gains a new LIFECYCLE TRIGGER on an existing seam, so 02-module-map carries an append-only AS-BUILT (g) note and 05-data-model an append-only (h) note correcting what its own retention line meant."
      },
      {
        "group": "publish doors + promotion rules (S6)",
        "count": 3,
        "targetGate": "G12",
        "note": "Slice S6 mounted the PUBLISH DOORS: five thin routes over `integrations/publish-scrub.ts` (previewPublish, publishDefinition) and `integrations/definition-store.ts` (requestPublish, withdrawPublishRequest, listPublishRequests), all of which were built and tested by slice E2 and had ZERO non-test callers - so the submit-for-review window E2 designed could not be opened and the review queue behind it had never been reachable in the running product. TWO NEW FILES. tests/contract/integrations-publish.test.ts - the five doors through the REAL app (buildApp on a real socket, real logins, two orgs, four roles, no handler called directly): submit/withdraw inside the tenant (the `org` launch-pad rule, the idempotent re-stamp, the org-admin admitted and the same-org peer refused, the PUBLISHED row refusing the withdraw so un-publish stays the reversible way back, and the platform reviewer refused the org's OWN withdraw); the queue outside it (a submission is the only thing that puts a tenant row in front of a non-member super-admin and withdrawing closes the window again; the entry's key set asserted as an EQUALITY so a field carried on by a spread fails rather than passing a 'does not contain skillMd' check; super-admin AT THE ROUTE, so an org-admin gets 403 rather than an empty list to infer from; and the literal `/definitions/publish-requests` really outranking `/definitions/:id`); the preview (the deterministic floor visibly applied ON THE WIRE - the planted credential absent from the whole response and `[REDACTED]` in the snapshot - the redaction report carrying path/rule/source/removedChars and no removed TEXT, the E2 review window opening and closing with the submission, and preview/publish byte-compared so the dry run cannot drift from the write); and the publish (the tier and the artifact landing in ONE gated write, the author's LIVE row untouched, another org resolving the scrubbed artifact through the ordinary merged registry, the `org` launch pad and the super-admin bar at BOTH layers, `requireModelPass` refusing with SECRET_GUARD_BLOCKED and storing nothing, and RE-PUBLISH SUPERSEDING wholesale with the replaced stamp recorded - the brief's 'promoting a user-built integration may replace the existing public one'). Every 2xx body against the shared schemas, every non-2xx against the envelope, and every no-existence-oracle 404 asserted BYTE-FOR-BYTE against a genuinely missing id. THE MODEL PASS IS NOT STUBBED: the suite boots without a model credential, so the chokepoint pass genuinely fails and the assertions ARE the end-to-end proof that the floor is the control rather than a claim about it. tests/security/publish-doors-isolation.test.ts - the memvault-class suite for the three properties that only become meaningful once the doors are mounted, and which tests/security/integration-publish-scrub.test.ts (the credential-containment half) does not cover. (1) THE D5 STRUCTURAL EXCLUSION asserted from the publish side: evidence and per-user feedback live in tenant-scoped collections built by another stream and cannot be imported here, so the suite plants a battery of evidence- and feedback-shaped fields ON the definition document - six at the top level, five per action - and proves the snapshot has no field either could occupy, by KEY-SET EQUALITY at three levels (snapshot, config, action) rather than by name. (2) THE PROVENANCE PROJECTION: authoredBy, trustedBy, goal and verification.checks[].detail gone from the snapshot while state, shape, declaredMutates and the verification verdict stay - with the case that keeps the projection honest, that a PROVISIONAL action must not read as human-written cross-org (an ABSENT authoring record means trusted, so scrubbing the record wholesale would open the write gate for every consuming org). (3) THE NOTE, the one field mounting created that leaves the tenant, read back from the PERSISTED row. Every containment assertion carries its control: the fixture is asserted REAL first, and the author is asserted to keep the whole record on their own live row. VERIFIED BY MUTATION before landing, source restored exactly: removing the five route registrations reddens 21 cases across the contract suite and mount-coverage; replacing the action whitelist with the pre-change spread reddens 2 security cases; making the provenance projection the identity reddens 1; removing the note scrub reddens 1; dropping requireRole from the queue route reddens 1; spreading the document into the queue entry reddens 2. Hermetic and LLM-free (the model pass fails by construction and the suites assert the degradation); enforced by npm test, not this census run. Ratchet applies from landing. S6 REVIEW ROUND (2026-08-20), three unfailable tests replaced and one live defect closed. (1) THE QUEUE RAN NO SCRUB AND CARRIED CONTENT. `publishQueueEntry` returned `doc.displayName` and `doc.key` verbatim to a super-admin of ANOTHER ORG, while both are package fields walked by `applyPublishFloor` - so an org-A row named `CRM sk_live_<key>` was served to org B's reviewer as the literal string while its published snapshot read `CRM [REDACTED]`. The queue was a strictly WIDER read of tenant content than the preview it is documented as being narrower than. The entry now projects off `applyPublishFloor(publishableContentOf(doc)).content.config`, and the security suite compares the queue's displayName BYTE-FOR-BYTE against the publish floor's answer for the same row rather than merely asserting that something was redacted. A second queue case pins the note's read-side scrub, which is a DIFFERENT property from the submit route's (that one keeps a credential out of the database, this keeps it out of another org whatever wrote the row) and is fixtured by writing the raw note straight through `store.requestPublish`. (2) THE NOTE CAP could not be distinguished from the zod `.max()`: the old test submitted EXACTLY the cap through the route, a length the schema already allows - and its `'a'.repeat(1000)` string matched the floor's LONG_HEX_RE, so the whole note became the 10-char `[REDACTED]` and `<= 1000` was true whatever the cap did. The cap moved to `requestPublish` (the ONE writer) and is asserted with an EQUALITY against a genuinely over-long, non-secret-shaped string, with the route's at-the-bound case kept as the reachability half. (3) TWO GATES WITH ONLY THE OUTER ONE EXERCISED. Removing `requireRole('super-admin')` from the publish route was a SURVIVING MUTANT (route and store both answer 403 for a visible row); the contract suite now asserts the ORDER - middleware refuses before any row is looked up, so it answers the same 403 for an id that names NOTHING where the store would answer 404. And `listPublishRequests`' own `if (actor.role !== 'super-admin') return []` is unreachable from HTTP behind that same `requireRole`, so the security suite calls the store DIRECTLY with a user, an org-admin and a foreign user, super-admin control asserted first. VERIFIED BY MUTATION before landing, source restored byte-identically each time: reverting the queue's floored read reddens 1, reverting the queue's note scrub reddens 1, dropping the store's `.slice` cap reddens 1 (1500 vs 1000), dropping `requireRole` from the publish route reddens 1 at the missing-id assertion only, and DELETING the store's `role !== 'super-admin'` tenancy filter reddens 1 (the Rule 5 proof). Counts: the two files hold 35 cases, up from 32. S6 REVIEW ROUND THREE (2026-08-20), two MAJOR defects closed, both about doors OTHER than the five this slice mounted and both reachable only BECAUSE it mounted them; no new test FILE, so `count` stays 2. (1) `requestPublish` HAD NO ORG GUARD. It admitted on `canWriteDefinition` alone, which answers TRUE for ANY super-admin over a row they can merely SEE - and mounting the submit door is what brings `isDefinitionVisibleTo`'s review-window branch to life, so a super-admin OUTSIDE the authoring org could re-stamp the tenant's standing request: `requestedBy` became their own id and the note arguing for publication became their own words, answered 200 because re-submission is idempotent by design. `withdrawPublishRequest` already carried the guard inline and the submit door had none; that asymmetry was the defect. Closed with ONE named term, `canWriteOwnPublishRequest` (writability AND `sameOrg`), used by both doors and both layers of each. The contract suite adds the wire case (the foreign reviewer refused 403, the ROW asserted unchanged, and org A's own super-admin admitted as the control that pins the rule to MEMBERSHIP rather than to the role); the security suite adds a five-case describe driven at the STORE with the actors the routes would pass - the window cannot be OPENED from outside (404, no existence oracle), cannot be OVERWRITTEN once open (403, with `getWritableForActor` asserted `ok` FIRST so the refusal is the write being refused and not the row being invisible), cannot be withdrawn from outside, admits the org-admin and the org's own super-admin while refusing a same-org peer, and refuses an ORG-LESS actor over an org-less row (planted with `integrationDefinitions.put`, because `store.create` refuses that shape outright - the honest fixture for a document only a migration could have written). (2) `POST /definitions/:id/global` PUBLISHED CROSS-ORG WITH NO SNAPSHOT. It called `setVisibility(..., 'global')`, so the row reached the cross-org tier with no `publishedSnapshot` and `publishedViewOf` served every other org the author's LIVE row through the read-time floor - no chokepoint model pass (the floor is layer 1 of 2), no `scrubbedBy`/`scrubbedAt`/`scrubVersion`, and a broken lineage since `republish` is `publishedSnapshot !== undefined`. Closed by routing `{global:true}` through `publishDefinition`, the same function `POST .../publish` calls, so ONE way crosses the boundary and it always writes a snapshot; `{global:false}` keeps the route alive because un-publishing has no equivalent on the publish door. The contract suite adds seven cases: the REACHABILITY this slice created (404 byte-compared against a missing id before the submission, 200 after), the snapshot landing with org B reading the scrubbed artifact, the publication being RECORDED so the next reviewer's `republish` and `supersedes` are true, the demotion still landing on `org` and keeping the reviewed artifact, THE INVARIANT walked over every mounted door that can write `visibility` (with its own non-vacuity half, since an `if (global)` guard would otherwise pass by never running), a pairwise refusal comparison proving the fold moved no bar, and the two-layer case below. VERIFIED BY MUTATION this round, each restored byte-identically (md5 against a pre-mutation copy): deleting the `sameOrg` term reddens 5 across both files; reverting `{global:true}` to `setVisibility` reddens 4; dropping the store's `.slice` note cap reddens 1; dropping `requireRole` from `POST .../publish` reddens 3; deleting `listPublishRequests`' `role !== 'super-admin'` filter reddens 1. A FOURTH gate of that class was found unpinned in the same sweep: `POST /definitions/:id/global`'s own `requireRole('super-admin')` was a surviving mutant after the fold, since every non-super-admin it refuses is one the store refuses too; it is now told apart the same way the publish door's is (middleware answers before any row is read, so 403 for an id that names NOTHING where the store must answer 404), asserted for BOTH doors in BOTH `{global}` directions with a super-admin's 404 as the control - dropping it now reddens 2. RECORDED LIMIT, measured not inferred: the pre-check / in-mutator F4b PAIR is not separable - deleting either APPLICATION alone leaves the suite green in both directions, because the survivor yields the identical verdict and row state. The mitigation is structural (both call ONE predicate, so there is one place to delete from), and the sibling captured-calls-isolation suite records the same limit for the same pattern. Counts: the two files hold 48 cases, up from 35. S6 REVIEW ROUND FOUR (2026-08-20), one MAJOR (a gate, not a defect) and four minors; ONE NEW FILE, so `count` goes 2 -> 3. THE MAJOR WAS THE SECRETS GATE ITSELF: the branch introduced a `stripe-access-token` finding, round three composed the fixture at runtime and reported it closed, and the gate stayed RED - because `gitleaks detect` scans git HISTORY, so a follow-up commit never removes a literal from the commit that introduced it. Measured before (`--log-opts=\"main..HEAD\"` = 1 finding) and after the branch's commits were replayed with the fixture composed from the start (= 0). No allowlist entry: `scripts/gitleaks.toml` already carries its own warning that each listed value is a deliberate act. MINOR-1 + MINOR-4, ONE DEFECT: folding `POST .../global` `{global:true}` into `publishDefinition` made a RETRY of it a full unreviewed re-publish - `visibilityWriteVerdict` permits `global -> global` on purpose (E2 narrowed the launch pad to `=== 'private'` so an artifact can be refreshed at all), so a reviewer re-asserting a tier they believe is set would re-scrub the author's CURRENT live row over the reviewed artifact in every consuming org, stamped `supersedes`, with no preview in the loop - while the route's contract, `docs/architecture.md` and the `setGlobal` descriptor all still read as a tier toggle. Closed with `promoteOnly` and a distinct `already-published` verdict (not an `ok` carrying `redactions: []` and a synthesised `modelPass`: no scrub ran, so there is no honest value for either), judged AFTER the pre-check so the door gains no existence oracle, and from the SAME read the gate used. `/publish` maps the arm it can never receive to INTERNAL rather than casting it away. The contract suite adds the case with BOTH halves - the retry leaves the stored snapshot byte-identical and free of text added after the review, and the publish door on the same row in the same state still supersedes, so idempotence is pinned on THIS door rather than on an inability to re-publish. A `global` row with NO snapshot is still published here, because that is the state the fold exists to end. MINOR-3: `chokepointModelPass` is now DEADLINED (`MODEL_PASS_BUDGET_MS`, an AbortController handed to `completeFast` exactly as `llm/gateway.ts`'s `CLASSIFY_BUDGET_MS` does it). `scrubForPublish` degrades on a THROW, and a provider that accepts the connection and never answers is not a throw - so the publish waited on the socket, and the fold had just put a second door behind that wait. NEW FILE tests/integrations/publish-model-pass-budget.test.ts, 3 cases: the signal really reaches the chokepoint, the rejection lands AT the budget and not before it, and the timeout DEGRADES to the floor with `modelPass: {status:'failed'}` recorded rather than refusing. It is the one file in this slice that mocks `llm/index.js`, and the header says why: a timeout around a call that never returns cannot be expressed with the real chokepoint (no credential fails fast, a credential answers), and the `opts.modelPass` seam the other suites use REPLACES the very function that carries the deadline. Every assertion runs through the real scrub, floor and failure posture. MINOR-2: the review queue was an unpaginated CROSS-TENANT scan that pulled every org-visibility definition of every tenant into the process - skillMd, lessons, action bodies, config schemas - to discard almost all of them in a JS filter. Both terms are now the database's (`{visibility:'org', publishRequest:{$exists:true}}`, which matches exactly what the JS predicate matched). Observed at the STORE READ, because the returned items are identical either way and that is precisely how a correct-looking answer hid an unbounded scan: `RecordingStore` SUBCLASSES the real `Store` and delegates every call to it, adding only the row count. PAGINATION IS RECORDED, NOT FIXED - a cursor on `IntegrationPublishQueueResponse` is a Rule 7 contract change with OpenAPI and cortex-cli regeneration behind it. VERIFIED BY MUTATION THIS ROUND, each restored and md5-checked against its pre-mutation copy: dropping `promoteOnly` from the `/global` door reddens 1 (the snapshot deep-compare); restoring the queue's wide scan + JS filter reddens 1 (5 rows read where 1 is read now); dropping the abort signal reddens 3; making the timer a no-op reddens 3 - and after a fix to the budget suite every one of those six fails on a named assertion rather than by exhausting a test timeout. The four earlier two-layer gates were RE-CONFIRMED failable from a clean tree: the store's `.slice` note cap 1 red (1500 vs 1000), `POST .../publish`'s `requireRole` 3 red, `POST .../global`'s `requireRole` 2 red, and DELETING `listPublishRequests`' `role !== 'super-admin'` filter 1 red (the Rule 5 proof). AND THREE CLAIMS WERE CORRECTED RATHER THAN DEFENDED. (1) The E1-F4b pre-check / in-mutator PAIR was RE-MEASURED here: deleting the pre-check application alone leaves 50/50 green, and deleting the in-mutator application alone leaves 50/50 green. It is genuinely not separable, so it is carried as a DISCLOSURE and never as two proofs; the mitigation stays structural (both call one predicate). (2) `sendPublishRefusal`'s `model_pass_required` arm is DEAD from the `/global` door, which never asks for `requireModelPass` - the docblock now names that door instead of implying a test covers it. (3) 'nothing reaches the global tier without an artifact' overclaimed: it walks the MOUNTED HTTP routes only. Retitled to say so, with `legacy-runtime-import.ts` and a direct `create({visibility:'global'})` named as outside it and recorded in docs/findings.md; the read-path floor is what makes those safe rather than correct. Counts: the three files hold 53 cases, up from 48. S6 REVIEW ROUND FIVE (2026-08-21), the wrong-unit round; NO new test FILE, so `count` stays 3. THE DEFECT: everything the reviewer was shown before approving is derived from ONE (org,key) row - `republish` is literally `publishedSnapshot !== undefined` and `supersedes` is stamped from the same field - while what approving DOES is decide what every OTHER org resolves for a KEY. `getForActor` resolves at most one `global` row per key (oldest createdAt, orgId tiebreak), so a second org publishing a key a first org already published was answered 200, with republish:false and no supersedes, for a snapshot no consumer could ever read - the incumbent keeps answering, permanently. DECIDED: the collision is REFUSED (`key-taken` -> SLUG_TAKEN 409), not superseded, because superseding is a way for one tenant to seize another tenant's key and change what every consuming org resolves; the escape hatch is a super-admin demoting the incumbent, and the test exercises it as its non-vacuity half. `republish` was deliberately NOT redefined per key - its row-lineage meaning is what `supersedes` records - so the reviewer gets both facts under their own names: `republish` (the row) and the new `keyHeldBy` (the key's holder, present only on a collision, and its key set asserted as an EQUALITY in that state so the new field sits inside the whitelist gate). The preview carries the same fact as a BARE boolean `keyHeldByAnotherOrg`, because an author reads that response and a published package is anonymous by construction; the queue is the attributed surface and carries the orgId. SECOND REFUSAL, same rule: `publishedViewOf` restores `key: doc.key` RAW cross-org (the registry resolves BY key), so the key is the one package field a snapshot cannot clean - and round two's flooring of the QUEUE's copy meant the reviewer read `[REDACTED]` while every consuming org's catalog read the literal. `publishDefinition` now compares the scrub's OWN output for `integrationKey` against the stored key and refuses (`key-redacted` -> SECRET_GUARD_BLOCKED 422) on BOTH doors onto the tier. No new predicate, and nothing tightened at the save path, so no stored row becomes unsavable and there is no migration. The case walks PRODUCTION WRITERS end to end - `saveAuthoredDefinition`, asserted to ACCEPT the key FIRST because that acceptance IS the exposure (`SAVE_KEY_RE` is a charset rule and a real Slack bot token is lowercase, digits and dashes, so it satisfies that rule and `VENDOR_SECRET_RE` alike), then the visibility route, the submit door and the publish door. THIRD: publishing now CONSUMES `publishRequest`. The stamp is what opens the cross-org review window (`isDefinitionVisibleTo`), so leaving it standing meant an un-publish handed the row back to every platform super-admin on a consent given for a publication that had already happened. The contract suite could not see it because both un-publish cases re-submit immediately; the case that asserted the OPPOSITE ('the record survives, so the transition stays reversible') was the defect written down as a property and is rewritten, with reversibility kept as its control. The sibling `tests/security/integration-publish-scrub.test.ts` case is rewritten the same way: the E1-F1 trapdoor refusal is now asserted while the row is still `global` (where `forbidden` is unambiguously the WRITE being refused rather than the row being invisible), and the shut window is four agreeing notfounds plus a re-submit control. AND A FOURTH, FOUND BY THE MUTATION RUN RATHER THAN BY REVIEW: the cross-org pick was written down THREE times - inline in `getForActor`, in `definition-registry.ts`'s `sortGlobals` (whose comment asserted it was 'the SAME order `getForActor` uses'), and in the new holder lookup. Reversing the store's comparator left the WHOLE SUITE GREEN, so list and resolve could have disagreed about which tenant's package a key names. One exported `oldestGlobalFirst`, imported up the edge the documented cycle already fixes, and the third-org case now asserts that the catalog and the by-key resolve name the same package. AND TWO CLAIMS CORRECTED RATHER THAN DEFENDED: `actionCount` off the publish floor is an EQUIVALENT mutant (`publishableActions` is a map, never a filter, so it counts exactly what `doc.actions` counts) - the docblock implied a distinction that cannot exist, now says so, and no test is claimed for it; and the withdraw's refusal on a `global` row was documented as protecting the reviewer's ability to un-publish, which never consulted the request at all. VERIFIED BY MUTATION THIS ROUND, each restored and md5-checked against its pre-mutation copy: deleting the `key-taken` refusal reddens 1; deleting the `key-redacted` refusal reddens 1; dropping `keyHeldBy` from the queue projection reddens 1; dropping `keyHeldByAnotherOrg` from the preview reddens 1; restoring the carried-forward `publishRequest` reddens 1; REVERSING `oldestGlobalFirst` reddens 1 - and reddened 0 before the registry stopped keeping its own copy, which survivor IS the fourth finding; and DELETING `listPublishRequests`' `role !== 'super-admin'` filter still reddens 1, the Rule 5 proof re-run against the changed store. Counts: the three files hold 70 cases, up from 53. ROUND FIVE, SECOND PASS (2026-08-21), after an independent verifier reproduced the collision at 0e16ef7 and named a LATENT half the first pass did not close. THE DEMOTION WAS THE DANGEROUS OPERATION, NOT THE PUBLICATION: refusing the collision stops NEW pairs and does nothing about the pairs `legacy-runtime-import.ts` and any in-process `create({visibility:'global'})` already wrote. For such a pair, `{global:false}` on the holder PROMOTED the shadowed row - every consuming org silently began executing a different tenant's package, with no publication event, no reviewer and nothing in any lineage. `setVisibility` now takes the shadowed siblings down with the holder (`demoteShadowedSiblings`, at the one chokepoint both the tenant route and `{global:false}` pass through), siblings BEFORE the target because with no multi-document transactions the order IS the crash state - stopping half way must leave the key MORE published, never newly handed over - and only ever downward (`global` -> `org`, never `private`, so the shadowed tenant keeps its own row). The new case plants the pair DIRECTLY through the store, which is the honest fixture for a state only a non-door writer can now produce, and asserts org C's catalog goes EMPTY rather than swapping owner, both rows land on `org`, and org B can still become the holder by publishing - the non-vacuity half. AND OLDEST-FIRST WAS RULED ON RATHER THAN DEFERRED: it is a deterministic TIEBREAK for a degenerate state, not an ownership rule, and it is KEPT. `getForActor`'s sort exists BECAUSE several globals per key are possible, so the resolver always knew and only the publish/review surfaces did not - a surface gap, not an unforeseen state, which is why the fix is on the surfaces. Kept because both reasons are about NOT MOVING: it is stable under new writes (a new row cannot displace an incumbent by existing - the anti-squatting property the refusal is also about), and changing it would itself silently swap what every consuming org resolves for data already on disk. AND THE F4b DISCLOSURE WAS WIDENED, because it only covered SINGLE deletions. Measured: deleting BOTH in-mutator re-asserts at once is green (0 red / 55), and deleting only the VISIBILITY term of both is also green - nothing in this estate pins that re-assert on these two doors. The membership half is STRUCTURALLY UNREACHABLE rather than untested: `canWriteOwnPublishRequest` reads `doc.orgId` (pinned by `definitionIdFor`) and `doc.userId` (carried forward verbatim by every writer in the repo), so no write can flip it between the pre-check and the mutator. Both terms are KEPT and neither is claimed as tested. `docs/decisions.md`'s 'SUPERSEDE IS THE NORMAL CASE' entry and the matching route docblock were CORRECTED IN PLACE rather than caveated below: replacement is per (org,key) ROW, and the brief's 'may replace the existing public one' reads most naturally as the cross-org case that paragraph did not describe. MUTATION, this pass: dropping the sibling demotion reddens 1. Contract lane at the previous commit: 64 files / 771 cases / 0 failed. Counts: the three files hold 71 cases, up from 70."
      },
      {
        "group": "the achieve reuse ladder (S4 parametrize + S5 compose)",
        "count": 4,
        "targetGate": "G12",
        "note": "S4+S5 (the reuse ladder): FOUR NEW FILES, all hermetic, LLM-free (only the model TURN is faked, at the seam or at agents/authoring-core.js, so both output contracts, both parsers and both deterministic guardrail suites are real), and green in the per-PR vitest lane from landing. Ratchet applies. integrations/achieve-reuse-ladder.test.ts (59) - the ladder at module level: the PICK stays lexical (matchActionForGoal untouched, one call site, no rung is handed definition.actions); verifyPlannedArgs as the ONLY check argsSchema ever gets (the executor never reads it, so an undeclared key would be interpolated by buildVars all the same); decision D1 as ONE allowlist predicate (mayBeModelFilled) shared by the rung's pre-filter and the targeting check - fillable iff the action cannot write or the argument lands in the BODY - including the case that repaired it, an automation-backed WRITE whose arguments have no readable slot at all and are therefore withheld rather than offered; the compose stage's join, its caps and its refusal to guess between two lists in one result; the CANONICAL case end to end with a boundary client at exactly 40 so lt and lte are distinguishable; and the degradation half (absent seam / model outage / no residual intent / no collections all leave achieve behaving exactly as it did before the ladder, which is the Rule-7 promise). contract/integrations-reuse-ladder.test.ts (11) - the same through buildApp with the REAL composition-root bindings: a filled argument asserted on the URL the executor actually addressed (not on the module's return value), the write gate answering the identical 403 + awaiting_consent envelope whichever rung filled the arguments, the composed outcome validated against the shared AchieveIntegrationGoalResponse, and two tests whose ONLY reason to pass is that server.ts binds planStep and appCollections - both verified by deleting each binding. security/achieve-compose-isolation.test.ts (6) - the Rule 5 class (memvault-isolation.test.ts): the compose rung is a NEW READ PATH into app_data, so the collection NAMES offered to the model are the caller's own, a peer org's collection name reads nothing, a name both orgs hold does not share rows, a same-org peer's PRIVATE artifact is invisible, a same-org peer's ORG-VISIBLE artifact does NOT open that peer's owner-shared namespace (blocker 2 - verified RED against the unfixed per-artifact binding), and an owner with TWO apps composes from their ONE owner scope instead of being refused a fabricated ambiguity (blocker 1 - likewise verified RED first). Proven to be a gate by DELETING the tenancy filter at the single query-binding point (appId: scope.scopeKey): the suite reds in both directions, rows via list and names via listCollections, and nothing else in the estate notices. ROUND THREE grew the first two files rather than adding a fifth, because what it fixes is the LADDER INVARIANT those files already state: a rung may only ever ADD an answer, never SUBTRACT one. The parametrize rung used to refuse the whole call on a failed guardrail verdict - so a call that executed before the slice stopped executing because a model wrote a bad argument - and the compose wrapper swallowed a failed execute, telling the caller their action returned no list when the truth was a remote 500. Both now have a module-level test and a wire-level one, each verified RED by restoring the pre-fix behaviour in the source. Round three also pinned COMPOSE_MAX_COLLECTION_ROWS, which silently truncated the join's key set with no wire signal and whose deletion left the estate green: a boundary pair at exactly 5000 and 5001 collection rows (only the last row matching) plus a literal value pin now reds on deletion, on a one-row drift and on the signal being silenced. And a 39-mutant source-side sweep over every safety-critical assertion in all four files found TWO UNFAILABLE ONES - argSlotsOf's 'targeting wins' rule (no fixture put one name in both the path and the body) and composeRows' collection-side String(k) coercion (the only numeric-key case put the number on the other side) - both now closed by tests, plus one dismissal recorded in docs/findings.md. ROUND FOUR grew the same four files again, and what it fixes is the invariant round three ASSERTED and did not reach: the compose rung still subtracted an answer on three paths, and TWO OF THEM did it AFTER the remote call had been made and had SUCCEEDED (compose_unknown_collection and compose_unshaped_result are both decided downstream of the execute), so the product performed the caller's work, got a good 200 back and handed over a refusal; compose_refused ended the call before the request instead. All three codes are removed - the ladder now introduces NO refusal code at all, asserted as a COUNT over AchieveRefusalCode read from the source rather than as a sentence in a doc. Each path has a module-level test and a wire-level one, each verified RED by restoring the pre-fix behaviour in the source, and each asserting that the rows the product had in hand reached the caller rather than only that the outcome word changed. The isolation suite tests 2, 4 and 5 used to key on compose_unknown_collection and now assert the stronger property: nothing was joined (no items, no composition), the caller kept their own action's answer, and test 2 compares the WHOLE response body for a name another org holds against the body for a name nobody holds - the no-existence-oracle claim as an assertion instead of a reading. A 54-mutant source-side sweep over the four files (each applied to the SOURCE, run, restored, verified byte-identical) returned EIGHT survivors, and a 31-mutant confirmation pass over the fixed code returned one more the first pass had not written a mutant for; SIX in total were real unfailable assertions now closed by tests: COMPOSE_MAX_ITEMS was never pinned to a literal (its sibling constant was, in round three); the reaches-no-store static guard searched for a CommonJS require() shape an ESM file cannot produce while the real hazard - a plain ESM import of CollectionsEngine - passed it; argSlotsOf QUERY slot had no observable case though D1 names path AND query; contains could become startsWith because every probe compared a prefix; String(field ?? empty) could lose its coercion because the only field-absent assertion used an empty compared value; and ends_with could become includes, which the first pass had not asked about at all - the absence of a mutant read as coverage. Four dismissals are written up in docs/findings.md, including the isolation suite's no-oracle equality, which is a regression guard rather than a killable assertion (the module cannot reach another tenant's scope, so it has no fact to leak) - what is killable, and now asserted, is that the reader's not-found answer is a bare tag with no payload for such a fact to travel in. automation/store-query-predicate.test.ts (4) - the OTHER caller of the extracted predicate. evalQuery's nine comparison semantics moved to data/simple-query.ts so the compose rung could share them without integrations/ importing automation/; a grep of the estate at that moment found NO test anywhere exercising store.query, so this file was written with the refactor and pins equivalence op-by-op through executeRecipe plus the coercion edges (strict eq, NaN orderings, String(field ?? '')) that a tidy-up of the extracted function would otherwise change under every shipped recipe."
      }
    ]
  },
  "conditional_carryover_20": {
    "note": "port with their subsystems",
    "groups": [
      {
        "group": "live-browser streaming (4)",
        "targetGate": "G8",
        "note": "unconditional per Q-01"
      },
      {
        "group": "llm gateway (2)",
        "targetGate": "G7"
      },
      {
        "group": "agent-runtime adapters (4)",
        "targetGate": "G7B"
      },
      {
        "group": "llm-router (1)",
        "targetGate": "G7"
      },
      {
        "group": "knowledge agent wiring (2)",
        "targetGate": "G7B"
      },
      {
        "group": "local daemon bridge / agent-face TUI (7, safety gates verbatim)",
        "targetGate": "G8A"
      }
    ]
  },
  "retired_by_design": {
    "content_machinery_14": {
      "note": "retire with FIXED-4/FIXED-6 architecture; not re-covered"
    },
    "sse_wire_internals_3": {
      "note": "sanitize-at-send re-pinned as new unit test (G5); phase-event + SSEManager unit retire"
    }
  },
  "mock_servers_and_fixtures": {
    "source": "ekoa-dev/cortex/tests/helpers/ + fixtures/",
    "mock_servers": [
      "mock-ifthenpay-server.mjs",
      "mock-invoicexpress-server.mjs",
      "mock-pipedream-server.mjs"
    ],
    "fixtures": [
      "ekoa/e2e/fixtures/audiencia-2vozes.wav",
      "cortex/tests/services/fixtures/citius-consulta.html",
      "cortex/tests/services/fixtures/citius-consulta-latin1.html",
      "cortex/tests/fixtures/citius/consulta-distribuicao.html"
    ],
    "helpers": [
      "ekoa/e2e/helpers/legal.ts",
      "cortex/tests/e2e/helpers/test-client.ts"
    ]
  }
}
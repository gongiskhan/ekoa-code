{
  "name": "@ekoa/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@ekoa/shared": "*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.17.6"
  }
}

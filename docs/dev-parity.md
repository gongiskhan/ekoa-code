# ekoa-dev parity ledger

`../ekoa-dev` (old Cortex, deployed at api.ekoa.io) keeps evolving while this repo is the
rebuild. Every upstream commit gets exactly one disposition here:

- **PORTED** - the functionality exists in ekoa-code (name the file or ref).
- **NOT-NEEDED** - it does not apply to the rebuild (state why).
- **OPEN** - functionality to bring here. An OPEN row is a live work item: it must end
  PORTED or NOT-NEEDED, never silently dropped (same discipline as `findings.md`).

`npm run parity:audit` fetches `../ekoa-dev`, lists upstream commits newer than the recorded
SHA below, prints row scaffolds, and exits non-zero while any commit is undispositioned.
Process: `.claude/skills/ekoa-dev-parity/SKILL.md`. Reference-access rules (read-only, never
copy secret values, runtime-truth validation before porting content): `docs/governance.md`.

**GitHub is not the source of truth for ekoa-dev.** On 2026-08-06 the audit reported "ledger
current" while four commits sat on the operator's other machine, committed and unpushed - the
whole served-app email plane among them. The audit therefore also fetches configured PEER
checkouts and reports commits they hold that `origin/main` does not. Peers are declared in the
marker below (or `EKOA_DEV_PEERS`), so the configuration travels with the repo rather than
living in one shell profile; an unreachable peer is a warning, never a pass.

<!-- parity-peers: madrid=ssh://dev-madrid/home/ggomes/dev/ekoa-dev -->

Last audited upstream commit: `9e6779f79d3bcbd30836c480678a94b360fc90fe` (2026-08-06, audited 2026-08-06)
Last audited peer commit (`madrid`): `9e6779f7` (2026-08-06, audited 2026-08-06) - now PUSHED; the peer and origin agree.

## Dispositions

Audited range: `8214def2..9e6b9679` (the 23 commits on `origin/main` past the July merge base).

| upstream | date | subject | disposition |
|---|---|---|---|
| `613da39c` | 2026-07-10 | Zoho Sign v1 integration | PORTED - superseded upstream by `b5e1f414`; final form in `api/src/integrations/zoho-sign.ts` |
| `27aa0f0a` | 2026-07-11 | root README | NOT-NEEDED - upstream repo housekeeping |
| `b5e1f414` | 2026-07-20 | Zoho Sign rebuild merge + webhook-driven proposal advance | PORTED (`api/src/integrations/zoho-sign.ts`, `sign-agreements.ts`) EXCEPT the Zoho OAuth-popup connect - see `e620e740` row |
| `fff10809` | 2026-07-22 | email signed PDF to signers | PORTED - superseded upstream by `cfb3c8fb`/`2e301631`; final gated form present |
| `5aca300e` | 2026-07-22 | Word track-changes on .docx | PORTED (`api/src/services/docx-redline.ts`, `api/src/apps/document-source.ts`, `docs/word-track-changes.md`) |
| `6e95f0a9` | 2026-07-22 | durable server-side proposal advance on Zoho webhook | PORTED (`api/src/integrations/sign-agreements.ts`) |
| `9603c388` | 2026-07-22 | deploy gotchas docs | NOT-NEEDED - ekoa-dev/ekoa-deploy operational notes |
| `b4e5d869` | 2026-07-22 | deploy note correction | NOT-NEEDED - same |
| `cfb3c8fb` | 2026-07-23 | drop send_completed_document | PORTED - final gated form present |
| `2e301631` | 2026-07-23 | post-sign redirect_pages + gated signed-PDF email | PORTED (`api/src/integrations/zoho-sign.ts` redirect_pages + gate) |
| `9237711b` | 2026-07-23 | fragment-free post-sign return bounce | PORTED (`api/src/integrations/zoho-sign.ts` `GET /api/zoho-sign/return`) |
| `c79cd6a2` | 2026-07-24 | docx comment resolution + thread review | PORTED (`api/src/services/docx-comments.ts`) |
| `d8da6efd` | 2026-07-24 | route document work to the document base | PORTED (`api/src/agents/guided-build.ts`) |
| `5df604de` | 2026-07-24 | dev client base-URL rewrite over Tailscale/LAN | NOT-NEEDED - ekoa-code's dev harness terminates this at the `scripts/dev.mjs` CORS proxy |
| `8bff3f55` | 2026-07-27 | gate the workspace Graph proxy | PORTED - stronger design in `api/src/integrations/m365-proxy.ts` (app-id gate + manifest opt-in + owner activation). Auditing it found BOTH halves dead: the manifest flag was stripped by `validateManifest` and the token seam was a permanent not-connected stub. Both fixed this run - see `findings.md` `m365proxy-manifest-flag-stripped` and `workspace-graph-token-was-a-permanent-not-connected-stub` |
| `78e7421d` | 2026-07-27 | browser-level attacker-origin proof for `/api/m365/*` | OPEN - port the test class (real foreign origin + Chromium CORS) to the e2e suite |
| `ca446cb0` | 2026-07-29 | credential saves must merge, not replace the stored bundle | PORTED 2026-08-05 - `mergeCredentialValues` + `CLEAR_CREDENTIAL` in `api/src/integrations/service.ts`, suite `api/tests/integrations/credential-merge.test.ts`. Went further than upstream: the shadow and the non-secret projection are computed from the merged bundle too, an undecryptable blob refuses the write, and the dashboard's save became an UPSERT (`upsertConfig`) because here it posted `POST /configs` every time and forked a duplicate row |
| `e620e740` | 2026-07-29 | Zoho connect is OAuth-only for customers | PORTED 2026-08-06 - `api/src/integrations/zoho-oauth.ts` + the config package flipped to `authType: "oauth2"` with `client_id`/`client_secret` dropped from the customer-facing form (the two fields Chrome mistook for a login form and autofilled). Also ported the upstream commit the audit missed, `09a29bb7`, which is where the popup flow actually landed |
| `d8e4538e` | 2026-07-29 | OAuth connect clears pasted client credentials | PORTED 2026-08-06 - the callback clears `client_id`/`client_secret`/`grant_code` with `CLEAR_CREDENTIAL`. Ported WITH the test upstream never wrote: it has no coverage of the callback route at all, which is how this regression reached a customer |
| `323b81ae` | 2026-07-30 | chat attachment intake (zip expansion, office sidecars, composer drag/paste) | OPEN - chat feature, not required by the served-app plane; includes a folder-staging path-traversal fix worth reviewing against `api/src` upload paths |
| `3c8e3b3d` | 2026-07-30 | never commit `.backups/` | NOT-NEEDED - ekoa-code backups live on the app-data plane (`api/src/apps/backups.ts`), not a repo directory |
| `cc9eb6cb` | 2026-07-30 | show a pending chat row immediately on send | OPEN - verify against the rebuilt (Atrium) chat page; close or port |
| `9e6b9679` | 2026-08-03 | merge: drop local duplicate Zoho webhook advance | NOT-NEEDED - merge housekeeping; content covered by rows above |

## Peer dispositions (`madrid`) - work that had not reached GitHub

Audited range: `9e6b9679..c4f7f2c6`, the four commits the operator made on `dev-madrid` on
2026-08-06 and had not pushed. The peer audit above is what surfaced them; at the time they were
invisible to `origin/main` and therefore to production.

**Pushed 2026-08-06** (operator-directed), together with a fifth commit `9e6779f7` written here to
land the two halves `c4f7f2c6` described but never committed. `origin/main` is now `9e6779f7`, so
these rows are ordinary upstream dispositions and the section is kept only for the history of how
they were found. Pushing does NOT deploy: `ekoa-dev`'s two workflows are PR/issue-triggered, and
production is released by hand.

| upstream | date | subject | disposition |
|---|---|---|---|
| `1d4eaf64` | 2026-08-06 | email delivery via Integrações + document extraction for served apps | PORTED 2026-08-06 - three pieces. (a) `capabilities` on `IntegrationAction` (`integrations/definitions.ts`) + `email-send` on the m365/google senders, so apps discover a sender by CLASSIFICATION and never by action name. (b) The email plane `integrations/app-email.ts` (`GET /integrations`, `POST /send`) - suites `integrations/app-email.test.ts` + `contract/app-email.test.ts`. (c) Document extraction `apps/app-vision.ts` + `apps/app-vision-route.ts` - suites `apps/app-vision.test.ts` + `contract/app-vision.test.ts`. **Diverges from upstream on purpose:** upstream dispatches with a synthesised admin actor and NO write gate, so a served page could send mail as the workspace unconditionally. Here the send goes through `callPlatformIntegration` with the OWNER as `actingUserId`, so the C2 consent gate binds and an unapproved send is refused with `awaiting_consent` and zero provider traffic. Also swapped `officeparser` for `pdfjs-dist` directly: v7 of it bundles tesseract.js, which fetches trained data at runtime - a second egress path this repo will not have |
| `fd209f70` | 2026-08-06 | provider email drafts + inbox notice + in-app assistant endpoint | PORTED 2026-08-06 (drafts) / NOT-NEEDED (assistant). Drafts: m365 `create_draft`/`send_draft`/`get_profile` and google `create_draft_simple`/`send_draft`/`get_profile` with `email-draft` / `email-draft-send` capabilities, plus `/draft`, `/draft/send`, `/inbox`. Both providers' drafts WRITE to the mailbox, so both are gated; `get_profile` is a read and joins the allowlist (`security/platform-write-gate.test.ts` pins the new mutating set). The in-app assistant is NOT-NEEDED: ekoa-code's `apps/app-assistant-route.ts` is the same endpoint, already further along (grounding + citations + action manifest + the H2 whoami detection) |
| `38365623` | 2026-08-06 | Cobranças featured artifact | PORTED 2026-08-06 - `api/assets/featured-artifacts/cobrancas/` (48 files, rank 30, v1.1.0). ADDITIVE, not a replacement: upstream keeps `legal-cobrancas` (rank 60) alongside it and so do we. Verified as runtime truth, not assumed: it seeds (42 featured, up from 41), builds, serves at `/apps/cobrancas/`, renders its full nav, auto-seeds its default profiles, reads the shared legal spine, and drives the newly-ported plane for real (`200 GET /api/app-email/integrations` returning the three capability-discovered senders) with ZERO console errors. TWO content defects fixed on the way in: (a) its `dados-omissao.test.mjs` resolved `seed-data.json` one directory too shallow, so the test ERRORED rather than asserting - it had never run, upstream included; fixed, and the invariant it guards (auto-seed and fork-seed are the same content) does hold. (b) its Definições page told the user "a plataforma não aplica qualquer confirmação adicional aos envios" - true of the old platform, FALSE here, where a send is a write behind the consent gate. Rewritten to state both approvals. This is the `governance.md` runtime-truth rule doing its job: ported content makes claims about a platform it was not written for |
| `9e6779f7` | 2026-08-06 | the two missing halves of "continuar edita NO LUGAR" | PORTED / NOT-NEEDED 2026-08-06 - this commit was WRITTEN during this run, on the peer, to commit the two changes `c4f7f2c6` described and left in its working tree. Its chat-stripes half is the same fix already ported here (see the row below); its `useAgentExecution` half is NOT-NEEDED, since ekoa-code already keys a follow-up on the artifact id alone |
| `c4f7f2c6` | 2026-08-06 | continuing a featured app edits it IN PLACE, never forks | PORTED 2026-08-06 (the real half) - `web/components/chat/chat-stripes.tsx` no longer forks, and `web/lib/featured-fork.ts` is deleted; regression suite `web/__tests__/components/chat-stripes-featured.test.tsx` pins the NEGATIVE assertion (fork is never called). NOTE: upstream's commit MESSAGE describes three changes but the commit contains only the file deletion - the other two are still uncommitted in dev-madrid's working tree. The `useAgentExecution` half is NOT-NEEDED here: ekoa-code already keys a follow-up on the artifact id alone and never sends a client-side project dir (the server resolves it), so the bug it fixes cannot occur |

### Open work this ledger opened

| item | disposition |
|---|---|
| The docx link/cloud ingest still holds the not-connected workspace stub - a build's tool call carries an appId but no owner down to `agents/seams.ts fetchFromCloud`, so it cannot name whose workspace to spend | OPEN - thread the run's owner through the seam, then wire it to `workspaceCredentials.accessToken` like the other two planes |
| ~~`zoho-sign/config.json` still `authType: "api_key"`~~ | CLOSED 2026-08-06 by the `e620e740` port |
| Trigger-creation listener inference (`cortex/src/handlers/triggers-handler.ts` `isPlatformProvider` branch) | PORTED 2026-08-14 (salomao-migration S2) into `api/src/events/service.ts` `listenerStamp`: platform provider => kind `listener` + pollConfig { actionName from `platformListenerConfig`, intervalMs: request `pollIntervalMs` ?? 60000 }, additive `pollIntervalMs` on `TriggerCreateRequest`, view emits `entrypoint`. NOT ported from that handler (still open): skill-based webhookConfig/listenerConfig kind inference for non-platform integrations, webhook auto-registration (registrationState/registrationMeta/responseSecretPath e.g. Stripe `whsec_`), and the one-trigger-per-automation replace-on-create semantics |
| Prod trigger ROW + poll-cursor migration (old `triggers.json` registry / `triggers.db` listener_state) | OPEN as state-migration policy, RESOLVED as recreation: the cutover runbook (`docs/operations-runbook.md`, salomao section) recreates the mailbox listener via the S2 API path against the imported artifact id; the cursor deliberately starts at arm time (the runbook names the email-loss window and orders arming before the switch). The ch10 import-tool still has no `triggers`/`automations` family - acceptable while recreation is one API call per trigger; revisit if a customer accumulates trigger fleets |

### Standing gaps predating this ledger

| upstream | subject | disposition |
|---|---|---|
| `4f0e7ae7`+`4c05aa7f`+local `bd5ac057` | PWA platform (`services/pwa.ts`: `/apps/:ref/{manifest.webmanifest,pwa-icon-*.png,sw.js}`, brand-versioned icon URLs) | OPEN - being ported this run to `api/src/apps/pwa.ts` |
| - | local-only ekoa-dev commit `bd5ac057` (rebased from `31f94f9c`) is unpushed; also preserved on `backup/local-pwa-icon-31f94f9c` | operator decision pending: push or fold |
| `1dd0019b` | 2026-05-17 | feat(mega-plan): Featured Artifacts + invisible base templates + orchestrator state machine (introduces `SELECT_BASE_TEMPLATE_SYSTEM`, model-first base selection) | PORTED (reasoning, not code) 2026-08-08 - WS6 incident fix (a "site" request built a slide deck: `api/src/apps/artifact-type.ts` ran its deterministic regex table BEFORE any model call, and it had no site/website signal at all). Inverted to MODEL FIRST on every non-empty request: a labelled type table + explicit disambiguation rules + worked examples (including the incident phrasing), returning `{type, reason}` - the reason is logged, so a future misroute is self-diagnosing (ekoa-dev logs `selection.reason` the same way at `execute-handler.ts:442`). The regex table survives ONLY as the offline/failure fallback, fixed for both incident bugs (site/website now matches landing; presentation now requires a real deck anchor, not bare "apresentação"). Not a line-for-line port: ekoa-code's five ARTIFACT TYPES (`shared/src/artifact-type.ts`) differ from ekoa-dev's five BASES, and there is no orchestrator "gather intent" state machine equivalent here - the classifier is a self-contained function, not a phase transition |
| `de6c081b` | 2026-05-20 | fix(chat-agent): complete integration build redirect flow (Issue 4) (introduces `original_message: envelope.message` on the build_intent event) | PORTED 2026-08-08 - WS6. `shared/src/events.ts` `build_intent.request.originalMessage` + `shared/src/jobs.ts` `JobCreateRequest.originalMessage` (additive, Rule 7), threaded end-to-end: `api/src/agents/chat.ts` (`emitBuildIntent`/`finishComplete` delegate payload) -> `web/components/chat/chat-runtime.tsx` (both the `build_intent` SSE safety-net listener and the primary `pendingDelegation` path) -> `web/hooks/useAgentExecution.ts` -> `api/src/routes/jobs.ts` -> `api/src/agents/build.ts`. Pre-fix, only the chat-agent's <=15-word build PARAPHRASE reached the classifier and the build agent's prompt; the paraphrase still names the artifact (`description`), it just no longer drives classification or briefing |
| `8308b33d` | 2026-06-15 | fix(cortex): retain chat context + bias chat agent to action (conversation-transcript injection into build runs, budget-based truncation) | PORTED 2026-08-08 - WS6. `api/src/agents/context.ts` `capHistory()` (16 turns / 48k total chars / 24k per-turn, newest-first budget allocation - same shape as `cortex/src/adapters/external.ts:445-447`) + `api/src/agents/build.ts` injects it via the existing `loadHistory`/`renderPrompt` (mirroring how `chat.ts` already used them) on FIRST builds only - a follow-up already resumes the real SDK session, so re-injecting history there would duplicate context the engine has |
| `eb05231d` | 2026-07-03 | fix(build): survive provider rate limits, carry chat context into builds, add document (Word/PDF) base (introduces `ekoa-data/plugins/skills/base-selector/SKILL.md`'s worked-examples table and `execute-handler.ts`'s session-persisted base selection + attachment-name feed) | PORTED (reasoning, not code) 2026-08-08 - WS6. The worked-examples table shape is lifted into `artifact-type.ts` `CLASSIFY_SYSTEM` (ekoa-code's own five-type examples, not ekoa-dev's five-base ones). Session persistence: `api/src/data/stores.ts` `SessionDoc.lastArtifactType`, written best-effort in `build-mechanics.ts` `baseFor()` (mirrors `execute-handler.ts:441` `setOrchestratorState(sessionId, {base: baseId})`) - internal-only, never on the wire `Session` schema, same posture as the existing `lastContext` field. Attachment names: `BuildCreateInput.attachments` -> `attachmentNamesOf()` -> `ClassifyOpts.attachments`, fed to the model prompt as a plain line (mirrors `execute-handler.ts:433-434`) |
| `2246ad99` + `0beae804` | 2026-06-26 | feat(knowledge): ripgrep-only knowledge base, accent-accurate, agent-first (no UI search); feat(knowledge): strip cookie/consent boilerplate from crawled content + seed ACT/DRE (render) | PORTED (list only, not the crawler) 2026-08-08 - WS8b. The operator reported "the knowledge base is empty"; it was not (`~/.ekoa/data/knowledge/vault/_shared` holds jurisprudencia=209,196, legislacao=43,250, legislacao-laboral=10,224 files, imported 2026-07-11 - disk-verified this run), the Sources tab was, because `knowledge_sources` had never been seeded. Ported the 5-source LIST verbatim from git-committed `ekoa-data/knowledge/sources.seed.json` into `api/assets/knowledge/sources.seed.json` (crawl params levels/maxPages/scope/render preserved per-row in `crawlConfig` for later use), and the seeding PATTERN (per-`seedId` idempotent insert, read-versioned-JSON-asset shape) from `cortex/src/services/knowledge-seed.ts` into `api/src/knowledge/sources-seeder.ts`, wired into `bootState()` (`api/src/server.ts`) right after `seedFeaturedArtifacts()`. NOT ported: the crawler itself that would EXECUTE these sources - `crawlSource`/`crawlStatus`/`refreshSchedule` stay honest stubs in `api/src/routes/knowledge.ts` ("no crawler in this build"); that is WS8c, a separate workstream, tracked as its own OPEN item below. The `kind: 'domino'` / 8-`.nsf`-database gap this row originally left open is CLOSED by WS8c (2026-08-08): the operator supplied the real runtime config read directly from `~/.ekoa/data/knowledge/sources.json` (the specific database list is not in any git-committed file this checkout can read under the read-only-git-fetch constraint, so it could not have been ported without that hand-off), now carried in `api/assets/knowledge/sources.seed.json` version 2 and the `domino` field on `KnowledgeSourceDoc`/`SourceInput` - see the `0e590627` row below for the harvester code itself |
| `2246ad99` (crawler half) | 2026-06-26 | same commit, the CRAWL EXECUTION half (frontier walk, render mode, seed-template expansion, nightly refresh scheduler) | PORTED 2026-08-08 - WS8c. Re-authored against this repo's own vault (`api/src/knowledge/vault.ts`) + FTS store (`index-store.ts`), not copied verbatim: `api/src/knowledge/crawl/ledger.ts` (per-page work queue, `pending`/`ok`/`error`), `extract.ts` (link/content extraction, boilerplate stripping, charset decode), `engine.ts` (`crawlSource` - resumable frontier walk, conditional GET, render mode via the existing shared `browser-pool.ts`), `runner.ts` (background lifecycle: synchronous already-running guard, `kind`-based dispatch), `scheduler.ts` (self-rolled nightly timer, no node-cron, default 03:00 local, `EKOA_KNOWLEDGE_REFRESH_HOUR`/`_DISABLED` env overrides). LEDGER SHAPE: deliberately DIVERGES from ekoa-dev's `<dataDir>/knowledge/ledger/<sourceId>.json` single-file-per-source layout on-disk representation (JSON array with atomic temp-file+rename writes and a source-id-keyed write-queue for serialized concurrent writes) - matches the upstream CONCEPT (a durable per-page work queue keyed by source id, same directory shape `<dataDir>/knowledge/ledger/`) closely enough that a pre-existing 199MB/14-file ledger directory from a machine that already ran ekoa-dev's crawler is picked up unmodified, since both are keyed by the same source ids (ID CONTINUITY, `api/src/knowledge/sources-seeder.ts` module doc) and read/write the same file paths - only the in-file JSON shape was re-authored, not the addressing scheme. `crawlSource`/`crawlStatus`/`refreshSchedule` are real now (`api/src/routes/knowledge.ts`); the web Sources tab's "Atualizar" trigger is re-enabled, gated to `role: 'super-admin'` (writing into the reserved `_shared` corpus is privileged; browsing it, WS8a, stays open to any org actor). Runtime-truth dispositions for the 5 seeded sources (`~/.ekoa/data/knowledge/sources.json`, read live, not guessed): DGSI enabled with its real 8-database domino config; PGDL seeded disabled with reason "concluído com sucesso, sem indicação de falha" (not broken, just already finished); DGERT seeded disabled with reason citing its wholesale 3046/3046-failed history (root cause not diagnosable offline - likely the TLS-repair gap, `c6d22e58` row below); ACT enabled with `render:true` (unchanged); DRE enabled with `render:false` + a real `userAgent` (Googlebot) + a real `seeds` array, taken from verified runtime state even though it contradicts the WS8 brief's `render:true` assumption. Test coverage: `api/tests/knowledge/crawl/{ledger,extract,engine,domino,scheduler,runner}.test.ts` (unit, real local `http.createServer` fixtures for HTTP-transport-level coverage, zero live requests - `runner.test.ts` uses an SSRF-refused loopback URL instead of a fixture server since the runner has no `fetchImpl` injection point) + contract coverage in `api/tests/contract/f5-ui-endpoints.test.ts` (super-admin gate, cross-org 404, real wire shapes) and `api/tests/contract/knowledge.test.ts` (descriptor auth tier). Deliberate SCOPE CUTS, tracked as their own rows below (never silently dropped): no PDF/office extraction during crawl (`88a63c8f`), no TLS AIA chain auto-repair (`b58a5854`/`c6d22e58`), `kind: 'api'` declared on the schema but its execution refused with an honest error rather than silently mis-run through the anchor crawler (`238d4ab8`) |
| `0e590627` | 2026-06-27 | feat(knowledge): DGSI Domino harvester (?ReadViewEntries XML enumeration) | PORTED 2026-08-08 - WS8c. `api/src/knowledge/crawl/domino.ts` (`parseUnids`, `crawlDominoSource`, `crawlDominoSourceById`): paginated `ReadViewEntries` XML enumeration across `domino.databases`, concurrent `OpenDocument` fetches with conditional GET (ETag), per-run document budget. Tested against saved fixture pages over a real local `http.createServer` (never dgsi.pt live) in `api/tests/knowledge/crawl/domino.test.ts` |
| `238d4ab8` | 2026-06-27 | feat(knowledge): API-ingest adapter (kind:'api' sources) | OPEN - WS8c scope cut, stated not silent. `KnowledgeSourceDoc.kind` carries `'api'` on the schema (so a source CAN be declared this way) but `api/src/knowledge/crawl/runner.ts` refuses to execute it ("Fontes do tipo \"api\" ainda não têm execução implementada") rather than silently mis-running a JSON/SharePoint/WordPress REST endpoint through the anchor (HTML link-follow) crawler, which would produce a misleadingly "successful" empty result. No seeded source currently declares `kind: 'api'` |
| `b58a5854` + `c6d22e58` | 2026-06-27 / 2026-07-06 | feat(knowledge): per-source CA certs - fix ACT SharePoint TLS chain; fix(knowledge): auto-repair broken TLS chains via AIA caIssuers | OPEN - WS8c scope cut, stated not silent. Manual X.509 chain verification / AIA-caIssuers auto-repair (~460 lines upstream, `cortex/src/services/knowledge-tls.ts`) is security-sensitive code deserving independent review, deferred rather than ported under time pressure. Its absence DEGRADES rather than breaks the affected source (ACT still partially works per the live runtime data read this run); DGERT's wholesale 3046/3046-failed history is the likely-but-undiagnosed candidate this gap explains, so DGERT ships seeded disabled with that reason stated rather than silently re-crawled broken |
| `88a63c8f` | 2026-06-27 | feat(knowledge): ingest crawled PDF/office documents (officeparser) | OPEN - WS8c scope cut, stated not silent. `api/src/knowledge/crawl/extract.ts` recognizes a document response (`docExtFor`) but the crawl engine does not extract its text - a discovered PDF/DOCX/etc. is simply not ingested (no misleading empty-body row is written either). The existing standalone upload path (`POST /knowledge/uploads`, WS4a) already registers a binary file un-indexed with the same honesty, so this is a consistent posture, not a new one |

# rc-1 Release Hardening - Findings

Run: 2026-07-08, Fable-led verification (this run fixes nothing; each `bug` row has a patch brief
in `docs/release/patch-briefs/`). Verified at rc-1 product code (main = rc-1 + one tooling commit).
Evidence lives under `docs/release/evidence/`; probe kit under `docs/release/probes/`.

Known blockers from the build run's terminal verdict are NOT re-reported here: the e2e:server
committed-baseline debt (band1 web-dashboard specs, band2 retired-/api/v1/action specs, erp-fork
CUTOVER deferral) is already documented in RUN_LOG.md (2026-07-08 DEVIATIONs) and owned by
`docs/release/e2e-harness-remediation-brief.md`.

Class vocabulary (per mission): `bug` (product defect) | `harness-gap` (works, nothing automated
covers it) | `judgment` (works, result is poor) | `docs-gap`. Severity: high | medium | minor.

**Status column (added by the batch-final run, 2026-07-10):** `fixed-verified` (fix landed + green
committed test) | `fixed-untested` (test owed) | `partial` | `open` | `wont-fix-minor` |
`deferred`. Rows marked `-> batch-final sN` are this run's work queue; this table is the
operator's dashboard.

## Summary

27 findings across 9 journeys (numbered F1-F30; the umbrella rows F15/F17/F18 were folded into the
granular Boot-B findings F20-F27). By class: **18 `bug`, 4 `judgment`, 3 `docs-gap`, 1 `harness-gap`,
plus F19 = a verified PASS (billing) recorded for the trail.** By severity: **10 high, ~10 medium, the
rest minor.**

The high-severity set, ranked by operator impact:
1. **F16 + F28 (J3, the priority journey):** builds report `completed` but serve the untouched
   scaffold placeholder - the real generated app lands in an unserved file, the manifest entrypoint is
   never edited - AND per-build verification passes the scaffold and bills for it. The build-to-served
   product does not deliver on its core promise, and its safety net does not catch that.
2. **F2 (LLM egress):** no credential-provisioning surface at all, and the default gateway topology
   401s even when credentialed - the platform cannot talk to a model without out-of-band wiring.
3. **F1 (auth):** the entire auth lifecycle past login/me is unimplemented; logout does not revoke.
4. **F20 (chat):** the terminal result is truncated and the truncated text is what gets PERSISTED as
   chat history - stored data corruption, not just a display bug.
5. **F21 (memory recall), F22 (memory UI dead on a schema violation), F26 (anon reply shows the token
   not the NIF), F25 (host-context bleed - high IF reproduced against prod).**

What is genuinely SOUND (verified, not findings): tenant isolation held everywhere probed (J5);
tokens-only anonymisation egress proven (32 audit rows + 9/9 chokepoint spec); billing arithmetic
reconciles to the token with no orphan rows (J9); the webhook admission plane is byte-exact to spec
(J8); deactivation is immediate and fail-closed at every plane (J1). The generated app CODE is correct
- the build system just serves the wrong file.

## Findings

| # | Journey | Class | Sev | Finding | Evidence | Suggested fix scope | Status (batch-final, 2026-07-10) |
|---|---------|-------|-----|---------|----------|---------------------|-----------------------------------|
| F1 | J1 auth | bug | high | Auth lifecycle beyond login/me is unimplemented: `refresh`, `logout`, `password`, `device`, `device/poll`, `device/approve`, `users/:id/password` all return raw HTML 404 despite being in the shared contract + spec ch03. Logout therefore has NO server-side revocation (a "logged-out" token stays valid to expiry; only admin deactivation revokes) and `passwordChangeRequired` can never be cleared. | J1-auth/j1-auth.json (b,c,h,i); J0-contract-sweep/sweep.json | Implement the auth router endpoints against the existing `auth/revocation.ts` machinery + jti revoke on logout; contract tests per endpoint. patch-briefs/F1 | **fixed-verified** (batch1-f1; tests/contract/auth.test.ts) |
| F2 | J2/J3 | bug | high | The model credential has NO provisioning surface: no HTTP route, no env var, no migration seeds `credentials/default`; a fresh boot is honestly un-credentialed and chat/build are dead. Worse, even WITH a credential the DEFAULT chokepoint topology cannot complete one turn: the SDK subprocess is pointed at the local gateway, whose `LLM_GATEWAY_API_KEY` auth is never provisioned on any boot path - 401 `Invalid or missing API key / JWT`. Working runs require `LLM_CHOKEPOINT_BASE_URL=https://api.anthropic.com` (bypassing the gateway plane). | J0-degradation/j0-degradation.json; J0-gateway-topology/preflight.md | Super-admin credential endpoint (ch06-consistent, write-only, audit-logged) + boot-time gateway-key provisioning so the default topology works; decide + document the sanctioned dev posture. patch-briefs/F2 | **fixed-verified** (batch1-f2; live turn green 2026-07-09) |
| F3 | J4/J6 | bug (narrowed from "audit dead" - that was REFUTED) | medium | Registo itself WORKS (Boot-B: 29+ rows, `memory_auto_extracted` + `anonymisation.egress-mask`, hash-chained, org-scoped reads correct at platform-crud.ts:80). The real gap: NO CRUD mutation is audit-logged - grep of every `logActivity` call site shows only execute/anonymisation/memory-extract/platform-integration/registo-read/commit-guard writers; org/user/knowledge/session create/update/delete write nothing. An org's admin audit surface is blind to admin actions. (Boot-A's "0 rows ever" = uncredentialed boot -> no egress/extract events + no CRUD logging.) | J5-isolation (registoX 0); J4-memory (registo=29); patch-brief code trace | Add `logActivity` at the CRUD mutation sites through the single chokepoint; contract test asserting a CRUD action appears in /registo. patch-briefs/F3 | **fixed-verified** (bf-f3; login+build+CRUD rows, metadata-only, org-scoped; LIVE Registo proof; targetIds wire fix) |
| F4 | J7 branding | bug | medium | `POST /api/v1/branding/research` is unimplemented (HTML 404) - the brand-research journey FAILS at step one. The branding save path also diverges from the contract: code mounts `PUT /api/v1/org/branding`, contract says `PUT /api/v1/branding` (HTML 404). | J0-degradation (research); J5-isolation (brandContract) | Either implement the research job (jobs infra exists) + alias the contract path, or amend the contract + spec; today it is a dead documented feature. patch-briefs/F4 | **fixed-verified** (batch1-routes; research + PUT /branding alias) |
| F5 | all | bug | medium | 31 of 167 contract-declared endpoints are unmounted, including FOUR whole domains never referenced in server.ts: `uploads` (chat `attachments` therefore cannot work), `app-assistant`, `integration-builder`, `ekoa-local` (incl. `/api/v1/events`). Partial gaps: memories bulk-delete + signals, knowledge sources PATCH/crawl/refresh-schedule, sessions seed-featured, triggers by-automation list, integrations `/:key/session` + provision-automations. The shared/ coverage gate exercises schemas, not mounts - so this never failed CI. | J0-contract-sweep/sweep.json (167/135/31; oauth-callback row is a false positive) | Triage table per domain: implement, or explicitly de-scope from the contract with spec annotation. Add a mount-coverage contract test so declared-vs-mounted drift fails CI. patch-briefs/F5 | **fixed-verified** (UI-called subset mounted + mount-coverage drift gate; de-scoped domains stay unmounted by decision) |
| F6 | all | bug | medium | No JSON catch-all 404: any unmounted `/api/v1/*` path returns Express default HTML, violating the QA-block invariant that every non-2xx body validates against the shared error envelope. Also, the ACCOUNT_DISABLED message differs by plane (REST: "A sua conta está bloqueada..."; SSE: generic "Não autorizado."). | J1-auth (refresh/logout bodies); sweep body-kind column | Terminal JSON-envelope 404 middleware after all routers + unify the disabled-account message; envelope contract test for unknown paths. patch-briefs/F6 | **fixed-verified** (terminal /api/v1 JSON-404; scoped per decision) |
| F7 | J3 build | judgment | medium | A FAILED build still registers + serves the owner a 200 scaffold shell referencing `bundle.css`/`bundle.js` that never built - a broken blank page with no failure state. (Anon correctly gets 410.) The build error IS persisted (api/src/agents/build.ts:228) but the /jobs/:id VIEW omits it (jobView, api/src/routes/jobs.ts:58-72) - the record looks cause-less to clients. | J0-degradation (build, servedpage, jobrecord) | Serve an honest "build falhou" state page for failed/unbuilt artifacts; return the persisted error in jobView. patch-briefs/F7 | **fixed-verified** (bf-f7; honest failed page + Job.error; stale-good wins; review approve) |
| F8 | J2/J3 | judgment | minor | Provider/credential error surfaces are not user-grade: chat streams the internal spec citation in English ("No model credential configured ... (ch06 §6.2)"), the adapter leaks "Claude Code returned an error result" + raw provider JSON to the end user, and build failure is a generic PT sentence with no cause. | J0-degradation (chat error); J0-gateway-topology/preflight.md | One error-mapping layer at the streaming sink: PT user-grade message + machine code; keep detail in logs. patch-briefs/F8 | **deferred** (minor; error-mapping layer owed post-batch) |
| F9 | J8 | bug | minor | Trigger disable (410 TRIGGER_DISABLED) is unreachable over the API: triggers expose only GET/POST/DELETE, no disable/patch - the 410 path exists only in unit tests. | J8-webhooks/j8-webhooks.json (J8f) | Add PATCH /triggers/:id {active} (or document delete-only lifecycle + remove the dead path). patch-briefs/F9 | **wont-fix-minor** (decide-not-fix, batch-final) |
| F10 | J6 | bug | medium | The ch17 per-org deny-list is dead in production wiring: `setRulesetResolver` is never called in server.ts, so every org runs the default EMPTY deny-list; there is no HTTP surface to manage deny-list parties. Additionally NER is the inert default, so only checksum-VALID PT structured IDs are masked - free-text names and checksum-invalid IDs go out in cleartext (the invalid-checksum pass-through is per §17.5 by design; the unwired resolver is not). | scout-verified code trace (api/src/llm/anonymise/index.ts:75, server.ts); Boot-B J6 shows what IS caught | Wire a real ruleset resolver (org settings-backed) + a minimal deny-list CRUD; document the NER posture honestly in ops docs. patch-briefs/F10 | **fixed-verified** (bf-f10; resolver wired + org-admin CRUD + egress test + LIVE deny-list masking proof; 2 reviewers) |
| F11 | J1 sessions | bug | medium | Session rename silently no-ops (contract PATCHes `name`, store field is `title` - no translation) and session responses omit the contract-required `createdAt`/`updatedAt`. Pre-found: a complete draft fix + contract test was left UNCOMMITTED on the tree by a prior session; preserved as `stash@{0}` ("pre-hardening-run: ..."). | git stash@{0}; shared/src vs api/src/services/platform-crud.ts | Apply/adapt the stashed fix + its contract test in a patch run (do not blind-pop; re-verify against rc-1). patch-briefs/F11 | **fixed-verified** (da0d0fa live fix + 11-case contract test; stash@{0} superseded -> archived as archive/stash-f11-pre-hardening + dropped) |
| F12 | docs | docs-gap | minor | LANDING.md is referenced as a run ground-truth doc but does not exist anywhere in the repo. | repo find | Either write it (post-run landing summary) or drop references. | **deferred** (docs-gap; per-run LANDING.md now exists under docs/autothing/runs/) |
| F13 | docs | docs-gap | minor | Stale module header: `api/src/llm/credentials.ts` still says the credential is a "Firestore singleton"; the store is Mongo (Amendment 2). | docs-runbook worker report | One-line comment fix riding any F2 patch. | **fixed-verified** (rode batch1-f2) |
| F14 | J8 | harness-gap | minor | The served-app owner bypass accepts BOTH `Authorization: Bearer` and `?token=`; the committed suite asserts only `?token=`. Untested accepted-auth surface. | worker-boot-a surprise (9); api/tests/contract/served-app.test.ts | Add the Bearer-variant assertion to the served-app contract suite. | **deferred** (harness-gap minor) |
| F16 | J3 build | bug | high | THE PRIORITY JOURNEY'S HEADLINE DEFECT. Both builds reported `completed` but the SERVED app at /apps/<slug>/ is the untouched Ekoa scaffold placeholder ("Your app is being created... Powered by Ekoa"), not the requested app. The model wrote the real, correct app into a standalone `pessoa.html` at project root - which is NEVER the served entrypoint - and never edited `frontend/src/App.jsx` (the manifest entrypoint that compiles to the served `dist/bundle.js`). Render confirmed: 0 buttons, 0 Pessoa content, scaffold copy. The job claims success; the user gets a placeholder; the real work is orphaned. | J3-build/build1-analysis.json, j3-render-v1.json (screenshot), build2-verify-theater.md | Constrain the build agent to the manifest entrypoint (edit App.jsx, rebuild dist) or reject builds that leave dist unchanged/orphan standalone files; honest-completion gate. patch-briefs/F16 | **fixed-verified** (batch1-f16-f28; BUILD_UNFULFILLED terminal + tests; live J3 re-proof = batch-final s6) |
| F28 | J3 verify | bug | high | Per-build verification (verifyBuilds=ON) is FALSE ASSURANCE. Build#2 ran with verification on: the banner "A testar a aplicação..." showed, `build-verify` billed 8155 tokens, and verification PASSED (no verifyNote). But the served bundle it drove was still the scaffold placeholder (dist/bundle.js rebuilt from the untouched scaffold App.jsx; the counter went into the orphaned pessoa.html again). The gate that exists to catch F16 passed a scaffold as a working app and charged the user for it. | J3-build/build2-verify-theater.md, build2.json | verify-runner must assert the served app fulfils the request (not just loads); a scaffold/placeholder served page must FAIL verification. patch-briefs/F28 | **fixed-verified** (batch1-f16-f28; VERIFY_FAILED gates completion + request threaded; live J3 re-proof = batch-final s6) |
| F19 | J9 billing | PASS (with 2 notes) | - | Billing arithmetic verified across all logged model calls: every call attributed to the right billee with the right agentType tag (chat, memory-extract, build, build-verify, classify-in-build-intent); weights per tier applied; no orphan/empty-billee rows; health meteringAnomalies=0, gatewayUnmetered=0 throughout. Note 1: dev creditTokensPerUsd yields amountUsd=0 on small runs (expected, not an error). Note 2: auto memory-extract fires + bills a memory-extract row after EVERY chat/build turn (by design) - a cost surface worth documenting. | J9-billing/actions-log-*.json; build2.json (typeCounts); health | (verification pass - no fix) | n/a (verified PASS) |
| F20 | J2 chat | bug | high | The chat run's terminal `complete.result` carries only a ~25-char TAIL of the answer; the accumulated streamed text is clobbered by the SDK result field (api/src/llm/client.ts:365, :694). This tail flows to the completion frame, the run snapshot (`GET /chat/runs/:id`), AND the PERSISTED assistant message (agents/chat.ts:200) - so stored chat history is corrupted, not just the wire frame. Reproduced across two runs. | J2-grounding/j2-grounding.json (completeTail) | In the chokepoint, prefer the accumulated deltas over the SDK result field. Contract/e2e assertion that result == concatenated chunks + persisted message is complete. patch-briefs/F20 | **fixed-verified** (batch1-f20; persisted == concatenated chunks) |
| F21 | J4 memory | bug | high [CAUSE-UNCONFIRMED] | Memory recall failed end-to-end in the observed run: a fact stored as an `extracted` memory in session A was NOT reflected in session B ("não encontrei informação sobre o nome da tua empresa"). BUT the injection path is CODE-CORRECT for this case: injection is wired for chat (agents/context.ts:72), the memory is scoped-visible, and the term-overlap scorer HITS on "empresa" - so `resolveMemoryInjection` WOULD render the fact into the system prompt. The miss is therefore likely model-behavior (the model consulted host context per F25 instead of the injected block) OR an extraction-timing race (fire-and-forget persist vs session-B assembly), NOT a scoring bug. | J4-memory/j4-memory.json (turnB, extracted) | Confirm-first: a unit test asserting the fact lands in the assembled prompt will PASS and redirect the fix to F25/model-priority or the timing race. patch-briefs/F21 | **fixed-verified** (wired at HEAD agents/context.ts:73 <- chat.ts:119; wiring test BACKFILLED batch-final s0: tests/memory/recall-wiring.test.ts; live loop re-proof j4 = s6) |
| F22 | J4 memory | bug | high | `GET /memories` responses violate the shared `Memory` schema: `memoryView` (api/src/memory/resolver.ts) omits required `orgId` and returns `tags: undefined` for tag-less memories. The web `/memory` page rejects every item and renders 0 cards despite 4 memories existing via API. The contract suite never caught it (Memory shape not exercised = coverage gap). | J4-memory/j4-ui-rootcause.json, memory-page.png | Include `orgId` + default `tags: []` in memoryView; add the Memory response shape to the contract suite. patch-briefs/F22 | **fixed-verified** (batch1-f22; contract/memories.test.ts) |
| F23 | J4 web | bug | medium | The `/memory` page emits 7 console errors (QA bar is zero): 404s to unmounted `GET /memories/stats`, `GET /memories/tags` (fall through to `/:id` NOT_FOUND) and `POST /auth/refresh` (F1). This is the live web consequence of the F5/F1 unmounted-endpoint gaps. | J4-memory/console-404-urls.json | Mount the endpoints (ties to F5) or stop the client calling them; assert zero console errors on /memory (QA block). | **fixed-verified** (via F1+F5 mounts; /memory console-clean asserted in batch1-routes) |
| F24 | J4 memory | judgment | minor | One of two extracted memories has content `**` (a markdown parse artifact) instead of a fact; extraction can persist junk memories. | J4-memory/j4-memory.json (extracted) | Harden the extraction parser against empty/markdown-only captures. | **wont-fix-minor** (decide-not-fix, batch-final) |
| F25 | J4 / security | bug | high [IF-REPRODUCED] | Cross-context bleed: an m-u1 tenant chat reply surfaced the OPERATOR's host context - the local path `~/dev/ekoa-code` and the operator's Claude Code auto-memory ("Ekoa rebuild"). NOT a confirmed prod leak. Static posture already blocks the obvious vectors: `settingSources:[]` (no host CLAUDE.md load) and chat allowedTools are knowledge-only. The inherited cwd explains the `~/dev/ekoa-code` string (buildSubprocessEnv sets no HOME/cwd for CHAT, unlike build), but the "Ekoa rebuild" operator-memory reference is NOT explained by static code and needs reproduction against the prod sandbox before being called a leak. | J4-memory/context-bleed.json | Reproduce-or-dismiss against prod posture FIRST; if reproduced, isolate the chat subprocess cwd/HOME + confirm no host-memory injection into tenant runs. patch-briefs/F25 | **fixed-verified** (mechanism reproduced+hardened+tested; re-fix af8b556; accepted residual + disposition doc committed; REMOTE tag update = operator action; live leg rides s6) |
| F26 | J6 anon | bug | high | Anonymisation de-anonymisation round-trip is broken for the user-visible reply: the model reformatted the token `200000005` as `200 000 005` (thousands spaces), defeating the exact-string de-tokenizer, so the user sees the synthetic TOKEN, not their real NIF. Privacy HELD (the token never left egress - 32 audit rows + the 9/9 anonymise-chokepoint spec prove tokens-only egress), but the returned answer is wrong. | J6-anonymisation/roundtrip-rootcause.json, chokepoint-vitest.json | Format-tolerant de-tokenizer (match a token ignoring internal whitespace/separators) in the streaming detok path. patch-briefs/F26 | **fixed-verified** (bf-f26; whitespace/format-tolerant detok; 4 review rounds + 13k-case security property) |
| F27 | J6 registo | docs-gap | minor | `GET /registo?type=anonymisation` returns 0 rows because entries are category `anonymisation` / action `egress-mask` and the bare `type` filter matches at a different granularity; the qualified query returns all 32. Filter-granularity confusion, not a missing row. | J6-anonymisation/j6-anonymisation.json (auditFilter) | Document or normalize the registo `type` filter granularity. | **wont-fix-minor** (decide-not-fix, batch-final) |
| F29 | J8 automation | bug | high | `POST /api/v1/automations/plan` (plan-from-goal - the natural-language automation-authoring path) deterministically 500s (`{code:INTERNAL, "Erro interno."}`, 2 identical attempts). Isolated: manual `POST /automations` = 201 and a direct `POST /automations/:id/runs` = completed as the same bc-adm, so the gate/persist/engine are fine - the fault is planner/rehearsal-specific. An `automation-plan` billing row IS minted, so the planner MODEL CALL ran; the throw is downstream (planner-result cross-validate/persist, or the kind:'rehearsal' run-start in service.ts startRunInternal). The `handle` wrapper (routes/automations.ts:60) swallows the real cause to a generic envelope - the true error is only on the api server's stderr. | J8-webhooks/j8b-automation.json (planFromGoal) | Reproduce with server stderr captured; fix the downstream planner/rehearsal throw; stop the handle wrapper from masking 500 causes (log the real error). patch-briefs/F29 | **fixed-verified** (bf-f29; structured plan_failed 200+reason; review approve) |
| F30 | J3 build | judgment | minor | Builds do NOT emit a `memory-extract` billing row (bc-u1 billed build/build-verify/classify only), contradicting the expected "builds auto-run memory-extract post-run". Either build autoExtract is off or build extraction is not metered - worth confirming which. | J3-build/build2.json (typeCounts); J9-billing | Confirm whether build post-run extraction runs + is metered; align with the chat path or document the difference. | **wont-fix-minor** (decide-not-fix, batch-final) |

## Post-batch-1 open items (2026-07-10, found on main outside the tagged slices)

| Item | Status | Notes |
|------|--------|-------|
| 502-masks-401: terminal credential rejection reported as retryable 502 (gateway.ts catch-all) | **CLOSED (consumer-run s7, 2026-07-11)** | typed CredentialError -> 503 credential_error (non-retryable), rate-cap -> 429, transient stays 502; /health claudeAuth.lastProviderError carries class+timestamp (no bodies/secrets); provider rejection reasons logged (bridge/server.ts); metadata allowlist (user_id only) kills the sibling mask |
| F2 gateway `apikey` principal skips checkAllowance, bills platform admin (exfil surface from build subprocess) | **open (deferred, batch-2)** | RUN_LOG batch-1 final-phase security observation |
| `/usage` dashboard crash (undefined `.toLocaleString()`; billing endpoints 200) | **open (deferred)** | known bug the operator will hit; fix owed post-batch |
| Login landing double-creates sessions (React StrictMode dev double-mount) | **open (deferred, dev-only)** | |
| Chat-SSE late-subscriber gap / run hangs on upstream auth failure / temp-session 404 persist | **open (deferred, batch-2)** | S1 adversarial-tester discovery findings |
| schema-coverage gate is an honor-system allowlist (27 false COVERED keys; 6 endpoint groups return schema-violating bodies) | **open (deferred, structural)** | registoEntry.targetIds wire fix lands in batch-final s3 |
| web/shared `SourceInput` divergence (seed-template source 400s from UI) | **open (deferred)** | |
| web `__tests__` excluded from tsc (web test files never typechecked) | **open (deferred, batch-2)** | |
| Remote tag `batch1-f25` still points at broken 8a2a67b | **operator action** | `git push origin +refs/tags/batch1-f25:refs/tags/batch1-f25` (local re-pointed to af8b556, batch-final s0) |

## Journey verdicts

| Journey | Verdict |
|---------|---------|
| J1 auth lifecycle | PARTIAL FAIL - login/me/deactivation planes correct (fail-closed epoch bump; REST+SSE+me all 403 ACCOUNT_DISABLED; Boot-B confirmed the served-app plane too: deactivated owner -> 403 ACCOUNT_DISABLED on /api/app-data, anon -> 410, reactivation restores); refresh/logout/password/device absent (F1). |
| J2 grounded chat | PASS on grounding - the reply carried BOTH seeded org-private facts (grounding works, via 4 knowledge tool_event frames not context injection); notifications pushed usage_updated; turn metered + persisted. One bug: truncated complete.result (F20). |
| J3 build to served app | FAIL (the priority journey). Both builds report completed but serve the untouched scaffold placeholder; the real app is orphaned in an unserved pessoa.html (F16). Build-verify ON did NOT catch it - it passed a scaffold and billed for the pass (F28). Mechanics that DO work: SSE lifecycle, artifact/git-snapshot creation, GitHub honest-off, app-data plane (201/readback), verify banner + build-verify billing, sharing default (anon 410). The generated code itself is correct - it just lands in the wrong file. |
| J4 memory loop | FAIL - extraction + privacy isolation work (m-u2 sees none of m-u1's; 404 on cross-user get), but recall into the next session fails (F21), the /memory UI renders 0 cards on a schema violation (F22) with 7 console errors (F23), a junk memory was stored (F24), and a caveated host-context bleed appeared (F25). |
| J5 org isolation | PASS end to end - knowledge/memories cross-org 404s exact; design-tokens neutral default (#0F766E) correct AND per-org overlay resolves (PUT brand #AB2244 -> ?app=<slug> serves it); Registo confirmed working + org-scoped in Boot B (F3 refuted, narrowed to CRUD-logging gap). |
| J6 anonymisation | PARTIAL - privacy HELD (tokens-only egress proven: 32 audit rows + 9/9 chokepoint spec), but the user-visible de-anon round-trip is broken by model whitespace reformatting (F26); per-org deny-list still unwired (F10); audit filter granularity (F27). |
| J7 brand research | FAIL - endpoint unimplemented (F4) |
| J8 automation + webhooks | MIXED. Admission plane PASS (HMAC/dedup/401/404 byte-exact; dedup = raw-body sha256; disable path unreachable - F9). Webhook->run pipeline PASS on a manually-created automation (trigger 201, valid-HMAC fire 200 accepted, spawned run reached completed). But the plan-from-goal authoring path (`POST /automations/plan`) deterministically 500s (F29) - the planner model call runs and bills, then a downstream throw is swallowed to a generic envelope. So webhooks work; NL automation authoring is broken. |
| J9 billing truth | PASS - Sigma(per-user) == Sigma(per-agentType) == 37387 metered tokens, exact; every call attributed with a legit tag; no orphan/empty-billee row; anomalies=0, unmetered=0. Build attribution precise (bc-u1 = build+build-verify+classify). See j9-reconciliation.md. |

## Positive verifications worth recording (not findings)

- Webhook HMAC scheme byte-exact to spec: `sha256=` prefix accepted, `sha1=` rejected, raw-body
  signature base, dedup on body hash with `{duplicate:true}` 200.
- Deactivation is immediate and fail-closed at every tested plane; token-epoch bump invalidates
  outstanding tokens; uniform-404 on cross-org ownership misses held everywhere probed.
- Billing zero-state + metering counters clean on an idle credentialed boot.
- A no-step automation runs end-to-end credential-less: the webhook -> queue -> run pipeline has no
  hidden model dependency.
- Design-tokens endpoint: neutral default (#0F766E), ETag + 304, never-5xx behavior as designed.

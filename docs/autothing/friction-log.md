- 2026-07-08T09:11:32Z [autothing/e2e] SIGKILLing the e2e-with-server parent orphans its playwright chromium children (12 leftover), which then starve later ci:lane runs of CPU -> add explicit child-browser cleanup on e2e teardown, or sweep ms-playwright orphans before a fresh lane.
- 2026-07-11 run 20260711-053853-0c6e0041: the three Phase-1 Explore subagents (Haiku) were unresponsive for ~3h and delivered AFTER the run completed; the lead fixed forward by doing recon inline (reports later confirmed all facts, zero contradictions). Same session, the code-review skill's 8-finder Agent fan-out was replaced with an inline review for the same reason. Also: the first GLOBAL GATE verdict print did not release the Stop hook (line contained backticks/em-dash + prose between the runId and videos tokens); a plain reprint matched. Consider hardening the verdict-line guidance to "one plain ASCII line".

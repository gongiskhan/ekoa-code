- 2026-07-12T16:39:27Z [autothing] Subagent final messages (Explore/Plan/review agents) never deliver back to the lead in this session — agents run and finish (their tool output reaches task .output files) but verdict messages are lost; run_in_background:false also spawns async → suggested prose fix: build-loop should instruct EVERY gate agent to WRITE its verdict/report to a file under the runDir as the primary channel, message as secondary; lead polls the file.
- 2026-07-17T14:59:15Z [autothing-walkthrough] walkthrough record.mjs 'assert: selector not uniquely resolvable' when a testid matches multiple rows -> the skill should note in storyboard-schema that data-testid asserts on list rows must be scoped (tr:has-text(label) [data-testid=...]) since a shared testid across rows is ambiguous.
- 2026-07-17T14:59:15Z [autothing-test] the full api vitest lane (~700-1000s serial, fileParallelism:false) intermittently flaked one contract file with 'TypeError: fetch failed' under machine load (dev stack + walkthrough recorder + concurrent mongods) - passed 5/5 in isolation. autothing-test could suggest quieting the machine (stop the dev stack) before the run-level full lane, or the note is already in known-flakes.
- 2026-07-17T14:59:15Z [autothing-codex-checkpoint] no OPENAI_API_KEY available; codex ran on ChatGPT auth (interactive-pool risk). The skill mandates API-key auth but has no self-unblock path when a key genuinely isn't provisioned - it should state the fallback (proceed on ChatGPT auth with serial+scoped calls + a logged DECISION) more explicitly rather than implying it's a hard blocker.
- 2026-07-17T14:59:15Z [autothing] mutation testing on a heavy TS monorepo (190 serial vitest files, bcrypt cost 12) is impractical whole-repo (648 mutants x multi-min full-run); the skill should acknowledge that a SCOPED mutation run on the fastest-testable changed module is the honest fallback when a full campaign would take days, and how to record it.
- 2026-07-17T19:48:03Z [autothing-plan] 5 parallel Explore subagents (Fable-pinned, very-thorough briefs) returned nothing for 46 min and ignored a SendMessage status ping; lead had to proceed to Plan agents self-exploring → suggest the skill recommend fewer/smaller Explore briefs (or a per-agent deadline + fallback) for mega-briefs.
- 2026-07-30T21:18:43Z [autothing] Lead ran `npm run typecheck 2>&1 | tail -1; echo tc:$?` - the pipe made $? the exit of `tail`, reporting tc:0 over a REAL failure that a subagent later caught. -> autothing/build-loop should state: never read a gate's exit code through a pipe; capture to a file (`cmd > /tmp/x.log 2>&1; echo $?`) or use PIPESTATUS.
- 2026-07-30T21:18:43Z [autothing] The per-slice codex gate died on auth (revoked ChatGPT refresh token, no API key) at the FIRST slice; the preflight doctor checked `codex --version` only. -> preflight should run a cheap codex AUTH ping (not just binary presence) so credential death is found at minute zero, which is exactly what Part 12.4 promises.
- 2026-07-30T22:41:14Z [autothing] A slice fix was re-verified only on the path the finding named; the SAME vulnerability class was live one file away (note paths checked at file granularity, the index db file not). -> autothing/build-loop should tell the re-review to enumerate EVERY path of the finding's class in the module, not just re-probe the reported repro.
- 2026-07-31T01:02:53Z [autothing] A slice's per-slice wall ran only the workspace it edited (api), so a RED baseline in shared/ (domain-count pin + AuthClass enum, broken by E1/E2) survived three slices undetected; the next implementer found it. -> autothing/build-loop's deterministic wall must run EVERY workspace's suite (npm test at the root / each workspace), not just the touched one - a monorepo slice can redden a sibling workspace it never edited.
- 2026-07-31T03:47:46Z [autothing] The lead verified a generated artifact by running its gate against the DIRTY working tree (contaminated by a concurrent slice's uncommitted edit), and treated exit 0 as proof. -> autothing/build-loop must require that any COMMITTED GENERATED artifact is verified from a pristine checkout (git archive/worktree of the commit), never from the working directory - a gate that reads a gitignored build output cannot see contamination.
- 2026-07-31T12:40:10Z [autothing] The lead committed with a FAILING typecheck: it ran several gates on one line (`npm run typecheck >/dev/null 2>&1; echo $?; npm run lint ...`), read a 2 in the scroll-back, and proceeded anyway because the later gates were green. -> a gate's non-zero exit must HALT the commit, not be one number among several; autothing/build-loop should require the wall to be a single command whose failure blocks, rather than a sequence the lead eyeballs.
- 2026-08-01T19:14:06Z [autothing-implement] Delegated slice agents told to "verify via targeted vitest + eslint (skip full tsc to avoid cross-talk)" passed vitest but shipped strict-null/implicit-any tsc errors TWICE (CS2 TS7016 .mjs import; CS3 TS2532). vitest/esbuild strips types, so vitest-green != typecheck-green. → When a full tsc is deferred for parallel-tree reasons, the orchestrator MUST run the authoritative full typecheck at gate time (it did, and caught both) AND the delegated brief should require at least `tsc --noEmit` scoped to the slice's files, or the agent should run the project typecheck in a git-worktree copy so cross-talk isn't a reason to skip it.
- 2026-08-01T19:14:06Z [autothing] codex cross-model gate (per-slice + checkpoint) is dead run-wide (ChatGPT OAuth refresh_token_reused). The skill assumes `codex login`/API-key auth is arranged; on this machine it was neither, and there is no in-run remediation. → autothing Phase 0 preflight should probe `codex exec` liveness (a trivial call) and, if dead, surface the reduced-bar consequence to the operator IMMEDIATELY at run start rather than at the first slice gate, so a Codex API key can be supplied before hours of building accrue an unverifiable cross-model bar.
- 2026-08-01T19:14:06Z [autothing-plan] Two safety-critical parser/verification slices (CS1, CS3) each needed 2-3 adversarial-review rounds because the load-bearing invariant (never a false-empty / never a truncated-pass-as-complete) is exactly the kind conventional unit tests pass while the invariant is broken. → planning could pre-seed such slices' acceptance with the adversarial inputs (a "must return ok:false for: login/WAF/session/filter chrome" table) so attempt 1 targets them, saving review rounds.
- 2026-08-03T08:32:07Z [autothing-adversarial-review] A verifier subagent wrote its throwaway fuzz harness INTO the repo tree (api/tests/legal/zzz-fuzz-scratch.test.ts), which broke a CONCURRENT slice's typecheck verification (exit 2, one error, in the foreign scratch file) and briefly read as the other slice's regression → the review/verify briefs should mandate scratch files live OUTSIDE the repo (/tmp) or in a git worktree, never in the working tree, whenever the run is parallel.
- 2026-08-03T09:19:52Z [autothing-parallel-work] Two parallel implement-agents both append to docs/decisions.md and docs/diagrams/*.excalidraw, so whichever commits first sweeps the other's uncommitted doc entries into ITS commit (happened B2/D2: eb03388 carried D2's ledger entries + diagram element). Nothing was lost, but attribution is wrong in git history → the disjoint-files rule should explicitly cover APPEND-ONLY shared doc files, e.g. per-slice fragment files merged by the orchestrator, or an orchestrator-owned commit for docs.
- 2026-08-03T10:42:14Z [autothing-parallel-work] THIRD occurrence: concurrent agents sharing one git index keep swallowing each other's staged/untracked files (a5970f8 took 7 of C2's new files; 54e9131 took CS6's decisions entries; a6810f4 took B2's index.ts hunk and briefly left main exporting a symbol service.ts did not define). Agents handled it correctly (commit-tree plumbing, hunk-by-hunk staging, refusing to rewrite history under a concurrent commit) but the cost is real: wrong attribution and a transiently broken main → parallel implement-agents should either commit through a per-agent temp index by default, or the orchestrator should serialise the commit step itself.
- 2026-08-03T12:13:23Z [autothing] FIFTH attribution incident, and the ORCHESTRATOR caused this one: I ran 'git add docs/findings.md && git commit', which commits the WHOLE INDEX, not the path I added — so a5121df ("docs(findings): …") silently swallowed slice D1's entire capability surface (14 files: integration-capability.ts, the routes, server wiring, 4 test suites, the regenerated OpenAPI + CLI client, diagram 02). Nothing lost, but the commit message is now actively misleading about what landed. I had logged this exact class against three subagents and then did it myself → the orchestrator must use the same discipline it demands: 'git commit -- <paths>' or a temp index, never 'git add' followed by a bare commit, in a shared tree.
- 2026-08-03T12:57:23Z [autothing] The orchestrator's own 'clean worktree' regression was NOT clean: symlinking node_modules from the main checkout makes npm's workspace links (node_modules/@ekoa/{shared,api}) resolve back to the MAIN tree, so the run measured a hybrid of worktree test files and main-tree source — including another agent's uncommitted work. Detected only because gate:openapi reported drift for a descriptor that does not exist in the worktree's own shared/. This is the SECOND self-inflicted instance of the 'a gate measured against a dirty tree proves nothing' class this session → a verification worktree needs a real install (or at minimum the workspace links replaced with worktree-local ones); symlinking node_modules wholesale silently defeats the isolation it is there to provide.

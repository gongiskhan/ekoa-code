# Flow Plan - run 20260708-203034-41fe4774 - batch-1 testable app off rc-1

Binding inputs: the operator batch-1 brief (conversation) + `docs/release/patch-briefs/{F2,F16,F28,F20,F1,F22,F4,F5,F6,F25}*.md` + `docs/release/FINDINGS.md`. Implement reads each slice's brief FULLY before coding it. Profile: **feature** (operator-forced). Branch `batch1-testable` @ b8b92f4. Slices are SERIAL in this order (spine first, meter-aware); tags are binding; one checkpoint commit + tag per slice; regression-test-FIRST (failing test committed, then green).

## Slices

| # | Slice ID | Title | Kind | Area skill | Parallel | Status | Tag |
|---|----------|-------|------|-----------|----------|--------|-----|
| 1 | f2-credentials | F2: credential endpoint + gateway-key boot provisioning | api | ekoa-architecture (SECURITY-BOUNDARY: egress/auth) | serial | in_progress-paused | batch1-f2 |
| 2 | f16-f28-honest-build | F16+F28: honest completion gate + request-fulfilment verifier (2 commits) | api | ekoa-architecture | serial | pending | batch1-f16-f28 |
| 3 | f20-stream-text | F20: streamed text over SDK result tail | api | ekoa-architecture (llm/ file, but mechanical) | serial | pending | batch1-f20 |
| 4 | f1-auth-lifecycle | F1: refresh/logout/password/device + super-admin password | api | ekoa-architecture (SECURITY-BOUNDARY: auth) | serial | pending | batch1-f1 |
| 5 | f22-memory-view | F22: memoryView orgId + tags/tier defaults | api | ekoa-architecture | serial | pending | batch1-f22 |
| 6 | routes-ui-called | Scoped F4+F5 UI-called mounts + F6 JSON-404 envelope | mixed | ekoa-architecture (SECURITY-BOUNDARY: new authz surface) | serial | pending | batch1-routes |
| 7 | f25-disposition | F25: host-context-bleed reproduce-or-dismiss (MANDATORY) | api | ekoa-architecture (SECURITY-BOUNDARY: egress/isolation) | serial | pending | batch1-f25 |

## Acceptance per slice (explorer-verified anchors)

- **f2-credentials** (brief F2, both halves + F13 rider):
  (a) super-admin, WRITE-ONLY, audit-logged `POST /api/v1/credentials` (new router; mount pattern `server.ts:470-506`; `requireRole('super-admin')` per `routes/billing.ts:31`; audit via `logActivity` `data/activity.ts:21`, type `credential.set`) calling `setCredential` (`llm/credentials.ts:140`). NEW shared schema (CredentialSetRequest/Response) + contract test (schema-coverage gate: cover or count-adjust).
  (b) boot-time gateway-key provisioning so the DEFAULT topology (LLM_CHOKEPOINT_BASE_URL unset -> local gateway `config.ts:162`) self-authenticates: `buildSubprocessEnv` (`credentials.ts:265-282`) currently presents the MODEL secret to a gateway that accepts only `gatewayApiKey` x-api-key or ekoa JWT (`gateway.ts:49-64`; 401s at :90/:127/:149); the gateway re-injects the model secret upstream (`client.ts:911`). Fix: boot provisions a gateway key when unset (server.ts bootState ~:545) and buildSubprocessEnv presents the gateway-acceptable credential when base URL is the local gateway. NO anthropic.com bypass; FIXED-13 intact.
  (c) F13 rider: fix stale comment `credentials.ts:3-4`.
  GATE: credential-endpoint contract test green (401/403 for non-super-admin, write-only - no read-back of secret); one chat turn completes end-to-end on default topology with `gatewayUnmeteredCalls` 0; regression test asserting subprocess auth admitted by gateway (extend `tests/llm/gateway.test.ts` + `tests/llm/credentials.test.ts`). Diagram: ch06 gateway/egress if topology contract changed (FIXED-12).

- **f16-f28-honest-build** (briefs F16+F28; TWO commits, one slice; the false-assurance pair):
  Commit 1 (F16): honest-completion gate in `agents/build.ts` (completion today is UNGATED - `:355` is note-assembly, only `finalizeOnce` guards `:356-359`; even bundle.ok=false completes). Add `mech.assertProgress` seam: `seams.ts:192-205` + `noopBuildMechanics` :207-221 + `build-mechanics.ts:75-209` + test `fakeMechanics` (`tests/agents/build.test.ts:20-33`). NOT-clean-completed when: `frontend/src/` subtree unchanged vs scaffold baseline (commit "Initial scaffold", `scaffold.ts:110`), OR dist/bundle.js scaffold-fingerprinted, especially with orphan top-level `*.html` (non-index.html returns null at `builder.ts:533` -> scaffold compiles). Plus build `customSystemPrompt` on `runAgent` (`build.ts:293-312`; wiring intact via `client.ts:644`/`:328`) naming the manifest entrypoint (`frontend/src/index.jsx` importing `App.jsx` - edit App.jsx) and forbidding standalone top-level HTML.
  Commit 2 (F28): thread request into `VerifyRunInput` - BOTH copies (`verify-runner.ts:26-32` AND `seams.ts:140-147`) - and `buildPrompt` (`verify-runner.ts:82-95`): assert served DOM is not the scaffold copy (markers: `scaffold-root`, "Let's build something that will change", "Powered by Ekoa" - `api/assets/scaffold-templates/App.jsx`) + expected elements from the request (`input.description` in scope at `build.ts:350`). `{ran:true,passed:false}` GATES completion (distinct non-success terminal); `{ran:false}` stays note-only (`verify-runner.ts:46`). REWRITE `tests/agents/build.test.ts:153-163` (it asserts the buggy ran+failed-still-completes).
  SHARED GATE: with one gate disabled, the other still independently fails a scaffold build (committed test proves each side alone); positive case - a build editing App.jsx completes clean and passes both. J3 priority-journey probe (`docs/release/probes/j3-build.mjs` over boot-b) serves a fulfilling app - run at slice end or defer the live probe to S7's boot if meter-tight (record which). Diagrams: ch05/ch07 build-serve + verification (FIXED-12).

- **f20-stream-text** (brief F20; `api/src/llm/client.ts` ONLY):
  Fix three clobber sites: `:365` (streamAgent `text = msg.result` -> keep accumulated, fall back only if nothing streamed), `:389` (oneShot parity), `:694` (runAgent `rawText = msg.text || rawText` -> `rawText = rawText || msg.text`). Persisted assistant message (`chat.ts:200` -> `persistence.ts:33`) and `complete` frame (`chat.ts:201` -> `streaming.ts:62-64`) equal the concatenated chunks. REWRITE `tests/agents/chat-lifecycle.test.ts:46-56` to the new semantics (deltas concatenate to the full answer); regression test: scripted stream whose concatenation != finalText, assert persisted + complete == concatenation (fake transport `_fake-transport.ts:56`).

- **f1-auth-lifecycle** (brief F1):
  Mount in `routes/auth.ts`: POST /refresh (re-sign `req.user` claims via `signToken` `jwt.ts:25`), POST /logout (`revoke(jti,...)` `revocation.ts:36`; `requireAuth` already checks `isRevoked` `middleware.ts:36` so /me 401s after), POST /password (NEW service fn in `auth/service.ts`: verify current, hash `password.ts:9`, clear `passwordChangeRequired` - read at `service.ts:80`), device/* (NET-NEW minimal device flow per shared contract: `shared/src/auth.ts` deviceStart :118 / devicePoll :124 / deviceApprove :131, DevicePollResponse discriminated union :66 - mongo-backed store, pending->approved/denied/expired, single-use, expiry+interval). `routes/users.ts`: POST /:id/password super-admin (`shared/src/users.ts:56`, set passwordChangeRequired true).
  GATE: NEW `tests/contract/auth.test.ts` - every endpoint's response safeParses against its shared schema; token rejected on GET /me after logout; the first-boot /auth/refresh 404 gone. Update `schema-coverage.test.ts` COVERED + EXPECTED_PENDING_COUNT (72 -> 65 if all 7 land). Contract-test activation note: `setActivation(id,{active:true,...})` or requireAuth fails closed.

- **f22-memory-view** (brief F22; `memory/resolver.ts` `memoryView` :27-29 only):
  Add `orgId: m.orgId`, `tags: m.tags ?? []`, PLUS `tier: m.tier ?? 'active'` (explorer-found: shared Memory requires tier :10 non-optional; UI create sends none -> still violates after brief's literal fix; extraction sets 'active' so 'active' is the honest default). Fixes all four routes via the one view (`routes/memories.ts:20,26,33,43`).
  GATE: NEW `tests/contract/memories.test.ts` - seed tagged, tag-less, AND tier-less memories; GET /memories + /memories/:id safeParse against shared Memory + MemoryListResponse (no coverage-count change - memories already COVERED via cross-org, which never body-validated). Web /memory page renders its cards (client CONTRACT_MISMATCH at `web/lib/api/core.ts:272-275` was the killer): verify via contract-validity + snapshot of the store path; full UI e2e optional if boot available.

- **routes-ui-called** (briefs F4 + F5 UI-facing subset + F6 404-envelope ONLY):
  F4: `POST /api/v1/branding/research` (org-admin; wire `agents/brand-research.ts:23` runBrandResearch; map contract `websiteUrl` (`shared/src/org.ts:43`) -> prompt; respond `BrandingResearchResponse {jobId}` :48 - NOT a job envelope) + `PUT /api/v1/branding` alias for the contract path (today only `/org/branding`, `org.ts:28`).
  F5 subset (ONLY these; every one has a live web caller): memories POST /bulk-delete (compose `deleteMemory`+`memoryWriteGuard` `resolver.ts:39,134`), POST /signals (honest minimal: contract-valid `MemorySignalResponse`, real adjustments where infra exists else zero-affected - per F5 brief), GET /stats + GET /tags (aggregate over `listVisibleMemories`; REGISTER BEFORE `GET /:id` at `memories.ts:23` or shadowed); knowledge PATCH /sources/:id (align `sourceView` `service.ts:53` to contract KnowledgeSource), POST+GET /sources/:id/crawl (no crawler infra: honest contract-valid minimal per F5 brief - never fake completed crawls), GET /refresh-schedule (`{schedule:null}` honest); integrations GET+POST /:key/session, POST /:key/provision-automations (no infra: honest contract-valid minimal per F5 brief). DE-SCOPED (stay unmounted): app-assistant, integration-builder, ekoa-local extras, agent-face, uploads, seed-featured, triggers-list.
  F6 (404-envelope part ONLY): terminal middleware `app.use('/api/v1', ...)` -> `sendError(res,'NOT_FOUND',...)` (`helpers.ts:16`) inserted after `server.ts:506`, before :512-514 app-serving; NOT on broader /api (would break /api/design-tokens.css :529, /api/demos, /api/m365 etc.). ACCOUNT_DISABLED message unification is OUT.
  GATE: operator pages (chat, build, memory, branding, knowledge, integrations) zero console 404s to these paths (web e2e or store-level probe); mount-coverage assertion over the newly-mounted set (design per explorer: unauthenticated probe - mounted router -> 401 envelope, unmounted -> 404 envelope; NOT the HTML heuristic, dead once the 404 envelope lands) + per-endpoint contract tests validating each new response against its shared schema; unmounted /api/v1/* returns ErrorEnvelope JSON not HTML. schema-coverage COVERED/count updates for each newly-covered descriptor.

- **f25-disposition** (brief F25; MANDATORY - batch not done without it):
  Reproduce against PROD sandbox posture (NOT dev cwd): isolated boot, tenant chat, check transcript for host-context strings - the `~/dev/ekoa-code` path AND the unexplained "Ekoa rebuild" operator-memory reference. Code facts: chat subprocess inherits API server cwd (`process.cwd()` - sdkOptions `client.ts:326` only sets when passed; chat passes none `chat.ts:139-154`) and HOME (`credentials.ts:279` only when homeDir passed); `settingSources: []` (`client.ts:320`) + knowledge-only tools (`tools.ts:38-39`) block the obvious vectors; build already isolates (`build.ts:301-302` cwd+homeDir=projectDir); brand-research + one-shot (`client.ts:753`) also pass neither.
  If NOT reproduced: dismissal doc with evidence + SHIP cwd/HOME isolation as defense-in-depth (chat/brand-research/one-shot get per-run sandbox cwd+HOME like build) + test asserting no host path reaches a tenant transcript. If reproduced: Critical - isolate, confirm no host-memory injection, regression-test; gates the batch.
  GATE: committed disposition verdict (reproduced+fixed+tested | dismissed-with-evidence+hardening-test) under docs/release/ or runDir with evidence pointer. Diagram: ch05 §5.4.1 subprocess-env if env/cwd contract changes (FIXED-12).

## Frozen invariants (every slice)
FIXED-13 chokepoint (no Anthropic client/base-URL literal/provider import outside `api/src/llm/`; no spawn with a non-chokepoint provider base URL); no weakening of gateway auth, tenant/org scoping, or the anonymisation egress path; regression-test-FIRST; import boundaries + module direction (routes never touch data/ directly - go through domain modules); suite ledger ratchets (green never -> red/skip); diagrams updated in-slice where structure/flow/shape changes (FIXED-12).

## Out of scope (do NOT pull forward)
Batch-2 e2e harness; F10, F26, F3, F9, F7, F24, F27; F6 beyond the 404 envelope; docs-gaps; de-scope-candidate F5 domains (app-assistant, integration-builder, ekoa-local extras, agent-face, uploads, seed-featured, triggers-list); e2e:server pre-existing baseline debt (documented in RUN_LOG DEVIATIONs 2026-07-08).

## Run mechanics
- feature profile: per-slice deterministic wall + committed tests + fresh-context review on EVERY slice; codexSliceReview on security-boundary slices (S1, S4, S6, S7 - S4 auth added by the Part 11.3 floor); deliberateRed/mutation OFF (profile); run-end codex checkpoint if credits available, degraded-and-logged if not, never blocking.
- api-kind slices: asciinema evidence; batched run-level independent-test pass; S6 is mixed (web-visible) -> per-slice independent test + design audit only if UI changed (it should not be - server-side mounts only; record kind-conditional skips).
- Meter-aware graceful landing: land at the last TAGGED slice; PAUSED RUN_LOG entry names the next action; F25 is mandatory - the batch is not "done" without it.
- Checkpoint format: `checkpoint: batch1 <sliceId> <title>` + the binding tag. RUN_LOG GATE entry per slice, UTC stamps. rc-1 tag untouched; NO merge to main.
- ci:lane concurrency flake: do not run ci:lane concurrently with docker builds/colima (known-flakes.md).

## Terminal output (operator contract)
Per-slice gate results + tags; F25 disposition with evidence pointer; whether J3 priority-journey probe now passes; one-line "what works now" (login/chat/build-serve/memory/branding pages); note: /config safeguard switch could not be restored programmatically (interactive-only setting) - operator must flip it.

## Decisions / assumptions ledger (planning)
1. NO RUN_SPEC: operator brief + patch briefs ARE the spec (operator-directed; alternative was a derived RUN_SPEC - pure duplication).
2. Plan-subagent step folded inline: slice split + design operator-prescribed; three very-thorough Explore reports verified every root cause at file:line. Alternative (spawn Plan agents) rejected: no degrees of freedom left.
3. F1 device flow: IMPLEMENT minimal per shared contract (operator brief lists device/* explicitly; schemas exist; alt was defer-with-dismissal - contradicts the brief).
4. F22 adds `tier: m.tier ?? 'active'` beyond the brief's literal fix (schema still violated without it; same function, same spirit).
5. F20 rewrites chat-lifecycle.test.ts:46-56 + F28 rewrites build.test.ts:153-163 (both encode the buggy behavior; test-bug class, logged not silent).
6. F16: completion found UNGATED (worse than brief) - the honest-completion gate is net-new, brief's fix scope still correct.
7. S6 endpoints lacking backing infra (signals, crawl, integrations session/provision) get honest contract-valid minimal implementations per the F5 brief's per-endpoint specs - never fabricated success states; de-scoped domains stay unmounted.
8. Slices SERIAL (shared api/src surface + meter-aware spine-first ordering; parallel fan-out would contend on server.ts/tests and reorder tags).
9. Turn cap 250 -> 560 (max(300, 80x7)).
10. Profile feature kept though 7 slices would size as build (operator-forced; codexSliceReview still runs on the 4 boundary slices; the floor never lowers).

# A2 — Track 6: Chat page inventory (input to Part B)

Run `20260717-190134-9d4c1cbf`, slice A2. Verified against branch `mega-run` working tree, 2026-07-17. Every claim cited `file:line`. Path correction up front: **there is no `web/src`** (FLOW_PLAN "Critical files" says `web/src` — wrong). The web app root is `web/` directly: `web/app/`, `web/components/`, `web/stores/`, `web/locales/`, `web/hooks/`, `web/lib/`.

## 1. Chat page: component tree, the two layouts, morphs, welcome cards, composer, mobile

One page owns everything: `web/app/(dashboard)/chat/[[...sessionId]]/page.tsx` (1901 lines), component `UnifiedChatPage` (page.tsx:156). Children imported at page.tsx:28-53:

- `SessionsPanel` (left history rail, desktop only, page.tsx:1500-1505) — `web/components/builder/sessions-panel.tsx`.
- `ChatPanel` (the transcript rail) — `web/components/builder/chat-panel.tsx` (1122 lines).
- `SidePanel` (the deliverables panel) — `web/components/builder/side-panel.tsx` (1041 lines).
- Empty-state pieces — `web/components/chat/empty-state.tsx` (`WelcomeMessageBubble` :239, `PromptSuggestionsStrip` :278, `ChatLoadingScreen` :190), `onboarding-card.tsx`, `onboarding-welcome.tsx`, `chat-stripes.tsx`, `animated-tagline.tsx`.
- Mobile drawers — `web/components/chat/mobile-sessions-drawer.tsx`, `mobile-side-panel-drawer.tsx`; `shortcuts-modal.tsx`.

**Three render branches** (not two layouts plus empty — the empty state IS the first layout):

1. **Empty state, full width** (page.tsx:1516-1707): date strip + `AnimatedTagline` header (1540-1547), `OnboardingCard` (1549), `LegalOnboardingCard` for the legal vertical (1551), `ChatStripes` (1553), its own composer (1573-1687), `PromptSuggestionsStrip` gated on `settings.chat.showExampleCards` (1689-1696), keyboard-hint footer (1699-1703). Onboarding-type sessions swap the header block for `OnboardingWelcome` (1530-1537). `showEmptyState` = no essential messages && loaded && !executing && no artifact context (page.tsx:572-596).
2. **Active conversation, chat-only**: rail takes `flex-1` full width (page.tsx:1723), no SidePanel.
3. **Active conversation, build/integrate**: rail fixed `w-full md:w-[380px] md:min-w-[320px] md:max-w-[420px]` (page.tsx:1722), SidePanel mounts `hidden md:flex flex-1` (page.tsx:1755-1759).

**The "width morph" surprise**: there is NO AnimatePresence width morph on the page. `AnimatePresence, motion` are imported (page.tsx:5) and never used in the file — dead import. The layout switch is a plain conditional className swap (page.tsx:1719-1724), unanimated. AnimatePresence lives only in: SidePanel's build-phase title crossfade (side-panel.tsx:592-605), the mobile drawers (mobile-side-panel-drawer.tsx:22, mobile-sessions-drawer.tsx:117), `animated-tagline.tsx:37`, and empty-state.tsx:205-230. B3's "kill width morph" is really "replace the className conditional and animate the panel entrance" — nothing to delete, something to add.

**Panel visibility logic** (the thing B3 replaces): `showSidePanel` = (`sidePanelState==='build'` && (isBuildSession || sessionHasJob || isExecuting)) || (`sidePanelState==='integrate'` && activeIntegrationBuild) (page.tsx:1214-1216). `isBuildSession` derives from `sessionJobs[id].artifactInstanceId` / `sessionPreviews[id].templateId` / `.appUrl` (page.tsx:1196-1203). Manual override `forceSidePanelOpen` (null/true/false, page.tsx:172, reset per session :525-530) wins via `sidePanelVisible = forceSidePanelOpen ?? showSidePanel` (page.tsx:1221). A floating open-button shows when closed (page.tsx:1743-1752). Note: `sessionHasPreview` (the BRIEF's name) does not exist as an identifier; the actual presence condition is this `showSidePanel` expression.

**Unified send router** (page.tsx:1223-1302): routing is a function of session data, not a mode flag — onboarding → `handleChatSend`; build session with job/artifact → `handleBuildSendMessage` (:706-736); build session without → `handleBuildFirstMessage` (:653-702); else `handleChatSend` (:921-1185). Queue-while-executing at :1231-1237, drained FIFO merged into one turn at :1309-1322.

**Chat run client flow** (`handleChatSend`): local-mirror user message with `persist:false` (:941-945), `api.chat.createRun` → `{runId}` (:981-987), `openChatRunStream(runId)` (:989; `web/lib/api/stream.ts:244`), handlers for `text_chunk`/`thinking_chunk`/`local_activity`/`tool_event`/`complete`/`error`/`ready` (:1083-1144). `finishStream` closes the EventSource inside `handleComplete` (:1002-1008, :1010-1014) — **the stream is torn down the moment `complete` arrives** (matters for B2, see §7). Delegation events arrive on the separate per-user notifications stream: `build_intent` (:745-789), `chat_answer` (:795-819), `integration_build_intent` (:824-844), `integration_ready` (:855-887).

**Composers: there are TWO.** The empty-state composer lives in page.tsx (textarea :1574-1582, Anexar/Captura/Cola URL controls :1583-1659, send/stop :1662-1685). The active-conversation composer lives inside ChatPanel (textarea chat-panel.tsx:542, quick-action chips :465-481, attachment chips :484-508, queued-message rows :510-533). The B5 chip must land in ChatPanel's composer (the empty state by definition has no sheets), but drafts flow between both via `composerDraft` (orchestration.ts:234, page.tsx:1327-1332, chat-panel.tsx:303-314).

**Mobile**: two FABs fixed bottom-right — side-panel opener (shown whenever `!showEmptyState`, page.tsx:1770-1779) and session history (:1782-1789). `MobileSidePanelDrawer` is an 85vh bottom sheet that simply hosts `<SidePanel/>` (mobile-side-panel-drawer.tsx:35-64). B6's "overlay = sheet feed" means whatever panel-host SidePanel becomes, the drawer inherits it for free.

**Transcript rendering** (ChatPanel): messages filtered to essential (chat-panel.tsx:385-460), `MessageBubble` (:617-885) renders `ThinkingBlock` (:739), the refusal affordance (:776-786), `TrustChip` (:829), `FeedbackButtons` when `metadata.traceId` present (:846-847), memories line when `metadata.memoriesUsed > 0` (:850-852), `stripCodeBlocks` keeps code out of the rail (:144-149). Live text renders via `StreamingChatSection` reading `streamingChat`/`streamingThinking` buffers (:1076-1089). Auto-scroll is `messagesEndRef.scrollIntoView` on message change (:252-254, :459) — the pattern B4's auto-follow reuses.

## 2. SidePanel content types, keying, VersionsPanel

`SidePanel` (side-panel.tsx:82) takes only `sessionId` + optional `onClose` (:60-65). Content selection today is a two-level switch:

1. `sidePanelState === 'integrate'` → early-return `IntegrationBuildPanel` (side-panel.tsx:405-411) — the existing precedent for whole-panel content swap.
2. Otherwise a 4-tab bar (`files`/`output`/`preview`/`versions`, tabConfig :100-105) driven by the global `sidePanelTab` (:107-108): preview :442-709, files :712-722 (`FileTreeView` :888), output :724-728 (`OutputPanel`), versions :730-753.

**Keying — everything is per-session maps indexed by `sessionId`:** `sessionPreviews[sessionId]` (:110-112), `sessionFiles[sessionId]` (:113-115), `sessionJobs[sessionId].artifactInstanceId/slug/shareable/status` (:117-125, :156-158). Preview URL derivation :127-137, ownership-append for cross-origin dev :146-151, effective status derivation :156-162, document-plane probe loop before pointing the iframe (:210-244), hot-reload via `reloadCount` (:247-261). Auto-switch to the preview tab when status flips to running (:177-185). Build-phase placeholder animation cycles 5 i18n phase keys every 4 s (:69-76, :187-199) with the AnimatePresence crossfade (:592-605).

**VersionsPanel machinery** (`web/components/artifacts/versions-panel.tsx`, 216 lines): keyed by `artifactId` only, lists via `api.artifacts.versionsList` (:56), restores via `api.artifacts.versionsRestore` (:75), `onAfterRestore` callback reloads the iframe (side-panel.tsx:733-746). It is git-sha-shaped (`sha.slice(0,7)` :189) and artifact-bound — consistent with bound decision B.C: do NOT route sheet revisions through this; a plain revision array on the sheet record.

## 3. Orchestration store shape

`web/stores/orchestration.ts` (1574 lines), zustand + `persist` (name `ekoa_orchestration`, version 4, :1545-1547).

**State** (interface :185-243): `sessions`, `activeSessionId`, `messages: Record<sessionId, ChatMessage[]>` (:191), `sessionJobs` (:194, shape :77-100), `sessionPreviews` (:197, shape :102-115), `sessionFiles` (:200), `sidePanelState: 'none'|'build'|'integrate'` (:204, union :137), `sessionSidePanelStates` per-session (:206), `sidePanelTab: 'files'|'output'|'preview'|'versions'` (:207), `isExecuting` (:208, global — one run at a time), `activeIntegrationBuilds` (:211), `activityMessages` (:214), `retryContexts` (:217), `streamingChat`/`streamingThinking` (:220-225), `queuedMessages` (:229), `composerDraft` (:234), `pendingAttachments` (:237), `pendingDelegation` (:240-243).

**Where "mode" lives**: `sidePanelState` is the mode. Set via `setSidePanelState` (:966-979), which also writes the per-session copy; `setActiveSession` restores it, forcing `'build'` for artifact-linked sessions (:657-690, artifact check :667-668). Callers: the build handlers (page.tsx:672, :710), the `?continue=`/`?featured=` flows (page.tsx:397, :430), and the SSE listeners (page.tsx:835, :866). B3's panel-host union (`panelContent` discriminated union per bound decision B.A) extends THIS store — precedent: `ActiveIntegrationBuild` (:144-153) already models per-session panel payload.

**`ChatMessage.metadata`** (:26-49): `jobId`, `traceId` (:28), `phase`, `isEssential`, `type`, `toolName/toolInput`, `memoriesUsed` (:34), `attachments`, `thinking`/`thinkingDurationMs` (:38-39), `localFileActivity` (:43), `refusal` (:48).

**Key actions**: `addMessage` (:755-805) — server-persists via `api.sessions.addMessage` unless `persist:false` (:790-799; chat/build runs pass false because the api pipeline persists server-side); `loadSessionMessages` (:807-833) maps server rows 1:1 including `metadata`; `setSessionJob` (:839-849); `setSessionPreview`; streaming buffer append/flush/clear (:1017+); queue ops (:1065+); `popLastUserTurn` (:1115); `initializeBuilderSession` (:1162); `hydrateSessionFromArtifact` (:1405). **`messages` is NOT in `partialize`** (:1548-1571) — transcripts always reload from the server; sheets that follow decision B.B (server-side on the session record) inherit reload-survival for free.

## 4. API side: SSE events, persistence, metadata, prompt assembly, classifiers

**Chat run pipeline**: route `api/src/routes/chat.ts` — `POST /api/v1/chat/runs` responds 202 `{runId}` and fires `executeChatRun` (:34-57), SSE at `GET /runs/:id/events` → `sseManager.attach` (:20-30), run re-sync `GET /runs/:id` (:59-65), cancel (:67-69). Runs are ephemeral, in-memory registry only (routes/chat.ts:6-7). Engine: `api/src/agents/chat.ts` — `createChatRun` (:49-62), `executeChatRun` (:69-282): user message persisted immediately (:121), context assembled (:124-132), tier decision floored at WORKHORSE (:137), stream loop with marker filtering on both channels (:192-207), final marker pass (:218-221), assistant message persisted with thinking metadata (:265-273), `finishComplete` emits `complete` (:274), THEN post-run extraction scheduled (:278).

**SSE event unions** (`shared/src/events.ts`, parity-gated: server-emittable == union == client-subscribed, :4-8): `ChatRunEvent` (:11-53) = `ready | text_chunk | thinking_chunk | tool_event | context_event | local_activity | complete | error`. `JobEvent` (:57-101) adds `routing | plan_step | preview_reload | artifact`. `NotificationEvent` (:179-198) = `ready | build_intent | chat_answer | integration_build_intent | integration_ready | usage_updated | change_request | branding_updated`. Emitters: `ChatStreamSink` (`api/src/agents/streaming.ts:48-83`), notification emitters (:131-160). `sse-manager.ts`: 200-event replay ring, replay only on Last-Event-ID reattach (:3, :34-49).

**Message persistence** (`api/src/agents/persistence.ts`): doc = `{_id, sessionId, role, content, timestamp, metadata?}` into the `messages` store, bumping session `messageCount`+`updatedAt` (:15-29). Contract: `SessionMessage` zod with `metadata: z.record(z.unknown()).optional()` passthrough (`shared/src/sessions.ts:6-16`); endpoints `getMessages`/`addMessage` (:104-116). Sessions also carry `lastContext` (persistence.ts:40-42).

**`memoriesUsed`/`traceId` surprise**: both are typed on the web `ChatMessage.metadata` (orchestration.ts:28, :34) and rendered when present (chat-panel.tsx:846-852; `FeedbackButtons` posts `traceId` as `runId` to `api.memories.submitSignal`, chat-panel.tsx:895), but **no writer exists anywhere** — neither web hooks nor `persistAssistantMessage` (whose metadata today carries only thinking, agents/chat.ts:265-273) ever sets them. BRIEF locked decision 10's "provenance (`memoriesUsed`/`traceId` exist on message metadata)" is true of the type, false of the data. B1 must start writing them (e.g. `traceId=runId` and a count out of `resolveMemoryInjection`, `api/src/memory/resolver.ts:113`) or the sheet footer shows nothing.

**Prompt assembly** (`api/src/agents/context.ts`) — where the canonical-context rule (B.B/locked 7) lands: `loadHistory(sessionId)` (:45-64) loads ALL session messages ordered by timestamp, filters provider-error turns, tail-dedups the last 3; `assembleRunContext` (:78-126) layers identity + memory + knowledge + prefetch + catalog + `lastContext` + history; `renderPrompt(history, message)` (:130-134) renders history as a `<conversation>` transcript block. The "one clear, testable function" for latest-revision-canonical is a change to `loadHistory` (collapse per-sheet turns to latest revision) or a sibling beside it.

**Classifiers — naming correction**: `classifyLocalFallback` and "conversation-types" (BRIEF B.D wording) **do not exist in this repo**. What exists: `api/src/agents/guided-build.ts` — `InBuildIntent = 'modification'|'integration-build'|'question'` (:18), `classifyInBuildIntent` (:34-50, FAST-tier `completeFast` + committed deterministic fallback to `modification`), `detectBuildIntent` (:53-67, keyword-regex fallback), called from `api/src/agents/build.ts:172-198`. All server-side and build-flow-specific. B.D's local heuristic (chip keep/clear) is NEW client code — nothing to extend; the guided-build fallbacks are the pattern to imitate, not a base.

**Model tiers**: `Tier = 'FAST'|'WORKHORSE'|'EXPERT'` (`api/src/llm/router.ts:23`), `decideForTier`/`decideForTask` (:179, :188); models in `api/src/config.ts:103-108` (FAST = `claude-haiku-4-5-20251001` :104). "Haiku call" in the BRIEF = FAST tier here; never name models outside config.

## 5. Post-run hook site for the reply-summary FAST call

`scheduleExtraction` (`api/src/agents/chat.ts:345-355`) wraps `runPostRunExtraction` (`api/src/memory/extraction.ts:86-149`) and is fired AFTER the terminal event, void-ed, at chat.ts:278 (normal completion) and :253 (build delegation). Extraction is the exact template B2 copies: one FAST-tier `runOneShot` with `decideForTier('FAST')` (extraction.ts:95-104), attributed per-run, best-effort catch (:106-109), double-gated by config + per-user setting (:87-90). The reply-summary hook sits beside the chat.ts:278 call site with sheet/revision context in hand (it has `runId`, `sessionId`, `cleanText`).

## 6. i18n mechanism + PT-PT examples

Zustand store `web/stores/i18n.ts` — `language: 'en'|'pt'` default `'pt'` (:28), persisted as `ekoa_language` (:40-41), `t(section)` returns a whole typed section (:34-37), `useTranslation()` spreads named sections (:47+). Dictionaries: `web/locales/pt.ts` + `en.ts` (~1598 lines each), typed by `web/locales/types.ts` — adding B strings means all three files. PT-PT conventions already in force: `'Introduza o seu nome de utilizador'` (pt.ts:53), `'…aberta no seu ecrã…'` (pt.ts:1456), `'Captura de ecrã do passo…'` (pt.ts:1460). Note: `por omissão` appears nowhere in pt.ts yet — first use will be Part B's. Language reaches the agent via the transport, and the page reads the i18n store, not settings (page.tsx:202-206 comment: settings `general.language` is NOT kept in sync — do not read it).

## 7. Part B implementation pins (per FLOW_PLAN slices B1-B7)

- **B1** (sheets persistence + shared schema + canonical-context fn + edit endpoints): `shared/src/sessions.ts` (Session zod gains `sheets` subdocs per bound B.B; or sibling `shared/src/sheets.ts` + `shared/src/index.ts` export) + contract test `shared/src/contract.test.ts`; `api/src/agents/persistence.ts` (sheet/revision writes beside `persistAssistantMessage`, message metadata gains `sheetId/revisionId/traceId/memoriesUsed`); `api/src/agents/context.ts` (`loadHistory` :45-64 is the canonical-context function to change/unit-test); `api/src/routes/sessions.ts` (65 lines, thin — edit endpoints slot here); suite ledger + diagram per repo rules.
- **B2** (reply_summary hook + SSE): `api/src/agents/chat.ts` beside :278/:345; `api/src/memory/extraction.ts` as the template; `shared/src/events.ts` union + parity gate; `api/src/agents/streaming.ts` sink. **Surprise that adjusts the plan**: the client closes the run EventSource the moment `complete` arrives (page.tsx:1002-1014), and chat-run streams are per-run and ephemeral — a post-run `reply_summary` on the run stream would be emitted into a ring nobody replays. Either (a) ride the per-user notifications channel like `chat_answer` (streaming.ts:143-146; the client listener pattern at page.tsx:795-819 carries `sessionId` for routing), or (b) keep the run stream open until `reply_summary`-or-timeout. (a) is the lowest tier and survives the client having navigated.
- **B3** (layout unification + panel-host union + blank transition): `web/app/(dashboard)/chat/[[...sessionId]]/page.tsx` :1214-1221 (visibility condition), :1719-1759 (layout), :595-596 (empty-state gate → "first send" transition hooks into `handleSendMessage` :1223 / `handleChatSend` :941); `web/stores/orchestration.ts` :137/:204-207/:966-979 (`panelContent` union); `web/components/builder/side-panel.tsx` :405-411 (the integrate early-return is the swap precedent to generalize into the host). No width morph exists to remove — drop the dead framer import (page.tsx:5) and animate panel entrance fresh.
- **B4** (sheet feed + desk surface + footer): new `web/components/chat/sheet-*.tsx` hosted by the B3 panel host; auto-follow pattern from chat-panel.tsx:252-254; footer provenance requires the B1 metadata writers (see §4 surprise).
- **B5** (summary cards + chip + in-place edit): `web/components/builder/chat-panel.tsx` `MessageBubble` :617-885 (card variant), composer :462-560 (chip); remember the second composer in page.tsx:1573-1687 stays chip-free (blank state has no sheets, per bound B.F). Continue-vs-new heuristic is new client code (see §4 — nothing named `classifyLocalFallback` exists to extend). Revision cards focus their sheet through the panel-host state, not DOM queries.
- **B6** (mobile overlay + i18n sweep): page.tsx:1765-1791 (FABs), `web/components/chat/mobile-side-panel-drawer.tsx` :62-64 (hosts SidePanel — inherits the panel host untouched); `web/locales/pt.ts` + `en.ts` + `types.ts`.
- **B7** (proof e2e): `web/e2e/` (real-UI login via `global-setup.ts` + `helpers/`; nearest prior art `chat-thinking.spec.ts`); reload-restore leans on messages-not-persisted-client-side (§3) so sheets must round-trip the server; summary-failure path = kill the FAST call, assert first-line degradation (extraction.ts:106-109 pattern).

**Surprise summary (plan deltas)**: (1) no AnimatePresence width morph exists — B3 is addition, not removal; (2) `reply_summary` cannot ride the chat-run stream post-complete without client changes — notifications channel or delayed close, decide in B2; (3) `memoriesUsed`/`traceId` are typed-but-never-written — B1 must add the writers or locked decision 10's footer is empty; (4) `classifyLocalFallback`/conversation-types don't exist — B.D heuristic is greenfield client code; (5) FLOW_PLAN's `web/src` path is wrong — paths as pinned above; (6) two composers exist — chip lands only in ChatPanel's.

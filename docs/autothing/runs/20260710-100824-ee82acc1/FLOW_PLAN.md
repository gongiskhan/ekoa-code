# FLOW_PLAN — batch-final consolidation (run 20260710-100824-ee82acc1)

Derives from `RUN_SPEC.md` (same dir). Profile: **feature** (operator-forced). Slices SERIAL (S1–S5 share api/tests infrastructure and the suite ledger; S6 needs all). Branch: `batch-final` off `main@ef786f8`. One checkpoint commit + tag per slice. rc-1 untouched; no merge to main.

| id | title | kind | size | tag | status |
|---|---|---|---|---|---|
| s0-reconcile | Reconciliation: branch, FINDINGS status column, stash disposition, F21 backfill test, remote-tag retry | api | 4 | bf-reconciled | pending |
| s1-f10-denylist | Deny-list resolver wired at composition root + org-admin CRUD + egress test [SECURITY-BOUNDARY] | api | 7 | bf-f10 | pending |
| s2-f26-detok | Whitespace/format-tolerant de-tokenization, return path only [SECURITY-BOUNDARY] | api | 4 | bf-f26 | pending |
| s3-f3-registo | Registo rows: login, build lifecycle, platform-CRUD mutations + targetIds wire fix | api | 5 | bf-f3 | pending |
| s4-f29-plan-failed | Automation plan: structured plan_failed instead of 500; full-stack error logging | api | 5 | bf-f29 | pending |
| s5-f7-failed-build | Honest failed-build serving state; Job.error on the wire | api | 4 | bf-f7 | pending |
| s6-proof | Phase-3 proof: boot stack, seed credential, J3 ×2 (verify OFF/ON), touched-journey probes, leave running | mixed | 6 | bf-proof | pending |

## Acceptance per slice

**s0-reconcile** — `git checkout -b batch-final ef786f8`. (1) FINDINGS.md gains a status column per RUN_SPEC taxonomy, covering ALL findings + the post-batch-1 open items (502-masks-401, gateway-apikey observation, /usage crash, StrictMode double-session, schema-coverage honor-system, SourceInput divergence, web-tests tsc exclusion → open/deferred). (2) Stash@{0}: tag `archive/stash-f11-pre-hardening` on `stash@{0}`'s commit, then `git stash drop` (superseded by da0d0fa — RUN_SPEC A3). (3) Backfill F21 wiring test: a stored active memory for the org/user appears in the assembled chat-run context (extend api/tests — assert via `assembleRunContext` or the chat agent's context seam), red-first not required (inherited-fix backfill; must be green at gate). (4) Retry `git push origin batch1-f25 --force-with-lease` ONCE (remote tag currently at broken 8a2a67b); if denied → RUN_LOG DECISION naming the exact command for the operator. (5) GATE: full `npm run ci:lane` green + `gate:sast`/`gate:secrets`/`gate:audit` green; FINDINGS committed; tag `bf-reconciled`.

**s1-f10-denylist** — per the design (RUN_SPEC A5): NEW `api/src/services/deny-list.ts` (list/add/remove + `denyListRulesetFieldsFor(orgId)` returning re-wrapped ciphertext, TTL cache + write invalidation); wire `setRulesetResolver` in `server.ts` buildApp; CRUD routes on `orgRouter` (`GET/POST/DELETE /api/v1/org/deny-list[/:id]`, org-admin+super-admin); shared descriptors + schemas (`DenyListEntryView` metadata-only, never cleartext); `logActivity('anonymisation','deny-list.add|remove')` ids-only. Tests FIRST (red): `api/tests/anonymise/ruleset-resolver.test.ts` (resolver carries ciphertext; deny-listed literal tokenized; audit `denyListAccessed`≥1; cache invalidation), `api/tests/contract/denylist.test.ts` (CRUD safeParse; literal in NO response; stored value ≠ plaintext; org-B isolation 404; builder 403), chokepoint test extension (store-backed resolver → outbound payload carries token never literal). schema-coverage COVERED += 3. Diagrams: ch03 endpoint map + ch17 ruleset flow. GATE: red→green, wall green, fresh review approve, codexSliceReview, tag.

**s2-f26-detok** — `api/src/llm/anonymise/index.ts` only: two-phase detok (exact pass unchanged; tolerant pass per-token bounded-gap regex — space-char gaps `[\s ]{1,4}`, digit-adjacent gaps `[ \t  ]{0,3}` + one `.`/`'` between digits, digit-edge guards), replacement = vault ORIGINAL byte-exact; `createDetokenizer` widened straddle bound + tolerant-prefix hold scanner. Detection/tokenisation untouched. Tests FIRST in `api/tests/llm/anonymise.test.ts`: spaced/dot-grouped/NBSP NIF, newline-wrapped PARTY token, streaming splits (mid-digit + mid-separator), negatives (`1.234.567` untouched; embedded-run guard), existing exact suites + 9 chokepoint specs green. GATE: red→green, wall, review, codexSliceReview, tag.

**s3-f3-registo** — writers: `auth/service.ts login()` → `auth.login`; `agents/build.ts` helper → `build.created|completed|failed{code}|cancelled` at every terminal (ids/codes only, NEVER description); `services/platform-crud.ts` org/session CRUD writers + fix `registoEntry.targetIds` (emit string-array ids or omit — A10). Tests FIRST: `api/tests/contract/registo.test.ts` (login row, session.create row, build.created row via mocked llm; org-B sees none; `RegistoListResponse.safeParse`; metadata-only assertions), `build.test.ts` extension (completed + VERIFY_FAILED rows). Diagram: ch12 audit taxonomy. GATE: red→green, wall, review, tag.

**s4-f29-plan-failed** — `automation/planner.ts` (4 throw sites → `{status:'failed',violations}`; pass-1 failure feeds corrective retry; pass-2 returns failed), `automation/service.ts` (`plan_failed` branch, nothing persisted), `routes/automations.ts` handle logs stack+cause, `shared/src/automations.ts` `reason` optional field, `web/stores/automations.ts` plan_failed branch. Tests FIRST: contract (4 bad-model-output cases → 200 `plan_failed`, never INTERNAL; happy path unchanged), planner unit (per throw-site; exactly-2-calls retry), service unit. GATE: red→green, wall, review, tag.

**s5-f7-failed-build** — `agents/jobs.ts` jobView emits persisted `error`; `shared/src/jobs.ts` declares it; `apps/serving.ts` document-branch gate: artifact not `active` AND no job ever completed AND latest job failed → `sendAppBuildFailedResponse` (200 HTML "A construção falhou.", no auto-refresh, no bundle refs; assets 503, no-store). Failed REBUILD over previously-good dist keeps serving old dist. Tests FIRST: `api/tests/contract/build-failure.test.ts` (4 cases per design). Diagram: ch07 serve states. GATE: red→green, wall, review, tag.

**s6-proof** — prereq: model credential (RUN_SPEC A7; operator notified). Boot via `.claude/skills/run-ekoa-code/driver.mjs` (or boot-b for the credentialed probe topology — whichever the probes expect; j3 expects boot-b at :4111), seed credential via F2 endpoint (`provision-credential.mjs`). Run `j3-build.mjs` phases setup→build1 (verify OFF)→served→j1→j5, then `j3b-followup.mjs` (verify ON). Then touched probes: j1, j2, j4, j6, j8b + registo assertions (new rows visible to org admin). Batched run-level independent test pass (api slices, Part 8.2) + built-in security-review + Codex checkpoint + evidence walkthrough (asciinema of the journey + screenshots of the served app). FINDINGS final statuses. Leave stack RUNNING; print served-app URL + admin login. GATE: J3 passes ×2; every probe green; ci:lane green; tag `bf-proof`.

## Verification / mechanics
- Deterministic wall per slice: `npm run lint && gate:chokepoint && gate:encryption-key && gate:garrison && typecheck && test` + `gate:sast` + `gate:secrets` + `gate:audit` (gate:ledger excluded — its Playwright/driver estate is the out-of-scope batch-2 e2e debt; see RUN_LOG DECISION); web build in ci:lane at S0/S6 (full lane) — intermediate slices may scope to api+shared suites for speed but the LANE must be green at every tag.
- The suite ledger (`scripts/suite-ledger-run.mjs`) tracks only the PORTED e2e artifacts; new vitest files do not enter its census.
- Reviews: fresh-context adversarial review EVERY slice; codexSliceReview S1+S2 (serial codex; API-key auth confirmed); run-level built-in security-review then Codex checkpoint in S6/final phase.
- Turn cap: 560 (7 slices × 80). deliberateRed/mutation OFF (feature profile). Design audit: kind-conditional skip on api slices; S6 evidence = walkthrough (served app browser pass) + asciinema.
- Known flakes: don't run ci:lane concurrently with colima/docker builds; legal-suite tail specs flake under load (re-run on quiescent machine before classifying).

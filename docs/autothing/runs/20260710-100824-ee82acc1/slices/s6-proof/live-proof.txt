PASS  AUTH: admin login 200 — status 200
PASS  AUTH: /me returns the user — role super-admin
PASS  F10: POST /org/deny-list 201 — status 201
PASS  F10: the literal is NOT echoed in the create response (metadata-only) — value write-only
PASS  F10: GET /org/deny-list lists the entry — 1 entries
PASS  F10: the literal is NOT in the list response (metadata-only) — never cleartext
PASS  F10: DELETE /org/deny-list/:id 200 — status 200
PASS  F3: GET /registo returns rows — 7 rows
PASS  F3: an auth.login row is present (login is audited) — login audited
PASS  F3: deny-list writes are audited (anonymisation.deny-list.add/remove) — CRUD audited
PASS  F3: Registo is metadata-only (no deny-list literal in any row) — no secret literal
PASS  SERVED: GET /apps/sales-crm/ serves 200 HTML — status 200
PASS  SERVED: it is the real app, not the Ekoa scaffold shell — no scaffold markers
PASS  SERVED: served-app CSP is frame-ancestors self (not the API default-src none) — frame-ancestors 'self'
PASS  HEALTH: metering anomalies 0, gateway unmetered 0 — claudeAuth.configured=false

=== LIVE DETERMINISTIC PROOF: 15/15 passed ===
